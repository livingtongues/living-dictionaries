packages:
  - 'packages/*'
  - site
  - e2e
  - '!packages/ids-import'
  - '!packages/functions'

onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
  # Linked workspace package; runs `tsc` via its prepare script to build dist/.
  - svelte-look
