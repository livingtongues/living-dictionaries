---
at: 2026-07-23T23:00
runs_on: mustang
provider: claude
model: claude-sonnet-6
notify: poly_pings
---

Assemble the next Living Tongues invoice DRAFT for Jacob (Polylingual Development). This job runs about every 4 weeks; today is a reminder day. The invoice bills Greg Anderson <livingtongues@gmail.com> at $31/hour. Do NOT modify code or commit anything for the invoice itself — this is a read-only analysis + reminder, delivered right here in this chat session. (The only commits you make are the re-arm bookkeeping in STEP 1.)

STEP 1 — RE-ARM FIRST so the 4-week chain never breaks. This is now a committed REPO cron (in ~/code/living-dictionaries/.cron/), no longer a machine-local `horse cron add` job. Before anything else, in ~/code/living-dictionaries:
  (a) Compute NEXT = 28 days from today (YYYY-MM-DD). Create `.cron/invoice-<NEXT>.md` with this exact frontmatter:
        ---
        at: <NEXT>T23:00
        runs_on: mustang
        provider: claude
        model: claude-sonnet-6
        notify: poly_pings
        ---
      and THIS ENTIRE prompt as the body, verbatim and unchanged.
  (b) Delete the OLD invoice file you were spawned from — the pre-existing `.cron/invoice-*.md` (the one that is NOT the new file you just created in (a)).
  (c) Stage EXACTLY those two paths — the new file and the deleted old file — with an explicit `git add <new> <old>` (do NOT `git add -A` or stage anything else), commit (e.g. "chore(cron): re-arm Living Tongues invoice"), and push.
Do all of (a)–(c) first, so the chain is guaranteed even if a later step fails.

STEP 2 — BILLING WINDOW. The previous Living Tongues invoice was #54, covering through ~Jun 26 2026. Bill from the day after the previous window through today (≈4 weeks).

STEP 3 — MINE JACOB'S WORK ONLY, excluding co-developers (e.g. Diego Córdova). In ~/code/living-dictionaries, Jacob's authors are 'jacob-8', 'Jacob Bowdoin', 'j-dev-agent'. Check BOTH branches svelte-5-migration and main:
  git log <branch> --since=<window-start> --author='jacob-8\|Jacob Bowdoin\|j-dev-agent' --date=short --pretty='%ad %s'

STEP 4 — ESTIMATE JACOB'S HOURS. Calibration from invoice #52: 17 of Jacob's commits corresponded to 84 billed hours (≈14 h/month; much of the time is team meetings + reviewing the co-developer's work, not just commits). Build a reasoned estimate = substantive feature/migration work + bug fixes + ongoing code review & meetings, sanity-checked against ~14 h/month across the window. State your assumptions and show the math.

STEP 5 — DRAFT 8–12 client-friendly bullet points describing the work in plain, benefit-oriented language (Greg is a linguist, not a developer).

STEP 6 — DELIVER, right here in this chat session — do NOT write an invoice draft file. Output: the billing period, your estimated hours, the dollar total (hours × $31), and the bullet list with ONE BULLET PER LINE (ready to paste into the finances Invoices editor "Work bullets" box). The finances "+ New" button assigns the next invoice number. Then push a short reminder to ntfy topic poly_pings summarizing: "Living Tongues invoice draft ready — ~<hours> h x $31 = $<total>; summary in this Horse session; create in finances Invoices (+ New > Living Tongues), paste, and email Greg from jwrunner7@gmail.com." Then stop.
