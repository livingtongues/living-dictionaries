# Log review — 2026-07-02 (new.livingdictionaries.app · window: last 24h)

## TL;DR
- **Healthy — the 107-error "spike" is one user × two deploys, not a regression.** All 107
  error/crash rows in 24h are the **single active contributor** (`eatb4running`, the `river` builder)
  catching **two back-to-back deploys** (builds 01:41 + 02:29 UTC) mid-session. Breakdown: **99×
  stale-chunk `Failed to fetch dynamically imported module`** (already `KNOWN_NOISE_PATTERNS`), **6×
  `sync_failed`** carrying a **Cloudflare 520** body, **2× `Internal Error`** (SvelteKit 500 on
  `/river/agents` during the swap). Real user-facing faults: **~0**; deploy collateral: all of it.
- **The dashboard will misread this as a spike.** The daily-error line
  (`build_daily_series`, `log-analytics.ts:389`) counts *all* error-level rows and does **not**
  exclude `is_known_noise` — so 07-02 renders **~107** when the real count is ~8. This is exactly
  **tutor's "noise vs real split"** proposal, now grounded in live LD data → Phase C.
- **One genuine coverage gap:** the 6 `sync_failed` rows got the **raw Cloudflare 520 HTML body** as
  their error message, so `classify_sync_failure` bucketed them `other` → **`error`** (unthrottled)
  instead of `network` → `warn` (throttled). Edge 5xx interstitials during deploy should classify as
  `network`. → Phase B.
- **Volume is still the API-v1 story, bigger than ever.** 7d: **v1_entry_updated 8,089**,
  **v1_media_attached 4,737**, **v1_relationship_created 3,305 (NEW — related-entries via API)**,
  v1_entry_deleted 859 — all one contributor's `api_key` pass on `river`. Still **invisible on
  `/admin/analytics`** → the ★ API-v1 panel stays the #1 open dashboard item.
- **Geo tax worsened (deploy churn + distance):** 104/105 sessions Kuala Lumpur → Boston origin.
  CWV p-avg: **TTFB 1,561ms (poor)**, FCP 3,311, **LCP 5,054ms (poor, >4s)**, INP 52 (good), CLS 0.
  Up from yesterday — cold reboots after each deploy inflated the tail. Capability 100% `opfs-worker`.

## 1. Errors & crashes
Window 24h: **107** error/crash rows — **one user** (`eatb4running@gmail.com`, `river` contributor),
concentrated **02:33–03:23 UTC**, straddling two deploys. Clustered:

| sev | message | n (24h) | users | source | verdict |
|---|---|---|---|---|---|
| ⚪ noise | `Failed to fetch dynamically imported module: …/nodes/14.*.js` + `…/7.*.js` | 99 (51+48) | 1 | client | **Already `KNOWN_NOISE_PATTERNS`** — stale chunks after deploy; SvelteKit hard-nav recovers |
| 🟡 P3 | `sync_failed` (Cloudflare **520** body) | 6 | 1 | client (worker) | **Open — classification gap**: 520 HTML → `other`/`error`; should be `network`/`warn` |
| 🟠 P3 | `Internal Error` (SvelteKit 500 on `/river/agents`) | 2 | 1 | client | Deploy-collateral 500 during swap; covered by the deploy-day-errors-fold backlog item |

### The deploy collision (root cause of all three clusters)
- **Two builds shipped ~48 min apart:** `app_version` **1782956489947 = 02:29:04 UTC** was the fresh
  one; the user still had **…489947 (01:41)** / an earlier bundle loaded. The version-poll toast
  (`+layout.svelte:73`, `pollInterval: 60_000`) fires a non-forcing "Reload" toast — correctly *not*
  forced, so an in-progress editor keeps navigating on stale chunks, each failed import logging an
  `error` until they reload.
- **520 = origin briefly unreachable during the container swap.** The worker dict-sync
  (`report-dict-sync-failure.ts`) fetched `/changes`, got Cloudflare's **"Web server is returning an
  unknown error · Error code 520"** HTML page as the response body, and shipped it as the `error`
  string. Because the message is HTML (not `Failed to fetch`/`NetworkError`),
  `classify_sync_failure` (`sync-failure-classify.ts:29`) fell through to **`other` → `error`**,
  unthrottled → 6 rows. **Should be `network` → `warn` → throttled.**
- **`Internal Error` ×2** — SvelteKit 500 on `/river/agents` navigation at 03:23, same session
  (`26d61b14…`), same swap window. A load ran against a mid-swap/stale server. Hard to distinguish
  from a real 500 at the message level, but the deploy-marker correlation is unambiguous.
- **No non-deploy errors this window.** No Keyman `firstElementChild` recurrence, no anon-401,
  no `SelectedDict` fault. Yesterday's open items saw no new hits.

## 2. Usage & engagement
| window | sessions | signed-in users | notes |
|---|---|---|---|
| 24h | 17 | 2 | `eatb4running` (13 sessions) + Jacob (1) |
| 7d | 105 | 3 | one contributor + Jacob + one prior |

**Top events (7d):** v1_entry_updated **8,089** · v1_media_attached **4,737** · heartbeat 3,819 ·
**v1_relationship_created 3,305 (NEW)** · v1_entry_deleted 859 · perf 533 · navigation 473 ·
session_start 109 · dictionary_opened 62 · v1_entries_written 51 · **aborted 50 (now `info` — the
demotion working)** · auth_login 20 · v1_sentence_updated 14 · entry_opened 13 · dictionary_created
10 · v1_feedback_received 7. **Top routes 24h:** `/river/entries` ×87, then `/river/agents`,
`/river/sources`, `/river/settings`, several `/river/entry/*`, `/admin/messages`.

**Who's doing what**
- **Admins (named):**
  - **Jacob Bowdoin** (level 2) — **26 events / 1 session, 06:36→06:43 UTC** (7 min). A quick triage
    dip into **`/admin/messages`** (opened one thread `22db9f38…`) + `/account`. No errors, no edits.
  - **Diego, Anna, Greg** — not active this window.
- **Everyone else (anonymous / aggregate):** **one** non-admin contributor (Kuala Lumpur) continuing
  to build **`river`** — **12,715 events / 13 sessions**, active 07-01 22:35 → 07-02 10:34. Drove the
  **entire API-v1 pass** (8,089 entry updates + 4,737 media attaches + **3,305 relationship creates**
  + 859 deletes, `via: api_key`) plus manual UI passes across `/entries`, `/agents`, `/sources`,
  `/settings`, `/import`, `/export`, `/history`. **All 107 errors were this session's deploy
  collateral.** No other non-admin traffic of note.

## 3. Performance & geography
- **Perf (7d):** `page_load` n=108, avg **2,495ms**, max 17.9s (cold-boot tail); `search` n=4, avg
  **14ms** (warm Orama — excellent).
- **Core Web Vitals (7d avg):** **TTFB ~1,561ms (poor)** · FCP ~3,311ms · **LCP ~5,054ms (poor,
  >4s)** · INP 52ms (good) · CLS 0 (good). **Worse than yesterday** (TTFB 1,074 → 1,561; LCP 2,981 →
  5,054) — the two deploys forced cold reboots + snapshot re-fetches during the window, inflating the
  tail on top of the standing distance tax.
- **Geo:** **MY / Kuala Lumpur = 104 sessions** vs US / Portland = 1. All real traffic is Malaysia →
  Boston origin (`page_load` TTFB avg 1,436ms) — the textbook distance tax, still only visible via a
  hand-run split → the geo-split-CWV backlog item remains the right fix.
- **Capability:** 100% `opfs-worker` (109/109 session_starts); no below-capability / Safari-<15.4.

## 4. Health & housekeeping
- **Volume:** 07-02 = **12,203 rows (11,321 server** — the v1 pass; 404 heartbeats; **107 errs**).
  07-01 = 4,979 (4,339 server). 06-30 = 3,020. Total **22,607** rows, all since the 06-26 cutover.
- **Retention:** `log_retention_ran_at` = **2026-07-02T17:00 ✅**; oldest row **06-26** (6d, ≤14d) —
  nothing to prune yet. **DB size 15.8 MB.**
- **Noise ratio:** heartbeats now only ~17% of 7d rows — **v1 audit rows dominate** (one `info` row
  per entry/media/relationship op; 16k+ v1 rows in 7d from one pass). Intentional audit logging;
  worth a sample/rollup only if bulk imports scale much further.

## 5. Coverage gaps (Phase B)
1. **🟡 Classify edge 5xx interstitials as `network` in `classify_sync_failure`.** Today's 6
   `sync_failed` rows carried a **Cloudflare 520 HTML page** as the error string and fell through to
   `other` → `error` (unthrottled). <File path="site/src/lib/db/sync/sync-failure-classify.ts" line="44" />
   only matches `Failed to fetch`/`NetworkError`/`Load failed`. **Add** a predicate for edge 5xx
   interstitials — e.g. message contains `Web server is returning`, `error code 5`, or a
   `<!DOCTYPE html>` body mentioning `cloudflare` — → return `network` (warn + 10-min throttle). Turns
   a deploy-swap blip from 6 unthrottled `error` rows into ≤1 throttled `warn`. Grounded in today.
2. **📊 Daily-error line counts noise (also Phase C).** `build_daily_series`
   (<File path="site/src/lib/db/server/log-analytics.ts" line="389" />) sums *all* `ERROR_LEVELS` rows
   with no `is_known_noise` exclusion, so today's chunk-load burst inflates 07-02 to ~107 vs ~8 real.
   The per-cluster `is_noise` flag already exists (line 648) — just needs to feed the daily headline.
3. **⚪ `Internal Error` server 500s during deploy** are indistinguishable from real 500s at the
   message level; the **deploy-day-errors-fold** backlog item (tag errors within N-min of a deploy
   marker / on a stale `app_version`) is the right home — no new event needed.

## 6. Dashboard improvements (Phase C + D)

**Phase C — fresh, grounded proposal:**
- **★ Split real vs noise in the daily-error headline & Errors line.** *(NEW, grounded in today.)*
  The dashboard's daily `errors` series counts known-noise rows, so **07-02 will render a false ~107
  spike** (99 stale-chunk noise + 8 real). The `is_noise` predicate is already computed per error
  cluster (`log-analytics.ts:648`); apply the same `is_known_noise || is_expected_error_response`
  fold to the daily-series `errors` SUM (or return `errors` + `real_errors` and plot both). A
  deploy-day chunk burst should never masquerade as a regression. → appended to backlog. **This is
  the LD instance of the cross-repo "noise vs real" theme (tutor/house).**
- **★ (still #1 open) API-v1 activity panel.** Verified **still unbuilt** — no `v1_` string appears in
  `log-analytics.ts`, and `v1_*` names aren't in `ALL_TRACKED_EVENTS`
  (<File path="site/src/lib/debug/log-events.ts" line="30" />). With **16k+ v1 rows in 7d** (now incl.
  `v1_relationship_created`) it's the single largest, richest, and *least visible* signal. Same spec
  as filed: group `v1_*` by type / dictionary (`context.dictionary_id`) / channel (`context.via`),
  success-vs-error, per-day trend. Serves human/agent editing parity.

**Phase D — sibling cross-pollination (read house 07-02 + tutor 07-01 reviews + backlogs):**
- **Port from house — "Server faults / schema-drift" strip** *(house 07-02, explicitly flagged for
  LD + tutor).* Isolate `source='server' AND level IN ('error','crash')` clustered by
  `context.route`, newest-first, with `SqliteError`/`no such table`/`no such column` highlighted as
  the schema-drift class. **Fits LD directly:** LD hit the `dictionary_partners` schema-drift crash
  earlier and today's `Internal Error` server 500s would land here cleanly, out of the mixed client
  error stream. `log-analytics.ts` already reads `source`. → appended (ported from house).
- **Port from tutor — noise/real split on the Errors panel** *(tutor 07-01).* Same fix as my Phase C
  #1 above — tutor filed it after 18 ResizeObserver rows read as real errors; LD's 99 chunk-load rows
  are the identical class. Treating this as the **shared cross-repo "noise-classification" mini-project**
  (client ignore-list + server reclassify + dashboard split) is the convergent theme across all three.
  → appended (ported from tutor / mutual).
- **N/A this run:** tutor's native-memory/RSS panel (LD is web-only); house's `/admin/revenue`
  (no payments).
- **LD wins to flag back to siblings:** the **★ API-v1 activity panel** is still an **LD-first** idea
  — house said N/A (no public write-API), but **tutor has a write surface that could use it**; flag it
  for tutor's next run. Standing LD wins: bot/headless exclusion across all metrics, the CWV panel,
  pipeline-liveness + event-coverage strips, deploy markers on the timeline.

## Recommendations / action items
- [x] ✅ **SHIPPED — Classify Cloudflare/edge 5xx interstitials as `network` in `classify_sync_failure`**
  (`sync-failure-classify.ts`) — status-based (502/504/520–527) + a CF-interstitial body fallback for
  paths that lost `.status`; demotes deploy-swap 520s from `error`×6 to a throttled `warn`. Tests added.
- [x] ✅ **SHIPPED — Split real vs noise in the daily-error series** (`log-analytics.ts`): registered
  `is_noise_msg()` SQLite fn, added `real_errors` to `DailyPoint`/`totals`, and pointed the errors
  line + "Errors" tile + `error_rate` insight at real faults (with a "+N noise" annotation). Cold
  rollup days fall back to raw `errors`. Tests + stories updated; visually verified (light + dark).
- [ ] 📊 **Backlog (still #1):** API-v1 activity panel — verified still unbuilt; top open item.
- [ ] 📊 **Backlog (ported from house):** "Server faults / schema-drift" strip (`source='server'`
  errors clustered by route, SqliteError-highlighted).
- [ ] ⚪ **Backlog (existing):** deploy-day-errors-fold covers today's `Internal Error` 500s — no new work.
- [x] ✅ Verified: `Failed to fetch dynamically imported module` already in `KNOWN_NOISE_PATTERNS`;
  `aborted` demoted to `info` (50 rows this 7d confirm it); retention cron ran 17:00 UTC (oldest 06-26);
  version-poll "Reload" toast wired (`+layout.svelte:73`); capability 100% opfs-worker.

## Trend context (7d / 30d)
| day | rows | server | heartbeats | errors+crash | note |
|---|---|---|---|---|---|
| 2026-07-02 | 12,203 | 11,321 | 404 | **107** | 1 user × 2 deploys — 99 noise + 8 real |
| 2026-07-01 | 4,979 | 4,339 | 278 | 4 | |
| 2026-06-30 | 3,020 | 1,502 | 1,328 | 3 | |
| 2026-06-29 | 1,852 | 3 | 1,734 | 7 | |
| 2026-06-27 | 8 | 0 | 0 | 0 | |
| 2026-06-26 | 545 | 25 | 75 | 11 | cutover day |

Data goes back only to the 06-26 cutover (30d == 7d == all). The 07-02 row-count jump is the
API-v1 bulk pass on `river` (11,321 server rows), not a noise loop; the error count is deploy
collateral against the one active editor, not a fault.
