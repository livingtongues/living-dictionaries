# Log review — 2026-07-23 (livingdictionaries.app · window: last 24h)

*Read-only production review of `logs.db` + `shared.db`, ending **21:03 UTC**. No application code or
production data changed. Current build: **`1784815674582`**. Findings were checked against current
sync/source/recovery code, tests, recent commits, standing decisions, the analytics page/reader, and
the July 22 house/tutor reviews.*

## TL;DR

- 🟠 **One active admin is genuinely wedged:** Dr. Greg Anderson's current-build admin mirror halted
  in three sessions on a legacy duplicate `dictionary_roles` natural key. Logging out/in did not
  repair it. Current source handles newly-created duplicates, but not a clean old local loser unknown
  to the server. Filed `.issues/admin-sync-legacy-dictionary-role-wedge.md`.
- 🟠 **One contributor hit a real edit-flow failure:** two `UNIQUE constraint failed: sources.slug`
  errors while repeatedly saving a new source in `iipay-aa`. The modal closes without validating the
  slug or handling failure. Filed `.issues/source-slug-duplicate-feedback.md`.
- ✅ **The OPFS session-bound fix held.** There were 362 viewer file replacements across 356 sessions,
  not yesterday's 36-in-one-session loop; one current-build viewer recovered and no signed-in editor
  exhausted recovery.
- 🟢 Human usage was healthy: **731 sessions / 44 signed-in users**, 170 entries created, 1,369
  searches, and 288 audio plays. The corrected finalized `real_errors` rollup fell from **2,233 on
  July 21 to 72 on July 22**, confirming the zombie exclusion.
- 🟢 Host and retention are healthy: CPU **4.7% avg / 38.3% hottest five minutes**, RAM **29.4% /
  55% max**, swap 116 MB, disk 18.1%, retention fresh at 20:10 UTC.

## 1. Errors & crashes

| Severity | Cluster | Reach | Verdict |
|---|---|---:|---|
| 🟠 P2 | Admin `dictionary_roles` natural-key collision | 4 terminal rows / 3 sessions / 1 admin / current build | Greg's admin sync halted repeatedly. Server canonicalization and normal pushed-loser convergence are already implemented/tested; the missing case is a clean legacy local loser the server cannot name. Filed issue. |
| 🟠 P2 | `UNIQUE constraint failed: sources.slug` | 2 rows / 1 session / 1 contributor | `iipay-aa`, Android, Add source → Save repeated. The constraint is correct; duplicate validation and user feedback are absent. Filed issue. |
| 🟡 P3/watch | `Unable to decode audio for waveform` | 27 rows / 3 sessions / 1 contributor | Enriched context consistently says `TypeError: Failed to fetch`, `source:"url"` — network delivery, not codec. Playback remains independent. Standing decision says revisit only at volume; today's concentration is one user repeatedly opening recordings. |
| 🟡 P3/watch | `RangeError: Maximum call stack size exceeded.` | 2 rows / 1 anonymous iPhone Chrome session | Fresh current build, `daan-davi` home then entries, minified `Pk/Nk` recursion. Same low-frequency family as the standing Opata watch, but a different dictionary. No confident fix; escalate only on a second real user or reproducible current-build recurrence. |
| ✅ fixed/watch | OPFS viewer replacement loop | 362 replacements / 356 sessions; 1 recovered; 8 exhausted / 3 sessions | Commit `1cd1caf6`'s page-session permit held: the population is crawler breadth, not a single runaway session. Current-build exhaustion was one anonymous Safari `arayeke-taino` viewer; no signed-in editor exhausted. |
| ⚪ known noise | `sync_failed` | 1,691 rows, overwhelmingly the ruled stale-tab family | One forgotten-laptop null-session stream plus Greg's live admin halt above. The stale stream remains excluded from `real_errors`; do not re-propose forced reload. |
| ⚪ crawler/deploy noise | `Internal Error`, `Rejected`, ServiceWorker/dynamic-import/CSS failures | low-frequency breadth | Anonymous Googlebot/prerender and deploy-tail strings already covered by standing decisions/classifier. |

The current build is not globally clean because it contains Greg's admin-sync halt and the one
anonymous stack-recursion watch. It has no broad signed-in application regression.

## 2. Usage & engagement

| Window | Human sessions | Signed-in users | Median span | p90 span |
|---|---:|---:|---:|---:|
| 24 hours | 731 | 44 | 32 s | 102 min |
| 7 days | 4,124 | 89 | 30 s | 68 min |

The raw hot database retains 14 days, so this run does not present a fabricated raw 30-day session
span; the dashboard's cold+hot materialization remains the authoritative 30-day view.

Human events: **170 entries created**, 2 deleted, 1,369 searches, 629 dictionary opens, 383 entry
opens, and 288 audio plays. Top navigation included `/` (122), `/jejueo/entries` (83),
`/iipay-aa/entries` (72), `/boienen-old-buhi-langua/entries` (41), `/admin/messages` (29), and
`/chat` (21).

### Who's doing what

- **Dr. Greg Anderson** — active throughout the window across 22 sessions, using messages,
  dictionaries, users, chat, contributor/import pages, and account switching. His apparent 3,242
  events / 1,695 errors include the ruled stale-tab storm, but the four current-build
  `sync_halted_repeated_failure` rows are real and persisted after logout/login.
- **Cailie Keating** — 10 sessions, visiting chat, messages, dictionaries, and users. Three error
  rows; no broad product fault or edit loss signal.
- **Diego Córdova** — 11 sessions across messages, admin, and chat; **zero errors**.
- **Jacob Bowdoin** — 4 sessions across home/about, admin dictionaries/messages, chat, and an
  Eastern Pomo import page; **zero errors**.
- **Everyone else (anonymous aggregate)** — 40 additional signed-in users plus public visitors.
  Sixteen people created 173 entries in the unfiltered count (170 after human/bot filtering);
  no individual non-admin is named. The source-slug failure affected one active contributor.

## 3. Performance & geography

| Metric | p50 | p90 | p95 | n |
|---|---:|---:|---:|---:|
| Page load | 829 ms | 3.79 s | 6.28 s | 556 |
| Search | 14 ms | 72 ms | 156 ms | 1,369 |
| Dictionary boot | 1.01 s | 4.49 s | 7.71 s | 519 |

Search remains excellent; page-load and dictionary-boot tails are broadly consistent with yesterday.
All 708 human `session_start` capability rows reported **`opfs-worker`** — no below-capability
fallback population.

Top human-session areas were California (74), Guangdong (34), Arizona (29), Oregon (28), New York
(21), North Carolina (17), Mexico City (15), and Virginia (14). Geography remains globally
distributed. The existing dashboard already provides the Boston-distance/TTFB and route splits; no
new panel is justified by today's mix.

## 4. Health & housekeeping

- `logs.db`: **2.34 GB**, 1,387,839 hot rows; oldest **2026-07-09 20:10 UTC**, newest 21:03 UTC —
  exactly the 14-day retention boundary.
- Retention ran **2026-07-23 20:10 UTC** and rollups are finalized through July 22.
- July 23 was at 137,284 raw rows by 21:02 UTC, continuing the already-ruled Googlebot crawl at
  roughly flat volume. Disk remains 18.1%; no storage action.
- The finalized corrected `real_errors` metric is **72 on July 22**, versus 1,550 raw client errors.
  This confirms commit `d6871c60` removed null-session zombie inflation from the headline.

## Host resources (past 24h)

Plenty of headroom: **282/288 samples**. CPU averaged **4.7%**, with a **38.3%** hottest five-minute
window; load1 briefly reached 2.45 on two cores but was not sustained. RAM averaged **29.4%** and
peaked at **55%**; swap peaked at 5.7% and ended at **116 MB**. Disk is **18.1%**. No resource action.

## 5. Coverage gaps (Phase B)

1. **Source creation failure → `source_save_failed` warn.** `EditSource.svelte` currently awaits
   `writes.insert_source` and then closes unconditionally. Add handled validation/error state and log
   `{dictionary_id, operation, failure_kind, slug}` (never citation text). This would have made the
   contributor's duplicate collision actionable without session reconstruction.
2. ✅ **Admin sync halt coverage already sufficient.** `sync_halted_repeated_failure` included engine,
   exact constraint, consecutive count, build, user/session, and breadcrumbs. No new event is needed;
   fix convergence.
3. ✅ **OPFS recovery coverage already sufficient.** Replacement/recovered/exhausted events proved the
   session bound and separated one current-build exhaustion from crawler breadth.
4. **No new broad inventory item.** Import/media upload failures, dictionary writes, sync, snapshots,
   retention, SES lifecycle, and host resources are already structured in current source.

## 6. Dashboard improvements (Phase C + D)

**Phase C — clean no-op.** Today's two P2s require product fixes, not operator panels. The dashboard
already shows current/stale builds, error breadth/loops/bot share, OPFS boot outcomes, sync health,
pipeline liveness, host/storage health, route performance, and Core Web Vitals. The raw headline
spot-check passed: finalized July 22 `real_errors=72` correctly reflects the shipped zombie exclusion.
The highest-value ready backlog item remains **persist one `admin_analytics_computed` event per
uncached analytics compute**; current source still only emits ephemeral `[profile]` console timings.
No load regression was visible in telemetry.

**Phase D — no new inbound port.** House's July 22 portable focus was new-vs-returning readership and
actionable revenue audiences; LD already has true monthly unique-visitor materialization and no
payments. Tutor's new panels are learner/chat/native-mobile specific. Neither sibling shipped a fresh
generic dashboard improvement LD lacks. LD's corrected zombie-free `real_errors` rollup and
page-session-bounded OPFS replacement remain useful patterns for the siblings where their topology
matches.

## Recommendations / action items

- [ ] 🟠 **Repair legacy clean duplicate roles during admin full-resync** using
  `.issues/admin-sync-legacy-dictionary-role-wedge.md`; this is blocking an active administrator on
  the current build and survives logout/login.
- [ ] 🟠 **Keep source creation open and actionable on duplicate slug** using
  `.issues/source-slug-duplicate-feedback.md`; validate against existing sources, offer reuse, and
  only close after a confirmed write.
- [ ] **Keep editor OPFS recovery deferred but explicit** in
  `.issues/dict-boot-persistent-opfs-recovery.md`; viewer recovery is now bounded/proven, while safe
  editor recovery still needs an external durable write ledger.
- [ ] **Low priority: persist uncached analytics compute cost** via the existing
  `admin_analytics_computed` backlog item so dashboard load performance can be trended.

## Trend context (7d / 30d)

The metric-honesty change is now visible: finalized `real_errors` collapsed on July 22 while raw
errors stayed crawler/stale-tab heavy.

<svg viewBox="0 0 500 175" width="100%" style={{maxWidth:'540px'}} role="img" aria-label="Daily sessions and real errors, July 17 through July 23">{[['07-17',495,1510],['07-18',419,1131],['07-19',372,1293],['07-20',713,1821],['07-21',648,2233],['07-22',490,72],['07-23*',485,88]].map(([day,sessions,errors],i)=><g key={day}><rect x={28+i*66} y={140-Number(sessions)/713*90} width="22" height={Number(sessions)/713*90} rx="2" fill="#6366f1"><title>{sessions} sessions</title></rect><rect x={50+i*66} y={140-Number(errors)/2233*90} width="22" height={Number(errors)/2233*90} rx="2" fill="#f59e0b"><title>{errors} real errors</title></rect><text x={50+i*66} y="154" textAnchor="middle" fontSize="8" fill="currentColor" fillOpacity="0.65">{day}</text></g>)}<line x1="18" y1="140" x2="490" y2="140" stroke="currentColor" strokeOpacity="0.25" /><text x="20" y="13" fontSize="9" fill="#6366f1">■ sessions</text><text x="115" y="13" fontSize="9" fill="#f59e0b">■ real errors (classifier fixed before 07-22 finalization)</text></svg>

| Day | Sessions | `real_errors` | Raw client errors |
|---|---:|---:|---:|
| Jul 17 | 495 | 1,510 | 1,521 |
| Jul 18 | 419 | 1,131 | 1,136 |
| Jul 19 | 372 | 1,293 | 1,332 |
| Jul 20 | 713 | 1,821 | 1,827 |
| Jul 21 | 648 | 2,233 | 2,258 |
| Jul 22 | 490 | **72** | 1,550 |
| Jul 23* | 485 | 88 | 1,498 |

*July 23 is partial and not finalized. The pre-July-22 `real_errors` values were calculated before the
new classifier/exclusion shipped; they are not retrospectively recomputed.*
