# Log review — 2026-07-24 (livingdictionaries.app · window: last 24h)

*Read-only production review of `logs.db` + `shared.db`, ending **21:03 UTC**. No application code
or production data changed. Current build: **`1784895759772`**. Findings were verified against
current source, recent commits, standing decisions, the analytics reader/page, open issues, and the
July 23 house/tutor reviews.*

## TL;DR

- 🟡 **One new real error:** Cailie hit one current-build Svelte `state_unsafe_mutation` while
  leaving an admin message thread. The markdown editor already has a guard intended for this exact
  blur/teardown path, so the residual was filed as
  `.issues/markdown-editor-residual-state-unsafe-mutation.md` rather than “fixed” by guesswork.
- ✅ **Yesterday's two P2 fixes are deployed and quiet.** Greg's legacy duplicate-role admin-sync
  halt has not recurred after the fix build, and `sources.slug` has no recurrence after duplicate
  validation, error feedback, and `source_save_failed` telemetry shipped. Greg's real browser still
  needs a positive sync-recovery confirmation.
- 🟢 Human usage was healthy: **842 sessions / 42 signed-in users**, 43 entries created, 919
  searches, 360 audio plays, and 696 dictionary opens. All 832 capability-bearing human sessions
  used the OPFS leader-worker database tier.
- 🟢 The corrected finalized error headline remains honest: **108 `real_errors` on July 23** versus
  1,916 raw errors dominated by bots and stale/deploy noise. July 24 is at 69 real errors so far.
- 🟢 Host/retention health is comfortable: CPU **3.7% average / 47.3% hottest five minutes**, RAM
  **29.2% / 52.5% max**, 97 MB swap, disk **18.8%**, and retention ran at 18:25 UTC.
- **Phase C/D clean no-op.** Neither sibling shipped a new portable dashboard win. Persisting
  `admin_analytics_computed` remains the highest-value ready analytics backlog item.

## 1. Errors & crashes

| Severity | Cluster | Reach in 24h | Verdict |
|---|---:|---:|---|
| 🟡 P3 | `Uncaught Error: https://svelte.dev/e/state_unsafe_mutation` | 1 row / 1 session / 1 admin / current build | Cailie opened a message thread and returned to `/admin/messages`; Tiptap's blur transaction entered Svelte's unsafe mutation guard. `MarkdownEditor.svelte` already defers its `tick++` in a microtask specifically for this path, so the existing defense is insufficient. Focused issue filed. |
| ✅ fixed / verify | Admin `dictionary_roles` natural-key halt | 1 old-build row at 21:13 UTC July 23; 0 after fix deploy | Yesterday's four-row wedge aged partly into this rolling window. Commit `10aacd6c` shipped clean-legacy-loser convergence and tests. No current-build recurrence; still confirm Greg's browser is actively syncing. |
| ✅ fixed | `UNIQUE constraint failed: sources.slug` | 0 | Commit `10aacd6c` shipped preflight duplicate handling, user feedback, `source_save_failed`, tests, and visual stories. No recurrence. |
| 🟡 transient | Cloudflare 524 / short network-error burst | 4 endpoint rows in one Diego session; 5 `network error` rows in one Jacob session | Each burst was seconds long and activity continued. No server-wide fault pattern. |
| 🟡 standing watch | `Unable to decode audio for waveform` | 2 rows / 2 sessions / 2 users | Both on yesterday's build; standing diagnosis remains transient audio URL fetch, not codec failure. Playback is independent. Volume fell from yesterday's 27 rows. |
| ⚪ known bot noise | `Internal Error` / `initial dict sync failed` / `Rejected` / `leader_boot_failed` | 64 / 40 / crawler breadth / 401 warns | Predominantly Nexus 5X Googlebot and Applebot boot/deep-link behavior. Existing standing rulings and classifier cover it. |
| ⚪ deploy/media noise | stale CSS/module loads and interrupted `play()` promises | low-frequency | Eleven builds appeared in the window; failures are thin across versions and already classified. Interrupted play/pause is benign browser media behavior. |

The current build is not perfectly clean because of the one admin markdown-editor error, but there
is no broad signed-in regression, crash, dirty-row warning, or repeated current-build halt.

### Dictionary recovery

`dict_boot_recovery_exhausted` produced 20 rows across 10 sessions, while the replacement stream was
again broad crawler traffic rather than a one-session loop. The detailed current-build replacement
rows were Nexus 5X Googlebot sessions reporting malformed ephemeral OPFS files in `gta`/`gutob`.
There was no new signed-in editor recovery exhaustion. The existing viewer recovery remains bounded;
safe editor recovery remains tracked in `.issues/dict-boot-persistent-opfs-recovery.md`.

## 2. Usage & engagement

| Window | Human sessions | Signed-in users | Median session span | p90 span |
|---|---:|---:|---:|---:|
| 24 hours | 842 | 42 | 8 s | 51 min |
| 7 days | 4,421 | 90 | 23 s | 63 min |
| 30 days | 8,343 raw-hot-window sessions | 136 | — | — |

The “30-day” raw query can only see the retained hot/archive material available to this probe and
must not be read as a full monthly audience count; the dashboard's forever daily/monthly
materializations are authoritative for true 30-day and monthly uniqueness.

Human events: **1,447 navigations**, 919 searches, 696 dictionary opens, 360 audio plays, 318 entry
opens, **43 entries created by 9 people**, and 3 entries deleted. Top destinations included `/` (87),
`/admin/messages` (47), `/chat` (33), `/iipay-aa/entries` (32), `/sugtstun/entries` (30),
`/nahuatl` (30), and `/boienen-old-buhi-langua/entries` (27).

### Who's doing what

- **Jacob Bowdoin** — active 23:51–15:38 UTC across 40 sessions during a large implementation/deploy
  day: analytics/storage/translate/messages, Enxet import and corpus work, Woleaian export/import/
  sources/settings, and entry review. His errors were a short network burst plus opaque script/data
  navigation noise, not an editing halt.
- **Diego Córdova** — active 23:45–20:51 UTC across 13 sessions in messages/chat and Enxet/Ponca
  import, sources, texts, and entries. A four-request Cloudflare 524 burst at 18:19 UTC was transient.
- **Dr. Greg Anderson** — active 21:12–20:07 UTC across 18 sessions in chat, admin messages,
  dictionaries, storage, translate, and import. The only terminal sync row was the tail of yesterday's
  pre-fix wedge; no halt occurred on the deployed fix build. Positive recovery confirmation remains
  open.
- **Cailie Keating** — active 21:19–20:57 UTC across 19 sessions, chiefly messages/chat/users. One
  admin message-editor blur produced the new Svelte P3; she continued using the application.
- **Everyone else (anonymous aggregate)** — 38 additional signed-in users plus public visitors.
  Nine people created entries. Public/contributor activity centered on dictionary browse/search/audio;
  no non-admin individual is named.

## 3. Performance & geography

| Metric | p50 | p90 | p95 | n |
|---|---:|---:|---:|---:|
| Page load | 894 ms | 5.63 s | 9.13 s | 662 |
| Search | 11 ms | 51 ms | 89 ms | 918 |

Search is excellent. Page-load tail is slower than yesterday's 6.28-second p95, but today's mix
included eleven deploy versions and globally distributed traffic; one day is a watch, not a
regression. The raw ad-hoc query looked for `message='perf'` and therefore did not reproduce the
dashboard's context-driven `dict_boot` aggregate; current source's `build_dict_boot` remains the
authoritative reader and the health page already renders daily cold p50/p95 and sample counts.

All **832** human `session_start` capability rows reported `opfs-worker`; no below-capability
fallback population appeared. Top human-session regions were California (70), New York (58), Alaska
(48), Kuala Lumpur (40), Texas (29), Illinois (27), Arizona (24), Guangdong (22), and Mexico City
(18). The dashboard already provides Boston-distance TTFB and Core Web Vitals geography; no new
presentation is justified.

## 4. Health & housekeeping

- `logs.db`: **2.28 GB** (2,283,712,512 bytes), 1,327,325 hot rows; oldest
  **2026-07-10 18:25 UTC**, newest 21:03 UTC — the expected 14-day boundary.
- `logs-archive.db`: **728 MB**; `shared.db`: **158 MB**.
- Retention ran **2026-07-24 18:25 UTC**; rollups are finalized through July 23.
- The rolling 24 hours contained **96,768 production rows**. The ongoing crawler load is below the
  recent 120–172k/day band, disk remains low, and CPU cost is negligible.
- Finalized July 23: **619 human sessions, 108 real errors, 1,916 raw errors**. The post-classifier
  metric remains far more truthful than the old 1,000–2,000 “real error” baseline.

## Host resources (past 24h)

Plenty of headroom: **282/288 samples**. CPU averaged **3.7%**, with a **47.3%** hottest five-minute
window; load1 briefly reached 2.56 on two cores but was not sustained. RAM averaged **29.2%** and
peaked at **52.5%**; swap peaked at 5.7% and ended at **97 MB**. Disk is **18.8%**. No resource action.

## 5. Coverage gaps (Phase B)

1. ✅ **Yesterday's source-save gap shipped.** `EditSource.svelte` now emits
   `source_save_failed` with dictionary/operation/failure kind/slug and keeps the modal actionable.
2. ✅ **Admin sync halt and OPFS recovery outcomes remain sufficient.** Existing events carried the
   exact constraint/stage/build/session needed to distinguish the real admin wedge from crawler
   recovery breadth.
3. **Residual markdown-editor error needs a regression test, not another telemetry event.** The
   global error hook captured route, admin identity, breadcrumbs, build, and a useful Tiptap stack.
4. **No new broad event inventory item.** Current source covers entry writes, import/media upload
   failures, sync retries/halts, database boot/recovery, snapshot/retention jobs, email lifecycle,
   host resources, and structured API failures. The day's unanswered question is implementation
   behavior inside a captured stack, not missing observability.

## 6. Dashboard improvements (Phase C + D)

**Phase C — clean no-op.** The page already provides current/stale build health, bot share and error
breadth, sync/boot outcomes, pipeline liveness, host/storage health, route performance, Core Web
Vitals, geography, and thin-sample guards. The headline spot-check passed: finalized July 23
`real_errors=108` against 1,916 raw errors. No telemetry suggests a dashboard-load regression, but
current source still has only ephemeral `[profile]` console timing. The highest-value ready backlog
item remains **one `admin_analytics_computed` server event per uncached whole-window calculation**.

**Phase D — no new inbound port.** House's July 23 review also selected the same persisted
analytics-compute event and shipped no fresh portable panel. Tutor's latest work is specific to
learner/chat/native-memory and day-video funnels. Neither warrants an LD backlog duplicate. LD's
session-bounded OPFS viewer recovery remains relevant to House's shared worker harness; the
cross-app reviews already carry that lesson.

## Recommendations / action items

- [ ] 🟡 **Reproduce and fix the residual admin markdown-editor blur mutation** via
  `.issues/markdown-editor-residual-state-unsafe-mutation.md`; the July 2 microtask guard did not
  prevent one current-build occurrence, so verification must cover blur plus immediate teardown.
- [ ] **Confirm Dr. Greg Anderson's deployed admin mirror now completes sync** via
  `.issues/admin-sync-legacy-dictionary-role-wedge.md`; the fix is deployed and no halt recurred, but
  a positive recovered-sync signal closes the user impact more strongly than silence.
- [ ] **Keep editor OPFS recovery explicit and deferred** in
  `.issues/dict-boot-persistent-opfs-recovery.md`; current viewer replacement is bounded and today's
  exhaustion was crawler-only.
- [ ] **Low priority: persist uncached analytics compute cost** through the existing
  `admin_analytics_computed` backlog item so dashboard load performance becomes trendable.

## Trend context (7d / 30d)

The corrected classifier has now held for two finalized days: `real_errors` stayed near 100 while
raw errors remained around 1,500–1,900. Earlier values are not retrospectively recomputed and should
not be compared as if the definition were unchanged.

<svg viewBox="0 0 500 175" width="100%" style={{maxWidth:'540px'}} role="img" aria-label="Daily human sessions and real errors, July 18 through July 24">{[['07-18',419,1131],['07-19',372,1293],['07-20',713,1821],['07-21',648,2233],['07-22',490,72],['07-23',619,108],['07-24*',565,69]].map(([day,sessions,errors],i)=><g key={day}><rect x={28+i*66} y={140-Number(sessions)/713*90} width="22" height={Number(sessions)/713*90} rx="2" fill="#6366f1"><title>{sessions} human sessions</title></rect><rect x={50+i*66} y={140-Number(errors)/2233*90} width="22" height={Number(errors)/2233*90} rx="2" fill="#f59e0b"><title>{errors} real errors</title></rect><text x={50+i*66} y="154" textAnchor="middle" fontSize="8" fill="currentColor" fillOpacity="0.65">{day}</text></g>)}<line x1="18" y1="140" x2="490" y2="140" stroke="currentColor" strokeOpacity="0.25" /><text x="20" y="13" fontSize="9" fill="#6366f1">■ human sessions</text><text x="115" y="13" fontSize="9" fill="#f59e0b">■ real errors (definition corrected before July 22 finalization)</text></svg>

| Day | Human sessions | `real_errors` | Raw client errors |
|---|---:|---:|---:|
| Jul 18 | 419 | 1,131 | 1,136 |
| Jul 19 | 372 | 1,293 | 1,332 |
| Jul 20 | 713 | 1,821 | 1,827 |
| Jul 21 | 648 | 2,233 | 2,258 |
| Jul 22 | 490 | **72** | 1,550 |
| Jul 23 | 619 | **108** | 1,916 |
| Jul 24* | 565 | **69** | 955 |

*July 24 is partial at 21:03 UTC. Pre-July-22 `real_errors` values use the old classifier and are not
retrospectively recomputed.*
