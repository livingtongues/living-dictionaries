# Log review — 2026-07-12 (livingdictionaries.app · window: last 24h)

*Read-only pass over the `living` VPS `logs.db` + `shared.db` rollups. No code changed. Every
recommendation re-checked against current source. Current build: `1783860603346`.*

## TL;DR

- **Healthy. No P1, no P2.** A busy contributor day (177k rows — new daily record, still
  crawler-driven), with **one LD admin lightly active** (Jacob: admin review + a touch of editing;
  Greg only via his zombie tab; Diego & Anna absent).
- **Yesterday's waveform ▶ P3 is CLOSED — not by a guard, by a rewrite.** Commit `56a40b63`
  ("Replace wavesurfer with a canvas Waveform") removed the `wavesurfer.js` async-import entirely, so
  the `Cannot read properties of undefined (reading 'play')` race can't happen. **0 occurrences in
  24h** (was 7). No further action.
- **One new low-frequency 🟡 P3 to watch — a search-path stack overflow.** `RangeError: Maximum call
  stack size exceeded` (2 rows / 1 user / 1 session) on `/opata/entries?q=…"Flores"` from an
  iPhone (Chrome-iOS) in MX-Sinaloa. Minified `Sk@/Qk@` mutual recursion — a real contributor, not a
  bot. Can't pin a fix from the minified stack; **watch for recurrence / a repro**, don't guess. See §1.
- **Crawler still dominates and hasn't settled.** A US-SC datacenter crawler logged **28,298 of the
  ~29k sessions**; the raw `dictionary_opened`/`entry_opened` (28k/27k) are Googlebot-inflated. The
  dashboard strips these correctly; true signed-in users ≈ **17 (24h) / 91 (7d)**.
- **`sync_failed` zombie tabs unchanged** — Greg (1,874, null-session), an anon null-null tab (820),
  Marlene (166). All known-noise per the 2026-07-09 stale-tab ruling. No action.
- **Host + retention green.** CPU avg 2.8%, RAM max 59.5%, swap 4.1%, disk 16.2%. Retention ran 18:52
  UTC; oldest row 06-29 (13d, inside the 14-day hot window). `logs.db` = **1.47 GB** (1.17 → 1.47,
  still climbing crawl-driven — watch).

---

## 1. Errors & crashes

Clusters over 24h (grouped by message + stack head). Current build `1783860603346`. Note: `sync_failed`
is level `warn`, so it lands in the by-user zombie-tab tallies (§2), not the error-cluster table below.

| Sev | Cluster | n (24h) | users | sess | On current build? | Verdict |
|---|---|---:|---:|---:|---|---|
| ✅ **closed** | `Cannot read properties of undefined (reading 'play')` | **0** | 0 | 0 | — | Waveform rewritten to canvas (`56a40b63`) — race removed. Gone (was 7). |
| 🟡 **P3 (new)** | `RangeError: Maximum call stack size exceeded` | 2 | 1 | 1 | no (`…817500163`) | Search-path recursion on `/opata/entries?q="Flores"`, iPhone/Chrome-iOS, MX-Sinaloa. Real user. **Watch — see below.** |
| ⚪ noise | `Rejected` (serviceWorker.register) | 1,960 | 0 | 1,960 | mixed | 1 row/session, all anon/bot prerenderers — 3rd-party. |
| ⚪ noise | `live_query_failed` | 378 | 0 | 81 | some | Crawler leader-worker family — known-noise (07-09/07-10). |
| ⚪ noise | `initial dict sync failed` / `leader_boot_failed` / `Failed to read dict bundle` | 250 / 97 / 15 | ≤6 | mostly bot | some | Boot-cascade / crawler family — known-noise. |
| ⚪ noise | `Script error.` / `gsi/client` DOM error / `Uncaught Error: oa` | 84 / 6 / 1 | ≤6 | few | some | Cross-origin + Google One-Tap — opaque, 3rd-party. |
| 🟡 note | `Internal Error` (no stack/context) | 65 | 0 | 64 | some | Anonymous, 1/session, spread across builds. Dead-end — coverage gap §5 (carried). |
| ⚪ noise | dynamic-import / preload-CSS / module-script / FA-kit 404s | ~40 | 0 | few | mixed | Stale-build chunk 404s during blue/green swaps + a transient FA-kit fetch — expected. |
| ⚪ noise | `[orama-watcher] delta scan failed` | 3 | 3 | 3 | some | Crawler/boot family — known-noise. |
| ⚪ noise | `sync_halted_repeated_failure` / `401 Missing session` / `malformed JSON` | 1 each | 0 | ≤1 | current | Zombie-tab give-up + anon transient — single rows. |

### 🟡 P3 (new) — search-path stack overflow on `/opata/entries`

Two `RangeError: Maximum call stack size exceeded` rows, **1 user / 1 session**, on
`https://livingdictionaries.app/opata/entries?q=%7B%22query%22%3A%22Flores%22%7D` (a search for
"Flores" in the Opata dictionary), from an **iPhone iOS 26 / Chrome-iOS (CriOS 150)** in **MX-Sinaloa**
— a genuine contributor, not a bot. The stack is minified mutual recursion (`Sk@ → Qk@ → Sk@ → …`),
so the exact culprit isn't legible; it's on an **already-retired build** (`1783817500163`, current is
`1783860603346`). Low frequency, one device, one session.

- **Recommend (do NOT apply):** **watch, don't guess.** With a minified stack and n=2/1-session on a
  retired build, there's no confident fix to propose — an unsourcemapped `Sk/Qk` recursion could be
  search rendering, a MultiString locale loop, or a highlight pass. If it recurs (especially on the
  current build or from more than one user), drill the session + try to repro a "Flores" search on
  `/opata` locally with sourcemaps. → watch item, no code change.

### ✅ Closed — waveform ▶ play race (canvas rewrite)

`56a40b63` swapped `wavesurfer.js` for a static canvas `Waveform.svelte` that plays via a plain
`<audio>` element buffered on demand (verified in current source — no `wavesurfer` variable, no async
`import()` gating the play button). Yesterday's P3 action item is moot — the fix shipped as a rewrite,
not the proposed null-guard. 0 rows in 24h.

---

## 2. Usage & engagement

**Raw session counts are crawler-inflated** — a US-SC datacenter crawler = **28,298 of ~29k sessions**.
A crude non-US-SC `session_start` filter gives **~690** sessions, but that still includes secondary
anonymous datacenter traffic (SG, CN-HE, BR-CE clusters); the dashboard's UA-frequency filter would
land lower (yesterday's rigorous figure was ~313). **Signed-in is the clean signal:**

| Window | Signed-in sessions | Signed-in users |
|---|---:|---:|
| 24h | **98** | **17** |
| 7d | — | **91** |

**Analytics events (24h):** `search_performed` 633 (95 sess / 8 users), `audio_played` 343 (21 sess /
**1 user** — one contributor doing heavy audio review), `entry_created` 55 (17 sess / 10 users),
`entry_deleted` 3. `dictionary_opened` (28k) / `entry_opened` (27k) stay Googlebot-inflated; the
dashboard strips these via `is_bot_row` (verified in `log-analytics.ts`).

**Perf/nav is dominated by opening dictionaries + entries** — 10 signed-in users created entries and
8 searched, a normal editing spread.

### Who's doing what

**Admins (named — from the `ADMINS` allow-list):**

- **Jacob Bowdoin** (`jwrunner7`, L3) — **the one real admin session** (259 events / 6 sessions,
  09:21–20:32 UTC). Admin-console review: `/admin`, `/admin/dictionaries`, `/admin/users`,
  `/admin/analytics`, `/admin/buckets`, plus `/chat`; and light dictionary work across
  `san-sebastian-del-monte`, `river` (settings/entries/contributors), `pingelapese`, `matukar`,
  `opata` — 2 entries opened, **1 dict change pushed**. His boot hit a transient `initial dict sync
  failed` that **`sync_self_healed`** (the recovery path worked). No blocking errors.
- **Dr. Greg Anderson** (`livingtongues@gmail.com`, L2) — present **only as a null-session zombie
  tab** (1,874 `sync_failed` rows = essentially all his 1,872 events). No real editing session.
  Known-noise (decisions 07-09).
- **Diego Córdova** (L3) & **Anna Luisa Daigneault** (L2, notify-off) — **not active this window.**

**Everyone else (aggregate, anonymous — never named):** ~17 signed-in non-Jacob users active in 24h,
all normal contributor work — entry creation/edits, heavy audio review by one MX contributor, and
searching across many small dictionaries. Two loud stale tabs (an anonymous null-null tab at 820
`sync_failed`, and Marlene at 166) are the same known-noise family as Greg's. One anonymous MX-Sinaloa
contributor tripped the new search `RangeError` (§1) — a real user, not abuse. No non-admin hit a
blocking error.

---

## 3. Performance & geography

**Perf (24h; page_load/dict_boot are crawler-mixed here — the dashboard's `audience_filter` strips bots):**

- `page_load` **p50 30 ms / p90 110 ms / p95 110 ms** (n≈28k) — very fast SSR shell + hydrate.
- `search` **p50 14 ms / p90 39 ms / p95 55 ms** (n=633) — instant local Orama search.
- `dict_boot` raw **p50 3,500 ms / p90–95 4,990 ms** (n=3,734) — **crawler-dominated** (the tight 4,990ms
  cluster is bot boots); the dashboard's `build_dict_boot` strips these. Human warm boots read healthy
  as in prior windows.
- `web_vital` — 0 rows in the raw `perf` name bucket this window (CWV rides under its own path; not a gap).

**Geo (top areas by sessions, 24h):** US-SC **28,298** (the crawler) ·· then humans: US-WA 103 · US-VA 58 ·
SG 54 · US-PA 41 · US-CA 31 · US-AZ 25 · CN-HE 25 · BR-CE 25 · US-IA 21 · US-NC 20 · US-TX 19. The
SG/CN/BR clusters are likely secondary datacenter crawlers; the US spread is the genuine contributor base.

**Capability:** **99.6% `opfs-worker`** (28,147) + **110 `idb-worker`** (0.4% fallback), **zero
below-capability** Safari. Every real session ran the leader-worker DB tier.

---

## 4. Health & housekeeping

- **Volume:** 24h = 177,492 rows — **a new daily record** (prior peak 07-10 = 166,989). 7d: 07-06 33k →
  07-09 151k → 07-10 167k → 07-11 98k → **07-12 177k**. The SEO/bot crawl has **not** settled. Signal
  rows (create/edit/search/audio/perf/nav) stay clean underneath.
- **`logs.db` = 1.47 GB** (1.17 GB on 07-11 → 1.47 GB) — still climbing crawl-driven, +300 MB/day. Disk
  % unaffected (16.2%). VACUUM-after-prune already dispatched (decision 07-09); **watch** — the curve
  should flatten only once the crawl stops. `logs-archive.db` = 627 KB.
- **Retention:** `log_retention_ran_at` = **18:52 UTC today** (fresh). Oldest row 2026-06-29 (13d) —
  inside the 14-day hot window. ✅
- **Noise ratio:** dominated by the US-SC crawler boot-cascade + three zombie-tab `sync_failed` storms,
  all classified known-noise.

## Host resources (past 24h)

**Plenty of headroom — nothing to act on.** 285 host-stats samples (~288 expected; no telemetry gap).

- CPU avg **2.8%** · hottest 5-min window **52.1%** · `load1` max **2.15** (2-core box — a single
  momentary blip at core count, not sustained)
- RAM avg **33.7%** · max **59.5%** (a touch warmer than 07-11's 47.9%, still comfortable) · swap max
  **4.1%** (70 MB)
- Disk **16.2%** — `logs.db` at 1.47 GB is the only thing worth a periodic glance; disk % itself is fine.

---

## 5. Coverage gaps (Phase B)

1. **LOW (carried) — `Internal Error` (65 rows, anon) carries no structured context.** Unchanged since
   07-10/07-11. No `context.route` / `dictionary_id` / source id, so each firing is a dead-end. Route
   through `log_server_event` with structured fields when raised server-side. Low frequency; no urgency.
2. **LOW (new, contingent) — the search `RangeError` had no handled context.** If §1's stack overflow
   recurs, a `track({ event, props })` or `log_event` around the search-render path (with `query` +
   `dictionary_id`) would turn the next occurrence from a minified dead-end into something drillable.
   **Don't add speculatively** — only worth it if the error reappears on the current build.

No new *high-value* coverage gap this run.

---

## 6. Dashboard improvements (Phase C + D)

### Phase C — top proposal (re-grounded in TODAY's data; verified NOT present)

**★★ Bot/crawler share % on each error cluster.** Re-affirmed as the top open item — *again* I had to
hand-run UA-frequency queries to learn the "29k sessions" was 28,298 crawler, that `sync_failed`'s
volume was three zombie tabs, and that `dict_boot`'s 3.5s p50 was crawler-dominated.
`build_error_clusters` (`log-analytics.ts:1386`) still selects **no bot column** — confirmed in source:
its `ErrorCluster` interface (line 262) has `is_noise` (a static pattern match) but no `bot_pct`.
Add `bot_sessions` + `bot_pct` (UA `is_bot_user_agent` ∪ webdriver ∪ frequency-bot sessions) and badge
clusters `> 90%` as "mostly crawler". Already filed in `.issues/future/dashboard-improvements.md`.

**Also still open:** the **`max_per_session` "⟳ loop" marker** on `ErrorCluster` (per-session error
breadth → loop-bug flag) — confirmed unshipped (no `max_per_session` in `build_error_clusters`).
Cross-app-wanted; cheap; prioritize alongside the bot-share item.

### Phase D — cross-pollinate (house + tutor, 07-11 reviews)

- **No new inbound port for LD this run.** house's 07-11 Phase C (a **thin-data guard** dimming
  percentiles below ~15–20 samples) is **already shipped in LD** — verified `THIN_SAMPLE_N = 15` in
  `$lib/analytics/dashboard-format.ts` + `VitalBar.svelte` + `HealthView.svelte`. tutor's 07-11 shipped
  the **At-a-Glance strip**, which LD pioneered. Both siblings are catching up to LD, not ahead of it.
- **Symmetry — LD wins the siblings are borrowing (flag for their next runs):** the **thin-data perf
  guard** (`THIN_SAMPLE_N=15`, tutor is copying it verbatim) and the **At-a-Glance plain-language
  story strip** (both siblings ported it). LD remains the furthest-along dashboard.
- **Skipped (inapplicable):** tutor's mobile device / OTA-adoption + memory-OOM panels (LD is web-only)
  and house's `/admin/revenue` funnel (LD has no payments) — per the standing skip list.

---

## Recommendations / action items

- [ ] 🟡 **P3 (new) — watch the search `RangeError`.** `Maximum call stack size exceeded` on
      `/opata/entries?q="Flores"` (iPhone/Chrome-iOS, MX-Sinaloa; 2 rows / 1 user / 1 session, retired
      build). Minified `Sk@/Qk@` recursion — no confident fix. Watch for recurrence on the current
      build or a second user; if it recurs, drill + repro a "Flores" search on `/opata` with sourcemaps.
- [x] ✅ **P3 (closed) — waveform ▶ play race:** 0 occurrences; fixed by the canvas rewrite (`56a40b63`),
      not the proposed guard.
- [ ] ⚪ **Watch — `logs.db` at 1.47 GB (+300 MB/day), crawl still not settled** (07-12 = 177k rows,
      new record). No action; confirm growth flattens once the crawl finishes (VACUUM-after-prune already
      dispatched). Disk fine at 16.2%.
- [ ] ★★ **Phase C (backlog) — bot-share % on error clusters** (top open item, re-grounded today) +
      **`max_per_session` "⟳ loop" marker** (cross-app-wanted, still unshipped). Filed in
      `.issues/future/dashboard-improvements.md`.

## Trend context (7d)

Daily `client_logs` volume (all rows, crawler-inclusive):

<svg viewBox="0 0 560 140" width="100%" style={{maxWidth:'580px'}} role="img" aria-label="daily log volume 7d">
{[['07-06',33479],['07-07',64616],['07-08',30142],['07-09',151198],['07-10',166989],['07-11',98158],['07-12',177492]].map(([d,n],i)=>{const x=24+i*76;const h=Math.min(n/177492*100,100);return <g key={d}><rect x={x} y={115-h} width="50" height={h} rx="2" fill="#8b5cf6"/><text x={x+25} y="128" textAnchor="middle" fontSize="9" fill="currentColor">{d}</text><text x={x+25} y={111-h} textAnchor="middle" fontSize="8" fill="currentColor" fillOpacity="0.7">{(n/1000).toFixed(0)}k</text></g>)}
<text x="24" y="10" fontSize="9" fill="currentColor" fillOpacity="0.55">07-12 = new record; SEO/bot crawl still not settled</text></svg>

Signed-in users 24h / 7d: **17 / 91**; signed-in sessions 24h: **98**. Capability 99.6% `opfs-worker`
(+0.4% idb fallback, zero below-capability). No P1/P2; waveform ▶ P3 closed by rewrite; one new
low-freq search `RangeError` on watch. A busy, contributor-driven day with one light admin session (Jacob).
