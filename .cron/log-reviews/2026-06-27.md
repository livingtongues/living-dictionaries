# Log review — 2026-06-27 (new.livingdictionaries.app · window: last 24h)

> **Third run.** 06-25 = zero-data baseline; 06-26 = first real (≈91% synthetic) data, 545 rows.
> Today the new app was **essentially idle**: the 24h window holds **8 rows / 1 session / 0 errors**
> — a single post-deploy HeadlessChrome smoke-test on `/about`. The triage-able dataset is still
> yesterday's 06-26 corpus. So this run is mostly: (a) confirm yesterday's P1 code fixes landed,
> (b) **spot-check the dashboard against raw queries** — which surfaced one real drift, and
> (c) Phase C/D grooming.

## TL;DR
- **🟢 Health is clean.** 553 rows total (+8 since yesterday), retention ran today (19:06Z), rollup
  current through 06-27, DB ~930 KB. **0 errors in 24h**, **0 new error clusters in 7d** (the same 4
  benign 06-26 clusters). Nothing is broken.
- **🟦 No new traffic.** Last 24h = one HeadlessChrome session on `/about` (the smoke-test). Still
  **zero genuine human traffic** pre-cutover; every "usage" number remains the 06-26 e2e harness
  (100% MY/Malaysia geo, 100% `opfs-worker`). Usage triage stays parked until humans arrive.
- **✅ Yesterday's three P1/P2 code fixes all shipped:** `+error.svelte` now maps HTTP status→level
  (401/403→warn, 404→info, 5xx→crash); `audio_played` is now wired to the real play handler
  (`Audio.svelte:56`); and the dashboard gained the tutor-borrowed **error-cluster + known-noise**
  panel, a **Humans/Bots toggle**, a **Core Web Vitals** panel, and a redesigned Browsers/devices panel.
- **🟡 Spot-check drift (the headline finding):** yesterday's report claimed **0 web-vital rows** and
  hypothesized "HeadlessChrome never flushes vitals." **Both are wrong** — there are **163 `web_vital`
  rows** (LCP/INP/CLS/FCP/TTFB), almost all from 06-26, and they flush fine on HeadlessChrome. This
  was a **review-query error, not a dashboard or pipeline bug** (the `web_vitals` reader is correct).
  Web-vitals telemetry is healthy and the CWV panel has real data.
- **Top opportunity (Phase C):** annotate the daily sessions/errors timeline with **deploy/version
  markers** — 6 `app_version`s shipped in a single day during the wiring sprint; a marker line would
  let a future error spike be pinned to the deploy that caused it.

## 1. Errors & crashes
**24h: 0 error/crash rows.** 7-day clusters are unchanged from yesterday — all 06-26, all synthetic,
none recurring into today:

| Sev | Cluster | n (7d) | users | Status | Note |
|---|---|---|---|---|---|
| ⚪ noise | `[post_request] Network error for /api/log:` | 5 | 0 | Now in `KNOWN_NOISE_PATTERNS` ✅ | Self-referential flush blip from the long-idle admin tab; excluded from the real-error count by the shipped cluster panel. |
| 🟡 P3 | `Internal Error` (500) on `/e2e-log-*/entries` | 4 | 1 | Cause still unrecoverable | Synthetic dict-load failures from 06-26; the real stack went to (now-rotated) `docker logs`. See §5 for the residual SSR-first-paint gap. |
| 🟡 (resolved-as-warn) | `Missing session` (401) on `/admin/analytics` | 1 | 0 | Fixed at source ✅ | The status→level map now records this as `warn`, not `crash`. Won't recur as a "crash". |
| 🟡 P3 | `Failed to initialize WebGL` (Mapbox, `/`) | 1 | 0 | Open (low) | One anon browser with WebGL off; graceful no-WebGL globe fallback still a nice-to-have. |

**No P1, no multi-user clusters, no `unhandled_rejection`.** Real-world error behavior remains
untested until human traffic lands.

## 2. Usage & engagement
**⚠️ Still synthetic.** 24h = 1 HeadlessChrome session (`/about`, 8 rows). The corpus below is the
06-26 e2e harness, carried for continuity.

| Metric | 24h | 7d | 30d | Note |
|---|---|---|---|---|
| Sessions (all) | 1 | 45 | 45 | +1 = today's smoke-test |
| Distinct `user_id` | 0 | 1 | 1 | almost all rows anon |
| Errors | 0 | 11 | 11 | all 06-26 |

- **Analytics events (all-time, all wired & firing):** `dictionary_opened` 21, `auth_login` 14,
  `dictionary_created` 10, `search_performed` 4, `entry_opened` 2, `dict_changes_pushed` 1.
  **`audio_played` = 0 emitted but now correctly wired** (`Audio.svelte:56`) — absence is just because
  no synthetic flow clicks audio. Resolved as a coverage gap.
- **Platform/geo/capability:** 100% `web`, 100% MY (Malaysia — the agent running e2e),
  100% `opfs-worker`, 0 below-capability sessions.

## 3. Performance & geography
- **Web vitals (corrected):** **163 rows** — TTFB 43, FCP 42, CLS 37, LCP 37, INP 4. Sample ratings
  are sane (LCP 1628ms `good`, CLS 0.003 `good`, TTFB 1049ms `needs-improvement`). They **do** flush
  on HeadlessChrome. The CWV panel shipped 06-27 has real data.
- **`page_load` (45 samples):** p50 ≈ 1222ms, p90 ≈ 1768ms, p95 ≈ 3229ms — but **two 0ms entries**
  (not real loads — bfcache/instant nav recorded as a load, dragging p50 down) and **one 17 876ms
  outlier** (likely a cold OPFS-worker boot) hidden behind p95. → Phase C data-quality item.
- **`search`:** p50 ≈ 13ms, max 33ms (local Orama — fast).
- **Geo TTFB:** single-region (MY ≈ 15 000 km from Boston) → ~1s TTFB is the distance tax in action,
  but with one origin region the by-distance RUM split still can't differentiate. Revisit post-cutover.

## 4. Health & housekeeping
- **Volume:** 553 rows total; 06-26 = 545, 06-27 = 8. DB **227 pages × 4096 = ~930 KB**.
- **Composition (all-time):** production 528 / NULL-build (server rows) 25.
- **Retention:** ✅ `log_retention_ran_at = 2026-06-27T19:06:50Z` (today). Oldest row 06-26 → ≤14d
  invariant trivially met. Archive lives in a separate `logs-archive.db` (nothing old enough yet).
- **Rollup:** ✅ `log_daily_metrics` current through **06-27** (06-26: 40 metrics/1846; 06-27: 11/27).

## 5. Coverage gaps (Phase B)
Yesterday's top gaps are **closed**. Remaining (no new data to surface fresh ones):

| # | Path | Event / name | Level | Fields | Why |
|---|---|---|---|---|---|
| **P3 (residual)** | `routes/[dictionaryId]/+layout.ts` catch (~L182) | `log_server_event` (in addition to the kept `console.error`) | error | `{ dict_id, phase:'dict_layout_load', error, stack }` | The catch now keeps `console.error` (patched client-side ✅ for client-side navigations), but on a **first SSR paint** failure console.error is unpatched → cause still lands only in ephemeral `docker logs`. Adding a `log_server_event` would close the first-paint case. Lower priority now that the common (client-nav) case is covered. |
| **P3** | `routes/api/*/+server.ts` failure paths | `log_server_event` | warn/error | `{ route, status, error }` | Success paths log; **failure** paths (auth send-code/verify, sync `/changes`, uploads, email-inbound) still mostly silent server-side. |
| **P3** | homepage Mapbox globe | no-WebGL fallback + filter Mapbox-internal WebGL errors as noise | n/a | n/a | One anon WebGL-disabled session got a hard globe failure. |

## 6. Dashboard improvements (Phase C + D)

**Phase C — spot-check result:** the only drift I found (web-vitals 0 vs 163) was a **prior-review
query error, not a dashboard bug** — the `web_vitals` reader (`WEB_VITAL_METRICS`, p50/p75/p95) is
correct and the CWV panel has real data. No headline-number bug to file. New concrete proposals:

1. **(primary) Deploy/version annotation markers on the daily timeline.** Today's corpus had **6
   `app_version`s in one day** (the wiring sprint). When a real error spike eventually appears, the
   first question is "which deploy?" Add vertical markers (first-seen timestamp per `app_version`) to
   the sessions/errors LineChart so spikes pin to a build. `$lib/charts/` LineChart already supports
   event markers (house uses them on the revenue MRR line). → backlog.
2. **(secondary) `page_load` data-quality: drop/bucket 0ms loads + surface the slowest load.** Today's
   spot-check found **two 0ms `page_load`s** (bfcache/instant-nav mis-recorded as loads, dragging p50)
   and a **17 876ms** outlier hidden behind p95. Filter `duration_ms <= 0` out of the percentiles and
   add a "slowest page load (with route)" stat so cold-boot pathologies are visible, not averaged
   away. → backlog.

**Phase D — sibling cross-pollination.**
- ✅ **tutor's error-cluster + known-noise** — the top borrow from last run is **now shipped on LD**
  (`KNOWN_NOISE_PATTERNS` + `is_noise` on `error_clusters`). Removing from the open borrow list.
- **house's "expected-response (401/403/404) bucket" in `error_audience`** *(filed 2026-06-27)* —
  **largely already covered on LD at the source**: `+error.svelte` demotes 401/403→warn and 404→info,
  so expected gates never reach the `error/crash` levels that feed `error_clusters`. Worth tracking
  only **if** LD later adds an `error_audience` real-user panel; logging as low-priority, not a gap.
- **house's `error_audience` real-user verdict card** — already in LD's backlog; mostly subsumed by
  LD's clusters + `is_noise` + human-only usage. Stays low.
- *LD wins the siblings should borrow back (flag in their reviews):* **bot/headless exclusion across
  ALL usage+geo metrics** (house de-bots only errors; tutor not at all — still the highest-value open
  cross-app item), the **Core Web Vitals** panel, and the **pipeline-liveness + event-coverage**
  strips. LD is the furthest-along dashboard.

→ Phase C items appended to `.issues/future/dashboard-improvements.md`; Phase D borrow-status updated there.

## Recommendations / action items
- [ ] **(Phase C, primary) Deploy/version markers on the daily sessions/errors timeline** — attribute
      future spikes to a build. Backlog.
- [ ] **(Phase C, secondary) `page_load` percentile hygiene** — drop `duration_ms <= 0`, surface the
      slowest load + route. Sourced from today's 0ms + 17.9s outliers. Backlog.
- [ ] **(Phase B, P3 residual) Add `log_server_event` to the `[dictionaryId]/+layout.ts` catch** so a
      first-SSR-paint dict-load failure isn't lost to rotated `docker logs` (client-nav case already
      covered by the kept patched `console.error`).
- [ ] **(Phase B, P3) Wire `log_server_event` into `routes/api/*` failure paths** (auth, sync,
      uploads, email-inbound) — failures are still server-side-silent.
- [ ] **(P3) Graceful no-WebGL globe fallback** + filter Mapbox-internal WebGL errors as noise.
- [ ] **Re-run after the production cutover / first genuine human traffic** — usage/perf/geo/web-vital
      panels only measure reality then; that's also when bot-exclusion stops being cosmetic.
- [ ] **Process: this command's reviewer should spot-check web-vitals against a raw query** — the
      06-26 "0 vitals" claim was a query miss, not real. (Reflects the command's own "spot-check a
      headline number vs your raw query — drift means a bug to file" rule; here the drift was in the
      *review*, so file the lesson, not a code bug.)

## Trend context (7d / 30d)
Sparkline (logs · errors · sessions per day):

```
2026-06-25  ▁    0 logs   0 err   0 sess   (baseline — pipeline alive, no traffic)
2026-06-26  █  545 logs  11 err  44 sess   (first real data; ≈91% synthetic/headless)
2026-06-27  ▁    8 logs   0 err   1 sess   (idle — one post-deploy smoke-test)
```

Still no multi-day human trend — two synthetic days bracketing an idle day. The first meaningful
trend arrives with post-cutover human traffic; until then the value of each run is (a) health
confirmation, (b) dashboard spot-checks, and (c) Phase C/D grooming — all done this run.
