# Log review — 2026-07-25 (livingdictionaries.app · window: last 24h)

*Read-only production review of `logs.db` + `shared.db`, ending **21:02 UTC**. No application code
or production data changed. Current build: **`1784974358735`**. Findings were checked against
current source, same-day commits, standing decisions, open issues, both admin dashboards, and the
July 24 house/tutor reviews.*

## TL;DR

- 🟢 **No new signed-in product failure.** The 103-row top cluster was the already-classified masked
  cross-origin `Script error.` family; one ChromeOS editing session generated 98 rows but continued
  saving and navigating for almost four hours.
- ✅ **Yesterday's admin editor teardown fix is deployed and quiet:** zero
  `state_unsafe_mutation` rows after the shared teardown-guarded editor ticker shipped.
- 🟢 **Healthy editing day:** 651 human sessions, 33 signed-in users, 165 entries created, 1,188
  searches, 345 audio plays, and 587 dictionary opens. All capability-bearing human sessions used
  the OPFS leader-worker tier.
- 🟢 **Speed improved:** page-load p95 fell from 9.13 seconds to 4.87 seconds; search p95 was 61 ms.
- 🟢 **Host and retention health are comfortable:** CPU 3.0% average / 25.2% hottest five minutes,
  RAM 28.8% / 49.3% max, 53 MB swap, disk 19.7%, and retention ran at 16:14 UTC.
- ✅ **The dashboard-compute backlog item shipped and is live.** Six
  `admin_analytics_computed` events prove uncached calculations are now trendable; no new dashboard
  proposal or sibling port is justified tonight.

## 1. Errors & crashes

| Severity | Cluster | Reach in 24h | Verdict |
|---|---:|---:|---|
| ⚪ known noise | `Script error.` | 103 rows / 3 sessions / 2 signed-in users | Current `classify-error.ts` explicitly folds this stackless, filename-less cross-origin browser signal out of real errors. One ChromeOS session produced 98 rows while continuing successful editing; no actionable application stack exists. |
| ⚪ crawler noise | `Internal Error` | 31 rows / 31 sessions / 0 users | 29/31 rows matched bot automation. Same standing crawler boot/deep-link family; no human breadth. |
| ⚪ startup retry | `initial dict sync failed` | 23 rows / 23 sessions | Mixed crawler/anonymous startup churn across nine builds. No signed-in terminal halt or dirty-row warning. |
| ⚪ anonymous recovery | `dict_boot_recovery_exhausted` | 14 rows / 6 sessions / 0 users | Entirely anonymous. Viewer replacement produced 204 one-per-session rows and one recovery; no signed-in editor exhaustion. Keep the existing editor-safe recovery design deferred. |
| 🟡 transient network/media | `Failed to fetch`, aborted operations, unsupported media source | low single digits per cluster | Thin across sessions and builds; users continued. No server-wide burst or core-flow halt. |
| ⚪ deploy churn | stale CSS/module fetches and one minified `.map is not a function` sequence | low frequency | The minified error occurred on an intermediate build alongside dynamic-import failures; current build has no recurrence. Cross-browser stale bundles are already classified. |
| ✅ fixed | `state_unsafe_mutation` in the admin reply editor | 0 | Commit `5e5a74ca` uses the shared deferred, teardown-guarded editor ticker in both rich-text and Markdown editors. The production watch is clean. |

### Leader-worker and recovery drill

There were 90 `live_query_timeout` rows across 28 sessions, but they were overwhelmingly anonymous
queries waiting for initial leader election. The only signed-in timeout recovered in **3.2 seconds**.
Six `live_query_failed` rows belonged to one anonymous current-build session; there was no signed-in
terminal failure. Existing telemetry already records table, source, leader presence, retry state,
SQLite code, build, and recovery, so no new event is needed.

## 2. Usage & engagement

| Window | Human sessions | Signed-in users | Median session span | p90 span |
|---|---:|---:|---:|---:|
| 24 hours | 651 | 33 | 24 s | 56 min |
| 7 days | 4,900 | 92 | — | — |
| 30 days | 15,036 retained/raw sessions | 139 | — | — |

The 30-day raw number is not a true monthly-unique count; the dashboard's monthly visitor
materialization is authoritative for that question.

Human activity included 1,665 navigations, 1,188 searches, 587 dictionary opens, 385 entry opens,
345 audio plays, **165 entries created**, and 16 entries deleted. The most-used destination was
`/jejueo/entries` (108 navigations), followed by `/` (94), `/tutelo-saponi/entries` (51), and
`/nahuatl/entries` (46).

### Who's doing what

- **Jacob Bowdoin** — active 22:53–17:02 UTC across 50 sessions: Enxet/Eastern Pomo/Ponca/Iipay Aa
  import conversations, sources, suggestions, API docs, admin messages/imports/users/dictionaries,
  chat, and analytics. Network blips were transient; no editing or admin-database halt.
- **Dr. Greg Anderson** — active 21:39–19:59 UTC across 12 sessions in messages, chat, admin
  dictionaries, Ponca, and the home/admin surfaces. No recurrence of the repaired duplicate-role
  sync halt.
- **Cailie Keating** — active 21:06–20:51 UTC across 7 sessions in messages, chat, admin, and
  contributor review. The prior reply-editor teardown error did not recur.
- **Diego Córdova** — active 21:19–23:38 UTC across 7 sessions in chat and Enxet/Iipay Aa message
  threads. No notable error.
- **Everyone else (anonymous aggregate)** — 29 additional signed-in users plus public visitors.
  Editing concentrated in Jejueo, Milang, Boienen, Sugstun, Poggerese, and other dictionaries; no
  non-admin individual is named.

## 3. Performance & geography

| Metric | p50 | p90 | p95 | n |
|---|---:|---:|---:|---:|
| Page load | 805 ms | 3.22 s | 4.87 s | 556 |
| Search | 12 ms | 43 ms | 61 ms | 1,187 |

Page-load p95 improved from July 24's 9.13 seconds; search remains excellent. The geographic mix
remained globally distributed, led by California, New York, Alaska, Kuala Lumpur, Texas, Guangdong,
Arizona, and Mexico City. The dashboard already presents Boston-distance TTFB and Core Web Vitals
splits, so duplicating that hand analysis here would add no decision value.

Every human `session_start` row with a database capability reported **`opfs-worker`**. There was no
below-capability fallback population.

## 4. Health & housekeeping

- `logs.db`: **2.28 GB**, 1,320,173 hot rows; oldest **2026-07-11 16:14 UTC**, newest 21:02 UTC —
  the expected 14-day boundary.
- `logs-archive.db`: **841 MB**; `shared.db`: **162 MB**.
- Retention ran **2026-07-25 16:14 UTC**; rollups are finalized through July 24.
- The rolling 24 hours held about **80,000 production rows**. The standing Googlebot crawl remains
  below its 120–172k/day high band and consumes little host capacity.
- Finalized July 24: **700 human sessions and 79 `real_errors`**. July 25 was at 51 partial
  `real_errors` at review time. The post-July-22 classifier remains stable and honest.

## Host resources (past 24h)

Plenty of headroom: **283/288 samples**. CPU averaged **3.0%**, with a **25.2%** hottest five-minute
window; load1 briefly reached 2.48 on two cores but was not sustained. RAM averaged **28.8%** and
peaked at **49.3%**; swap peaked at 5.2% and ended at **53 MB**. Disk is **19.7%**. No resource action.

## 5. Coverage gaps (Phase B)

1. ✅ **The observed incidents are already attributable.** Live-query telemetry separates timeout,
   recovery, terminal failure, table, source, leader presence, code, build, and user/session. The
   editor error had a complete deployed stack and route trail. OPFS events separate replacement,
   recovered, and exhausted outcomes.
2. ✅ **Recent import/media coverage is present.** Current source emits structured
   `import_upload_failed` and `media_upload_failed`; entry writes, sync, snapshots, retention, email
   lifecycle, host resources, and API-v1 operations also have durable events.
3. **No new broad inventory item.** A scan of all 159 API endpoints and server/background-job logging
   found remaining `console.error` calls, but today's production data does not show an unanswered
   failure in those paths. Converting every caught exception without evidence would create volume,
   not insight.

## 6. Dashboard improvements (Phase C + D)

**Phase C — shipped improvement, no new panel.** Commit `10aacd6c` completed the prior highest-value
item: `get_log_analytics` calls an endpoint hook only after an uncached whole-window compute, and the
endpoint emits one bounded `admin_analytics_computed` server event with duration, scope, days, and
audience. Six production events appeared in this window. The same change serves cached analytics
stale-while-revalidate and recomputes pipeline liveness fresh, protecting perceived load. The backlog
was corrected from open to shipped. Current source already covers current/stale builds, error
breadth/loops/bot share, sync and boot outcomes, pipeline liveness, host/storage, route performance,
Core Web Vitals, geography, and thin samples.

**Phase D — no new inbound port.** House shipped the same compute event plus stale-while-revalidate
after measuring 7.8–32.5-second background calculations; LD now has that pattern too. Tutor's current
work is native over-the-air update and day-video observability, which does not fit LD. Neither sibling
adds a generic dashboard primitive LD lacks. The outbound lesson is convergence: all three apps now
persist bounded analytics-compute cost and can make future cache decisions from evidence.

## Recommendations / action items

- [x] **Close the admin reply-editor teardown watch:** the shared deferred ticker shipped in
  `5e5a74ca`, its focused unit/browser verification is recorded in
  `.issues/markdown-editor-residual-state-unsafe-mutation.md`, and production has zero recurrence.
- [x] **Close the analytics-compute observability backlog item:** `10aacd6c` is deployed and six
  `admin_analytics_computed` events confirm the uncached-only signal works.
- [ ] **Keep safe editor OPFS recovery explicit but deferred** in
  `.issues/dict-boot-persistent-opfs-recovery.md`; today's exhausted recoveries were anonymous, while
  automatic replacement for editors still requires a durable external dirty-write ledger.
- [ ] **Continue the ordinary current-build watch for leader-worker terminal failures.** Act only if
  `live_query_failed` reaches a signed-in user or gains breadth; today's six rows were one anonymous
  session and the only signed-in timeout recovered in 3.2 seconds.

## Trend context (7d / 30d)

The corrected real-error definition has now held for four finalized days while human traffic
remained healthy.

<svg viewBox="0 0 500 175" width="100%" style={{maxWidth:'540px'}} role="img" aria-label="Daily human sessions and real errors, July 19 through July 25">{[['07-19',372,1293],['07-20',713,1821],['07-21',648,2233],['07-22',490,72],['07-23',619,108],['07-24',700,79],['07-25*',374,51]].map(([day,sessions,errors],i)=><g key={day}><rect x={28+i*66} y={140-Number(sessions)/713*90} width="22" height={Number(sessions)/713*90} rx="2" fill="#6366f1"><title>{sessions} human sessions</title></rect><rect x={50+i*66} y={140-Number(errors)/2233*90} width="22" height={Number(errors)/2233*90} rx="2" fill="#f59e0b"><title>{errors} real errors</title></rect><text x={50+i*66} y="154" textAnchor="middle" fontSize="8" fill="currentColor" fillOpacity="0.65">{day}</text></g>)}<line x1="18" y1="140" x2="490" y2="140" stroke="currentColor" strokeOpacity="0.25" /><text x="20" y="13" fontSize="9" fill="#6366f1">■ human sessions</text><text x="115" y="13" fontSize="9" fill="#f59e0b">■ real errors (definition corrected before July 22 finalization)</text></svg>

| Day | Human sessions | `real_errors` |
|---|---:|---:|
| Jul 19 | 372 | 1,293 |
| Jul 20 | 713 | 1,821 |
| Jul 21 | 648 | 2,233 |
| Jul 22 | 490 | **72** |
| Jul 23 | 619 | **108** |
| Jul 24 | 700 | **79** |
| Jul 25* | 374 | **51** |

*July 25 is partial at 21:02 UTC. Pre-July-22 values use the old classifier and are not
retrospectively recomputed.*
