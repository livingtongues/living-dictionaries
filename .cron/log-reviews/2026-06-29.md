# Log review — 2026-06-29 (new.livingdictionaries.app · window: last 24h)

> **Fifth run — and the first with real human traffic.** The prior four were zero-data / synthetic /
> idle. Today the VPS ingested **1,674 rows / 8 sessions** from **two signed-in users** (both in
> Kuala Lumpur, MY) across a heavy **7-deploy** dev day — and the pipeline immediately caught a **real
> P1 server crash** that all four synthetic runs never could. This is the proof-of-value run.

## TL;DR
- **🔴 P1 BUG CAUGHT — production `shared.db` is missing the `dictionary_partners` table.** A
  `crash`/`server` row at 09:36:45Z: `no such table: dictionary_partners` from
  `/api/admin-sync` → `fetch_changes`. The live DB genuinely lacks the table even though
  `20260525_initial.sql` (which `CREATE`s it) is marked applied — because the repo's initial migration
  was **consolidated** after the prod DB was provisioned (the live `migrations` table still lists a
  since-deleted `20260526_messages.sql`), and migrations run once-per-name so the added `CREATE` never
  ran. Since the table is in `SYNCABLE_TABLE_NAMES`, admin-sync pulls 500. **Filed
  `.issues/missing-dictionary-partners-table-prod.md`** with the fix (a new idempotent backfill
  migration + a schema-drift audit). **This is the single most important action.**
- **🟢 Pipeline + retention healthy.** Retention cron ran today (`log_retention_ran_at =
  2026-06-29T17:31:21Z`); newest row is live (22:30Z). DB grew to ~2.79 MB / 2,227 rows total.
- **👤 Two users, both KL/MY:** **Jacob Bowdoin** (admin L2, `jwrunner7`) — 7 sessions across the
  7-deploy day, hit the crash; plus **one non-admin signed-in account** (not in the allow-list) that
  left a tab open **11 h** (668 min, ~1,200 heartbeats) on `/create-dictionary`.
- **🟡 First real RUM = the distance tax is real.** TTFB avg **1,214 ms**, LCP **2,900 ms**, FCP
  **2,757 ms** — mediocre, exactly as predicted for ~15,000 km Boston→KL. Not a regression; geography.
- **0 real error clusters** beyond the P1 crash + the known `/api/log` flush-blip noise (6×, 1 session).

## 1. Errors & crashes
**24h: 7 error/crash rows → 2 clusters.**

| Sev | Cluster | n | sessions | users | Status |
|---|---|---|---|---|---|
| 🔴 **P1** | `no such table: dictionary_partners` (`crash`/server, `/api/admin-sync` → `fetch_changes`) | 1 | 1 | Jacob | **OPEN — filed `.issues/missing-dictionary-partners-table-prod.md`** |
| ⚪ noise | `[post_request] Network error for /api/log:` | 6 | 1 | 1 | In `KNOWN_NOISE_PATTERNS` ✅ (self-referential flush blip, 05:16–05:29 during a deploy) |

**The P1 in one line:** the live `shared.db` never got `dictionary_partners` (consolidation drift — the
initial migration was rewritten after it was already applied), so every admin-sync pull reaching that
table 500s. Root-cause + fix in the issue file. **Recommend (do NOT apply here):** ship a new
idempotent `CREATE TABLE IF NOT EXISTS dictionary_partners` backfill migration, audit `sqlite_master`
for *other* objects the consolidation dropped (the messages tables + the `db_metadata` delete-trigger
arm), and make `fetch_changes` skip-and-log a missing syncable table instead of 500-ing the whole sync.

No P1 client crashes, no `unhandled_rejection`, no multi-user cluster.

## 2. Usage & engagement
First real (non-synthetic) traffic. 1,674 rows / 8 sessions / 2 users, **100% Kuala Lumpur, MY**,
100% `web`, 100% `opfs-worker`, 0 below-capability, 0 webdriver-flagged (correctly — both are real
humans, not automation; M1's `navigator.webdriver` fold shipped today and stays dormant until real
bot/e2e traffic). Heartbeats dominate (1,556 of 1,674 = 93%); 47 perf, 13 navigation, 10 session_start.

**Who's doing what:**
- **Jacob Bowdoin** (`jwrunner7@gmail.com`, admin L2) — **7 sessions, 05:12–11:21Z (~6 h of dev)**, the
  engine behind today's **7 deploys** (`app_version` rolled 7× from 05:12→11:24). Routes touched: `/`,
  `/admin`, `/admin/team`, `/create-dictionary`. **Notable:** hit the `dictionary_partners` admin-sync
  500 at 09:36 — i.e. *he* surfaced the P1 just by working. Geo = KL (he's in Malaysia → owns the high
  TTFB below). Sessions were short/iterative (21–65 min), consistent with build-deploy-test loops.
- **One non-admin signed-in user** (real `user_id`, **not** in `ADMINS` — kept anonymous per policy;
  almost certainly a paired test account given the same-KL geo and `/create-dictionary` focus) — a
  single **668-min (11 h)** session, 11:24→22:32Z, **1,221 events that are ~all heartbeats** = an idle
  tab left open, not 11 h of active use. See §3/§5 — this inflates heartbeat-span "session duration".

**Analytics events (24h):** none of the named vocab (`dictionary_opened`/`search_performed`/
`entry_opened`/`audio_played`/`dictionary_created`) fired today — today's two users lived on `/`,
`/admin*` and `/create-dictionary` (admin/dev work), not the entry/search/audio reader flows. Expected
for a dev day; the reader-event vocab stays unexercised until real contributor/visitor traffic.

## 3. Performance & geography
**First real RUM — small sample (8–10), single region.**

| Metric | 24h avg | Note |
|---|---|---|
| `page_load` | **2,144 ms** (max 3,999) | OPFS-worker boot + KL distance |
| TTFB (web vital) | **1,214 ms** | the Boston→KL ~15,000 km distance tax — "poor" by CWV but geographic, not a regression |
| LCP | **2,900 ms** | "needs-improvement" (>2.5 s); dominated by the 1.2 s TTFB |
| FCP | **2,757 ms** | tracks LCP |
| INP | 42 ms | "good" |
| CLS | ~0 | "good" |

- **Geo:** 100% Kuala Lumpur — can't drive the by-distance RUM split (one region), but it **confirms
  the model**: a ~15,000 km user pays ~1.2 s TTFB. The geo-TTFB panel will become meaningful once
  traffic spans regions post-cutover.
- **Capability:** 10/10 `session_start` = `opfs-worker`; no degraded tiers.

## 4. Health & housekeeping
- **Volume:** 2,227 rows total (06-26 = 545, 06-27 = 8, **06-29 = 1,674**). DB **2.79 MB** (was ~908 KB
  — the 1,674-row real day ~tripled it; heartbeats are 93% of that, worth watching post-cutover).
- **Composition:** 1,671 production-client (1,665 info / 6 error) + 3 server (2 info, 1 crash).
- **Retention:** ✅ `log_retention_ran_at = 2026-06-29T17:31:21Z` (today). Oldest row 06-26 → ≤14d
  invariant trivially met.
- **Heartbeat ratio:** 93% — high, skewed by the single 11 h idle tab (~1,200 of them). Not harmful
  yet, but see §5 — heartbeats fire on an idle/hidden tab and both inflate volume and distort the
  heartbeat-span duration metric.

## 5. Coverage gaps (Phase B)
| # | Path | Event / field | Level | Why |
|---|---|---|---|---|
| **P1 (fix, not log)** | `sync-helpers.ts` `fetch_changes` | catch `no such table` per-table → `log_server_event` + skip | error | A missing syncable table 500s the **entire** admin sync; it should degrade (log+skip) not crash. Pairs with the backfill migration. |
| **P2 (new)** | `remote-log.ts` heartbeat loop | gate heartbeats on `document.visibilityState==='visible'` (or stamp `hidden:true`) | n/a | Today's 11 h idle tab emitted ~1,200 heartbeats, inflating volume + the heartbeat-span "session duration". A hidden tab isn't engagement. |
| P3 (residual) | `routes/api/*/+server.ts` failure paths | `log_server_event` | warn/error | Auth/OTP, sync, uploads, email-inbound failures are still server-side-silent (today's P1 only surfaced because it's a hard throw → patched path). |
| P3 (residual) | `[dictionaryId]/+layout.ts` catch | `log_server_event` alongside `console.error` | error | First-SSR-paint dict-load failure still lands only in rotated `docker logs`. |

## 6. Dashboard improvements (Phase C + D)

**Phase C — verified + grounded proposals.**
- **Verified shipped today (06-29, `.issues/log-review-followups-2026-06-29.md`):** `navigator.webdriver`
  automation fold (M1), per-route p95 (`performance.by_route`, L2), `classify-error.ts` consolidation
  (M2), no-WebGL globe fallback + noise filter (L6). All confirmed in `log-analytics.ts` /
  `remote-log.ts`. ✅
- **Confirmed NOT a gap:** `error_clusters` has **no source filter** and includes `crash` — so today's
  server `dictionary_partners` crash **does** surface on the errors panel. Good.
- **NEW proposal #1 (primary, grounded in today's P1): schema-drift guard on the health strip.** The
  P1 would have been caught *before* a user hit a 500 if the dashboard asserted every
  `SYNCABLE_TABLE_NAMES` table exists in `shared.db`. Add a cheap check to the pipeline-health panel:
  `SELECT name FROM sqlite_master` ∩ `SYNCABLE_TABLE_NAMES` → list any missing in red ("⚠ schema drift:
  `dictionary_partners` missing — migration consolidated after apply"). Catches consolidation drift
  loudly. → backlog.
- **NEW proposal #2 (grounded in the 11 h idle tab): visibility-aware session duration.** The existing
  open "real session-duration distribution" item gets sharper today — a heartbeat-span median would
  read **668 min** for an idle tab. Either gate heartbeats on visibility (Phase B P2 above) or compute
  duration as **active (visible) span only**. → fold into the existing backlog item.

**Phase D — sibling cross-pollination (read house + tutor 06-28/06-29 reviews + backlogs).**
- **Borrow INTO LD:** house's **★ leader-worker health: failures by SQLite result `code` +
  current-vs-stale build** (filed house 06-29). LD already has the leader-worker health panel (shipped
  06-26) but only shows timeout/recovered/failed + had_leader/source — adding a `code` histogram
  (11=CORRUPT, 26=NOTADB…) + an `app_version` split on the failed bucket distinguishes "corruption on a
  stale build (self-heals)" from "RPC timeouts on the current build (a real regression)". Data already
  in `context.code` + `app_version`. → add to backlog.
- **Borrow INTO LD (Phase B, not dashboard):** house/tutor's **intent→shown success-event** idea — an
  open event (`entry_opened`) paired with a "rendered" success event so analytics can measure the
  intent→shown gap and rank silently-degraded reads. Fits LD's `entry_opened`/`dictionary_opened`
  vocab. → low until reader traffic; note in backlog.
- **Already aligned / declined:** deploy-markers + page_load-hygiene are in sync across all three.
  tutor's chat-funnel items are RN-only (N/A). The `webdriver` fold is now shipped in all three (LD
  M1 today; house session-level; tutor ported) — **LD is no longer the only one.**
- **LD wins to flag back to siblings:** today LD got the **first real-traffic validation** of the
  pipeline (caught a P1 schema-drift crash server-side) — a reminder to house/tutor that `error_clusters`
  **must include `source='server'` crashes** (LD's does). Plus the standing LD-only wins: bot/headless
  exclusion across all usage+geo, the CWV panel, and the pipeline-liveness + event-coverage strips.

→ Phase C #1/#2 + the house leader-health-code borrow appended to `.issues/future/dashboard-improvements.md`.

## Recommendations / action items
- [ ] **🔴 P1 (do first): create the `dictionary_partners` backfill migration + schema-drift audit** —
      see `.issues/missing-dictionary-partners-table-prod.md`. Live admin-sync 500s without it.
- [ ] **(Phase B, ties to P1) Make `fetch_changes` skip-and-log a missing syncable table** instead of
      500-ing the whole admin sync.
- [ ] **(Phase C, NEW, primary) Schema-drift guard on the pipeline-health strip** — assert every
      `SYNCABLE_TABLE_NAMES` table exists in `shared.db`; would have pre-empted today's P1.
- [ ] **(Phase B P2 / Phase C) Visibility-aware heartbeats + session duration** — today's 11 h idle tab
      emitted ~1,200 heartbeats and would read as 668 min of "engagement".
- [ ] **(Phase C, ported from house) Leader-worker failures by SQLite `code` + current-vs-stale build.**
- [ ] **(Phase B P3 residual) `log_server_event` in `routes/api/*` failure paths + the
      `[dictionaryId]/+layout.ts` catch.**
- [ ] **Re-run after the production cutover** — usage/perf/geo only generalize beyond KL-single-region
      once multi-region human traffic lands.

## Trend context (7d / 30d)
Sparkline (logs · errors · sessions per day):

```
2026-06-25  ·    0 logs   0 err   0 sess   (baseline — pipeline alive, no traffic)
2026-06-26  ▃  545 logs  11 err  44 sess   (synthetic e2e harness, ≈91% headless)
2026-06-27  ·    8 logs   0 err   1 sess   (idle — one post-deploy smoke-test)
2026-06-28  ·    0 logs   0 err   0 sess   (fully idle — cron healthy, no traffic)
2026-06-29  █ 1674 logs   7 err   8 sess   (FIRST real human traffic — 2 users, 7 deploys, 1 P1 caught)
```

First real-traffic day. The headline isn't a number — it's that the logging pipeline did exactly its
job: a server-side schema-drift crash that no synthetic run could produce was caught, clustered, and
root-caused from `client_logs` alone. The first multi-region trend still awaits the production cutover.
