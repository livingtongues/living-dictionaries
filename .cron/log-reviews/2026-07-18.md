# Log review — 2026-07-18 (livingdictionaries.app · window: last 24h)

*Read-only pass over the `living` VPS `logs.db` + `shared.db` rollups. No code changed. Every
recommendation re-checked against current source. Current prod build: **`1784341957685`** (deployed
today — replaced yesterday's `1784294143202`). Window is a rolling 24h through ~21:04 UTC (07-18
calendar day still partial).*

## TL;DR

- **Clean, quiet Saturday — zero real user-facing bugs.** Every error cluster is a known-noise family
  (stale-tab `sync_failed` zombie, bot 500s, blue-green stale-bundle 404s). **`source='server'` had 0
  error/crash rows.** Nothing new broke. (§1)
- **★ Yesterday's P2 export crash is CONFIRMED FIXED in prod.** The `friendly-name.ts` optional-chain
  fix shipped in build `1784341957685` — **0 `glosses` errors in the last 24h** (the 6 historical rows
  are all on the *old* build `1784294143202`, last seen 07-17 13:56 UTC). Closing it. (§1)
- **Engagement steady & broad: 36 signed-in users / 96 sessions (rolling 24h), 101 over 7d.** 50 entries
  created by **8 people**, plus a **204-update agent-API editing run on `wenshanhua`** (Jacob's API key)
  — the new "Entry edits · UI vs Agent API" panel now has real data flowing. 1,007 searches, 3,424
  entry-opens, 509 audio plays (mostly anonymous). (§2)
- **Host + retention green.** CPU avg **1.8%** / hottest 5-min **19.6%**, RAM avg **21.7%** / max
  **43.6%** (momentary), swap **0**, disk **16.6%**, 287 host_stats samples. Retention ran **20:35 UTC**;
  oldest row 07-04 (14 d). (§4, Host)
- **Phase D: LD is AHEAD on the malformed-`context` guard — the siblings' flag is STALE (again).**
  house's 07-17 review *still* says "LD has 0 guards / 56 sites." Wrong: LD ships **56 `json_valid`
  read-guards + `stringify_context_capped` on write**. Re-broadcasting so house/tutor stop carrying it. (§6)

---

## 1. Errors & crashes

Clustered by `message` + stack head over the rolling 24h. **Every row is a known-noise family** —
nothing needs a fix:

| Sev | Cluster | n | users | sessions | Verdict |
|---|---|--:|--:|--:|---|
| ⚪ noise | `sync_failed` (error) | 1,251 | 2 | 0 (null) | **1,186 are Greg's "river" zombie tab** (forgotten-laptop stale build, null-session) + Marlene 185. Known (07-09/07-14). |
| ⚪ noise | `Rejected` (serviceWorker.register) | 279 | 0 | 279 | 1 row/session, all anon/bot prerenderers — 3rd-party. Known. |
| ⚪ noise | `leader_boot_failed` | 25 | 1 | 9 | Anon/bot mid-boot; `will_retry`, self-heals. Known (07-10). |
| ⚪ noise | `initial dict sync failed` | 23 | 3 | 22 | Boot-cascade/crawler family — known-noise. |
| ⚪ noise | `play() interrupted by pause()` | 16 | 0 | 4 | Benign audio pause race. Known. |
| ⚪ noise | `Internal Error` (= the 15 `crash` rows) | 15 | 0 | 14 | **All bots** — SSR 500s on crawler traffic. Known (07-13). |
| ⚪ noise | `live_query_failed` (8) · `Failed to read dict bundle` MISUSE (4) | 12 | 0 | few | Leader/bot + self-heal families — known. |
| ⚪ noise | stale-swap chunk 404s (`Failed to fetch dyn. module` 4 · `Unable to preload CSS` 2 · `ServiceWorker fetch` 5 · `Script error.` 2) | ~13 | ≤1 | few | Blue-green deploy-swap 404s + cross-origin — known-noise. |
| ⚪ noise | `sync_halted_repeated_failure` | 1 | 0 | 0 (anon worker) | 1 anon worker halt (nyishi-type recursion watch, 07-15). Not a real contributor — non-actionable per 07-15 ruling. |
| ⚪ noise | `@context.toLowerCase` (1) · `gsi/client oa` (1) · `malformed JSON` (1) · `Load failed` (1) | 4 | 0 | few | JSON-LD extension / Google GSI / torn mid-swap response — 3rd-party/transient. Known. |

**Level totals (24h):** info 36,062 · error 1,315 · warn 1,052 · unhandled_rejection 306 · crash 15.
The 15 `crash` rows are **100% bot `Internal Error` 500s**. `source='server'` error/crash rows: **0.**

### ✅ The `/export` crash (yesterday's P2) — verified fixed in prod

- `friendly-name.ts:9` now reads `entry.senses?.[0]?.glosses` (all 3 refs), committed in build
  `1784341957685` and live. **0 `glosses` errors in the last 24h.** The only `glosses` rows in the
  whole 48h window (6) are on the *previous* build `1784294143202`, last at 07-17 13:56 UTC — i.e. all
  pre-fix. Closing `.issues/export-friendlyname-glosses-crash.md`.

---

## 2. Usage & engagement

| Window | Signed-in sessions (rolling live) | Signed-in users | All sessions (incl. anon/bot) |
|---|---:|---:|---:|
| 24h | **96** | **36** | 4,095 |
| 7d | 908 | **101** | — |

**Analytics events (24h):** `dictionary_opened` 3,999 (21 users) · `entry_opened` 3,424 (10) ·
`search_performed` 1,007 (11) · `dict_changes_pushed` 490 (15, healthy sync) · `audio_played` 509
(mostly anonymous) · **`entry_created` 50 (8 users)** · **`v1_entry_updated` 204 + `v1_entries_written`
30 (agent API, 1 key)** · `auth_login` 10 · `entry_deleted` 1.

### Who's doing what

**Admins (named — from the `ADMINS` allow-list; roster is Jacob / Diego / Greg / Cailie):**

- **Jacob Bowdoin** (`jwrunner7@gmail.com`, L3) — **4 real sessions**, ~00:39→10:58 UTC. Routes: `/admin`,
  `/admin/health`, `/admin/analytics`, `/admin/dictionaries`, `/admin/messages`, `/chat`, `/opyt321`.
  **Plus an agent-API editing run on `wenshanhua`** (his API key: 30 writes + **204 entry updates** via
  `api_key`) — this is what lit up the new UI-vs-Agent-API entry-edits panel. His raw count carries ~696
  null-session heartbeats from a backgrounded/stale tab of his own (benign — *not* `sync_failed`, just
  older-build heartbeats). No errors.
- **Diego Córdova** (`diego@livingtongues.org`, L3) — **absent today** (0 navigation events, no real
  session). Nothing to report.
- **Dr. Greg Anderson** (`livingtongues@gmail.com`, L2) — **1,232 raw events, but 1,186 are the "river"
  zombie tab** (§1, null-session stale build). Real activity ≈ **a few events** — one `/chat` nav. Very
  light; nothing broke. (Known-noise, no forced-reload — 07-09/07-14.)
- **Cailie Keating** (`ck1105@georgetown.edu`, L2) — **essentially absent**: 1 stale tab throwing 7
  `sync_failed` (client-behind stale-build 409, self-heals on reload), no real navigation. No real work.

**Everyone else (anonymous / aggregate — never named):** a healthy, broad contributor cohort. Heaviest
non-admin editors ran **~1,194 (26 sessions — a genuine multi-session editing marathon) / ~508 / ~503 /
~305** real events. The **8-person / 50-entry** creation spread + the 204-update `wenshanhua` agent run
is the day's editing headline — community + agent editing, not one power user. Public visitors drove
essentially all 509 audio plays anonymously. US-dominated (§3).

---

## 3. Performance & geography

- **Perf (24h):** `page_load` **p50 100 ms** (excellent) · p90 **976 ms** · p95 **1.86 s** (n=4,032,
  up from 2,027 — more traffic, same shape). `page_load` TTFB **p50 6 ms** / p90 290 ms / p95 643 ms.
  `search` **p50 10 ms** / p90 33 ms / avg 17 ms (snappy). `dict_boot` **avg 2,211 ms** (n=869, steady
  ~2.2 s). Nothing regressed on the new build.
- **Geo:** **US 3,760 sessions / 15 users** dominates. SG 75 / IN 23 / CN 36 are 0-user datacenter/crawler
  sources — discount. Real tail: MX 63 (6 users), FR 16 (3), GB 12 (4), KR 8 (1), NL 5 (1). No far-region
  alarm.
- **TTFB by distance to Boston origin:** <1,000 km p50 188 ms (n=195) · 1–3,000 km **p50 5.5 ms**
  (n=3,235, the Boston/US-east cluster) · 3–6,000 km p50 339 ms (n=261) · 6,000+ km **p50 1,074 ms**
  (n=125). The expected distance→TTFB tax, no new alarm — far-region users are a small slice.
- **Capability:** `opfs-worker` **4,001** vs `idb-worker` 147 — overwhelmingly the leader-worker DB; no
  below-capability (Safari < 15.4) alarms.

---

## 4. Health & housekeeping

- **Volume (24h):** ~38,750 rows — info 36,062 · error 1,315 · warn 1,052 · unhandled_rejection 306 ·
  crash 15. Settled and flat; the July-11/12 SEO-crawl spike (98k/184k) stays gone.
- **`logs.db`:** 926,959 rows total; oldest **2026-07-04** (14 d, hot-window edge). Disk **16.6%**.
- **Retention:** ran **20:35 UTC** today (fresh); rollup finalized through **07-17**. ✅ Green.
- **`real_errors` rollup still ~inflated by known-noise** (07-18 partial 995; ~1,388–1,807 recent days) —
  the null-session `sync_failed` zombie + bot 500s the classifier doesn't fold. Not re-derived; the
  metric-honesty backlog fix is still open (§6). Not a new watch (07-16 ruling).
- **i18n / dirty-rows:** nothing escalating. The free-form `ps.*`/`sd.*` user-data warns are now gated
  out at the source (committed 07-17, per the standing 07-17 decision) — working as designed.

## Host resources (past 24h)

**Plenty of headroom — nothing to act on.** CPU avg **1.8%**, hottest 5-min **19.6%**; load1 max **1.34**
(well under 2 cores); RAM avg **21.7%** / max **43.6%** (a momentary spike, avg flat — not a leak); swap
**0 MB** (1 MB parked); disk **16.6%**. **287** host_stats samples (full cadence). Every trend flat vs
prior days.

---

## 5. Coverage gaps (Phase B)

*No new logging gap justified today.* There were no real user-facing errors to have missed — the one
recent bug (export crash) is fixed, and every other cluster is a well-understood bot / stale-tab /
deploy-swap family already captured cleanly. The telemetry did its job. Adding SSR-origin logging for
bot traffic still isn't worth it.

---

## 6. Dashboard improvements (Phase C + D)

**Phase C — clean no-op.** Today's data justifies no new panel — it's a quiet green day with no finding
that a dashboard should surface (standing rule 2026-07-08/14: dashboards are for aggregate health/trend,
not per-error lists). Both `/admin/analytics` + `/admin/health` responded quickly during today's queries
— no load-perf regression. **Highest-value buildable backlog item to build next** (already open,
re-confirmed grounded today): **fold the null-session zombie `sync_failed`/`leader_boot_failed` storm out
of the `real_errors` headline** — it read ~1,251 `sync_failed` today, **1,186 from one dead tab**, so the
forever metric is ~100× the true user-facing count. Narrow safe fix (exclude `sync_failed`/`leader_boot_
failed` where `session_id IS NULL`); keeps real-session failures fully visible. Its companion one-liner —
adding **`Importing a module script failed.` + `Unable to preload CSS`** to `KNOWN_NOISE_PATTERNS`
(`Unable to preload CSS` fired twice today) — is the same metric-honesty class and equally cheap.

**Phase D — cross-pollinate (read house + tutor 07-17 reviews + backlogs):**

- **★ Re-broadcast: LD's malformed-`context` 500 guard is SHIPPED on BOTH sides — the siblings' flag is
  STALE.** house's 07-17 review *still* carries "(sibling carry) LD to borrow house's malformed-`context`
  guard (still 0 guards / 56 sites)." **Verified again today: LD's `log-analytics.ts` has 56
  `json_valid(context)` read-guards / 0 unguarded `json_extract`, and `stringify_context_capped` on the
  write side never persists invalid JSON.** Both halves shipped long ago. Nothing to build; broadcasting
  back so house/tutor stop re-flagging LD next run.
- **`admin_analytics_computed` compute-cost event — all three apps converging.** house filed it 07-17
  (ported from LD/tutor); LD keeps it as an open LOW backlog item; tutor keeps it in sync. No change —
  still worth building when the `/admin/analytics` load-perf watch next needs a persisted trend.
- **Candidate borrow from house — "New vs returning users over time."** It's house's build-next and fits
  LD's plain-language / user-numbers directive well; LD doesn't surface it yet. LOW priority behind LD's
  own two metric-honesty build-next items. Noting, not filing a fresh entry (house's is unshipped).
- **tutor's OTA / Mobile-health panels are RN-only — skip** (LD is web-only).
- **Symmetry (LD → siblings):** the malformed-`context` full-guard (read + write) is done in LD; the
  bot/headless exclusion across all usage+geo, CWV panel, pipeline-liveness strip, thin-data perf guard,
  At-a-Glance strip, and Sync-health stuck-(user,dict) panel remain LD-originals the siblings track.

---

## Recommendations / action items

- [x] ✅ **P2 export crash — CONFIRMED FIXED in prod** (`friendly-name.ts` optional chain, build
      `1784341957685`; 0 `glosses` errors in 24h). Close `.issues/export-friendlyname-glosses-crash.md`. §1.
- [ ] ★ **Phase C build-next — fold null-session `sync_failed`/`leader_boot_failed` out of `real_errors`**
      so the forever metric stops being ~100× inflated by one zombie tab (`is_noise_error_message` /
      rollup: exclude those two messages where `session_id IS NULL`). Backlog (filed 07-16). §6.
- [ ] ★ **Phase C build-next — add `Importing a module script failed.` + `Unable to preload CSS` to
      `KNOWN_NOISE_PATTERNS`** (`classify-error.ts`). Backlog, one-line each. §6.
- [ ] ★ **Phase D — re-broadcast to house/tutor: LD's malformed-`context` guard is SHIPPED both sides**
      (read: 56 `json_valid`; write: `stringify_context_capped`). Stop carrying the "LD has 0 guards" flag. §6.

## Trend context (7d)

Signed-in **sessions** (calendar-day rollup) and **`real_errors`** per day (07-18 partial). `real_errors`
looks high but is **~100× the true user-facing count** — one forgotten-laptop `sync_failed` tab drives
most of it (§4/§6):

<svg viewBox="0 0 480 175" width="100%" style={{maxWidth:'520px'}} role="img" aria-label="Sessions and real_errors, 7d">{[['07-12',462,599],['07-13',483,1596],['07-14',544,1807],['07-15',545,1704],['07-16',443,1388],['07-17',495,1510],['07-18*',386,995]].map(([d,s,e],i)=><g key={d}><rect x={32+i*62} y={135-s/545*100} width="23" height={s/545*100} rx="2" fill="#6366f1"><title>{s} sessions</title></rect><rect x={55+i*62} y={135-e/1807*100} width="23" height={e/1807*100} rx="2" fill="#f59e0b"><title>{e} real_errors (mostly zombie noise)</title></rect><text x={43+i*62} y="149" textAnchor="middle" fontSize="8" fill="currentColor" fillOpacity="0.6">{d}</text><text x={43+i*62} y={131-s/545*100} textAnchor="middle" fontSize="7" fill="currentColor">{s}</text></g>)}<line x1="22" y1="135" x2="466" y2="135" stroke="currentColor" strokeOpacity="0.25" /><text x="22" y="12" fontSize="9" fill="#6366f1">■ signed-in sessions (rollup)</text><text x="200" y="12" fontSize="9" fill="#f59e0b">■ real_errors (zombie-inflated — §4)</text></svg>

Sessions flat ~380–545/day (07-18 partial, Saturday). Daily log volume settled ~24–35k/day (07-18
34,818) — the SEO-crawl spike is long gone. Zero real user-facing bugs today.
