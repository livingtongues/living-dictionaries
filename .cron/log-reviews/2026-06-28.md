# Log review — 2026-06-28 (new.livingdictionaries.app · window: last 24h)

> **Fourth run.** 06-25 = zero-data baseline; 06-26 = first real (≈91% synthetic) data, 545 rows;
> 06-27 = idle + one smoke-test (8 rows). Today: **fully idle.** The 24h window holds **0 rows / 0
> sessions / 0 errors** — and there are **no rows on 06-28 at all**; the newest log in the whole DB
> is `2026-06-27T01:03Z` (≈45h ago). No deploy today → no post-deploy smoke-test, and still zero
> human traffic pre-cutover. The 553-row corpus is **byte-identical to yesterday**, so triage stays
> on the 06-26 synthetic set. This run is: (a) confirm the pipeline is alive despite no ingestion,
> (b) verify yesterday's two dashboard ships landed, (c) one fresh, data-grounded Phase C/B finding,
> (d) Phase D grooming.

## TL;DR
- **🟢 Pipeline alive, just no traffic.** Retention cron ran today (`log_retention_ran_at =
  2026-06-28T19:53:53Z`), proving the active `sveltekit_blue` container is healthy and writing
  `db_metadata` — but **0 `client_logs` ingested in ~45h** because there was no deploy (no smoke-test)
  and no human traffic. This is *idle*, not *broken*. **0 errors, 0 new clusters.**
- **🟦 Corpus unchanged.** 553 rows total (same as 06-27), 528 production-client / 25 server, DB
  ~908 KB. Every usage/perf/geo number is still the single 06-26 e2e run (100% MY/Malaysia geo).
- **✅ Yesterday's two dashboard ships landed (verified in code):** deploy/version markers
  (`log-analytics.ts` `deploys` → `deploy_events` on the Traffic ComboChart + Errors LineChart) and
  `page_load` percentile hygiene (`duration_ms > 0` filter + `PerfSummary.slowest = {duration_ms,
  route}` via the inline-tested `url_route` helper, rendered as a "slowest Ns · /route" row).
- **🟡 Fresh finding (the headline):** the **non-headless Playwright e2e sessions slip the bot
  regex**. 413 rows are `HeadlessChrome` (correctly tagged bot), but **99 + 16 rows** carry a plain
  `Chrome/148|149 … (X11; Linux x86_64) … Safari/537.36` UA with **no `HeadlessChrome` token** — and
  `is_bot_user_agent` explicitly classifies that as a **human** (there's even a unit test asserting
  it). So ~99 sessions of synthetic e2e traffic **masquerade as humans on the default dashboard
  view**. Fix: stamp `navigator.webdriver` at `session_start` and fold it into the bot filter.
- **Top opportunity:** the `webdriver`-flag capture above — kills the fake-human pollution now and
  future-proofs against Selenium/Playwright once real traffic lands. Likely a shared gap → flag to
  house + tutor.

## 1. Errors & crashes
**24h: 0 error/crash rows** (0 rows of any kind). 7-day clusters are **unchanged from yesterday** —
all 06-26, all synthetic, none recurring:

| Sev | Cluster | n (7d) | users | Status | Note |
|---|---|---|---|---|---|
| ⚪ noise | `[post_request] Network error for /api/log:` | 5 | 0 | In `KNOWN_NOISE_PATTERNS` ✅ | Self-referential flush blip from the idle admin tab; excluded from the headline count by the cluster panel. |
| 🟡 P3 | `Internal Error` (500) on `/e2e-log-*/entries` | 4 | 1 | Cause unrecoverable | Synthetic dict-load failures from 06-26; real stack went to rotated `docker logs`. See §5. |
| 🟡 (resolved-as-warn) | `Missing session` (401) on `/admin/analytics` | 1 | 0 | Fixed at source ✅ | `+error.svelte` status→level map records this as `warn`, not `crash`, going forward. |
| 🟡 P3 | `Failed to initialize WebGL` (Mapbox, `/`) | 1 | 0 | Open (low) | One anon browser with WebGL off; graceful no-WebGL globe fallback still a nice-to-have. |

**No P1, no multi-user clusters, no new `unhandled_rejection`.** Real-world error behavior remains
untested until human traffic lands.

## 2. Usage & engagement
**⚠️ Still 100% synthetic, and now stale (no 06-28 rows).** The corpus below is the frozen 06-26 e2e
harness, carried for continuity.

| Metric | 24h | 7d | 30d | Note |
|---|---|---|---|---|
| Rows | 0 | 553 | 553 | nothing since 06-27T01:03Z |
| Sessions | 0 | 45 | 45 | all 06-26 e2e (+1 06-27 smoke) |
| Distinct `user_id` | 0 | 1 | 1 | almost all rows anon |
| Errors | 0 | 11 | 11 | all 06-26 |

- **Analytics events (all-time, all wired & firing):** `dictionary_opened` 21, `auth_login` 14,
  `dictionary_created` 10, `search_performed` 4, `entry_opened` 2, `dict_changes_pushed` 1.
  `audio_played` = 0 (wired at `Audio.svelte:56`; absence is just no synthetic audio click).
- **Platform/geo/capability:** 100% `web`, 100% MY (Malaysia — the e2e agent box), 100%
  `opfs-worker`, 0 below-capability. **But see §6:** ~99 of these "human" sessions are actually the
  Playwright runner mis-classified, not real MY users.

## 3. Performance & geography
- **Web vitals:** **163 rows** — TTFB 43, FCP 42, CLS 37, LCP 37, INP 4 (all 06-26, healthy; flush
  fine on HeadlessChrome). The CWV panel has real (if synthetic-sourced) data.
- **`page_load` (45 samples):** avg ≈ 1544ms, **max 17 876ms** (cold OPFS-worker boot). The new
  `duration_ms > 0` filter + "slowest · route" stat (shipped today) now handle the 0ms-bfcache and
  cold-boot outliers that distorted last run's percentiles.
- **`search`:** avg ≈ 14ms, max 33ms (local Orama — fast).
- **Geo TTFB:** single-region (MY ≈ 15 000 km from Boston) → the ~1s TTFB is the distance tax, but a
  one-region corpus can't drive the by-distance RUM split. Revisit post-cutover.

## 4. Health & housekeeping
- **Volume:** 553 rows total; 06-26 = 545, 06-27 = 8, **06-28 = 0**. DB **227 pages × 4096 ≈ 908 KB**.
- **Composition:** production-client 528 / server (NULL UA) 25.
- **Retention:** ✅ `log_retention_ran_at = 2026-06-28T19:53:53Z` (today) — **the key health proof
  this run:** zero ingestion didn't mean a dead box; the active node's cron ran on schedule. Oldest
  row 06-26 → the ≤14d invariant is trivially met.
- **Rollup:** `log_daily_metrics` current through 06-27 (06-26: 45 metrics/1846; 06-27: 11/27); no
  06-28 metrics because there were no 06-28 rows. Consistent.
- **Note:** `db_metadata` has no `log_rollup_ran_at` key (only `log_retention_ran_at`) — the rollup
  is folded into the retention cron, so this is expected, not a gap.

## 5. Coverage gaps (Phase B)
Yesterday's top gaps stay closed. Residual (unchanged) + one **new** gap surfaced by today's UA
spot-check:

| # | Path | Event / field | Level | Fields | Why |
|---|---|---|---|---|---|
| **NEW P2** | `remote-log.ts` `session_start` | add `context.webdriver = navigator.webdriver` (+ fold into the bot filter) | info | `{ webdriver: boolean }` | ~99 e2e rows carry a plain `Chrome … Linux x86_64` UA (no `HeadlessChrome` token) and are classed **human** by `is_bot_user_agent`, polluting the default dashboard view. `navigator.webdriver` is `true` under Playwright/Selenium and is the reliable signal a UA regex can't catch. Captured **nowhere** today (verified). |
| P3 (residual) | `routes/[dictionaryId]/+layout.ts` catch (~L182) | `log_server_event` (alongside the kept `console.error`) | error | `{ dict_id, phase:'dict_layout_load', error, stack }` | Client-nav case covered by the patched `console.error`; a **first-SSR-paint** dict-load failure still lands only in rotated `docker logs`. |
| P3 (residual) | `routes/api/*/+server.ts` failure paths | `log_server_event` | warn/error | `{ route, status, error }` | Failure paths (auth send-code/verify, sync `/changes`, uploads, email-inbound) are still server-side-silent. |
| P3 (residual) | homepage Mapbox globe | no-WebGL fallback + filter Mapbox-internal WebGL errors as noise | n/a | n/a | One anon WebGL-disabled session got a hard globe failure. |

## 6. Dashboard improvements (Phase C + D)

**Phase C — verify + one fresh proposal.**
- **Verified shipped (06-28):** deploy/version markers (`deploys` → `deploy_events` on Traffic
  ComboChart + Errors LineChart) and `page_load` percentile hygiene (`duration_ms > 0` + `slowest`
  via `url_route`, inline-tested). Both present in `log-analytics.ts` + `analytics/+page.svelte`. ✅
- **Spot-check finding → new proposal (primary): `navigator.webdriver`-based automation exclusion.**
  Today's UA breakdown shows the default **Humans** view is ~99 sessions of *headed Playwright*, not
  humans — the headless half (413 rows) is correctly de-botted, but the regex can't catch a headed
  automation UA. Stamp `navigator.webdriver` at `session_start`, then extend the human/bot SQL
  (`is_bot_ua(user_agent)=1 OR webdriver=1` → bot) so automated sessions land in the Bots bucket
  regardless of UA. Kills the pre-cutover fake-human pollution *and* future-proofs against real
  Selenium/Playwright hits post-cutover. → backlog.

**Phase D — sibling cross-pollination (read house + tutor 06-27/06-28 reviews + backlogs).**
- **Borrowing INTO LD (new, not yet on LD's backlog):**
  - **house's `per-route p95 performance`** (slowest routes first, once `track_timing` accumulates) —
    fits LD cleanly; LD has `page_load`/`search` perf but no per-route breakdown. → add to backlog.
  - **house's `error-per-use by feature`** (errors normalized by the matching usage event, to rank
    what's breaking relative to how much it's used) — fits LD's event vocab. → add to backlog (low
    until real traffic).
- **Already covered / declined:** house just *ported FROM LD* the deploy-markers, page_load-hygiene,
  and session-duration-distribution items (LD shipped the first two; the third is on LD's open
  backlog). house's `error_audience` real-user split stays LOW on LD (subsumed by clusters +
  `is_noise` + human-only). tutor's open items are RN-only (chat funnel, Mobile Health) — N/A.
- **LD wins to flag back to the siblings:** the **`webdriver` automation-exclusion** idea above is
  almost certainly a *shared* gap (house + tutor run e2e too) — recommend they each capture
  `navigator.webdriver`. Plus the standing LD-only wins: **bot/headless exclusion across ALL
  usage+geo metrics**, the **Core Web Vitals** panel, and the **pipeline-liveness + event-coverage**
  strips.

→ Phase C + the two Phase D borrows appended to `.issues/future/dashboard-improvements.md`.

## Recommendations / action items
- [ ] **(Phase B/C, P2 — NEW, primary) Capture `navigator.webdriver` at `session_start` and fold it
      into the human/bot filter** — stops headed Playwright/Selenium sessions masquerading as humans
      on the default analytics view (today: ~99 fake-human rows). Justified by today's UA spot-check.
- [ ] **(Phase C) `per-route p95` perf panel** — ported from house; slowest routes first once
      `track_timing` data accumulates. Backlog.
- [ ] **(Phase C, low) `error-per-use by feature`** — ported from house; errors normalized by the
      matching usage event. Backlog.
- [ ] **(Phase B, P3 residual) `log_server_event` in the `[dictionaryId]/+layout.ts` catch** so a
      first-SSR-paint dict-load failure isn't lost to rotated `docker logs`.
- [ ] **(Phase B, P3) `log_server_event` in `routes/api/*` failure paths** (auth, sync, uploads,
      email-inbound) — failures are still server-side-silent.
- [ ] **(P3) Graceful no-WebGL globe fallback** + filter Mapbox-internal WebGL errors as noise.
- [ ] **Re-run after the production cutover / first genuine human traffic** — usage/perf/geo/CWV only
      measure reality then; that's also when the `webdriver` + bot exclusions stop being cosmetic.

## Trend context (7d / 30d)
Sparkline (logs · errors · sessions per day):

```
2026-06-25  ▁    0 logs   0 err   0 sess   (baseline — pipeline alive, no traffic)
2026-06-26  █  545 logs  11 err  44 sess   (first real data; ≈91% synthetic/headless)
2026-06-27  ▁    8 logs   0 err   1 sess   (idle — one post-deploy smoke-test)
2026-06-28  ·    0 logs   0 err   0 sess   (fully idle — no deploy, no traffic; cron healthy)
```

Still no human trend — three synthetic/idle days. The first meaningful trend arrives with
post-cutover human traffic; until then each run's value is (a) health confirmation (today: cron alive
despite zero ingestion), (b) dashboard spot-checks (today: the `webdriver`/fake-human finding), and
(c) Phase C/D grooming.
