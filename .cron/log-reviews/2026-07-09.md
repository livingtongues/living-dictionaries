# Log review — 2026-07-09 (run 2 · 21:05 UTC · window: last 24h, delta since run 1 @ 14:45)

*Second read-only pass of the `living` VPS `logs.db` + `shared.db` rollups, ~6h after
[run 1](#log-review--2026-07-09-livingdictionariesapp--window-last-24h) below. Focus: what changed
since 14:45 UTC. No code changed. Every claim re-checked against current source.*

## TL;DR (run 2)

- **Still HEALTHY on current builds — no new P1.** Zero new error classes on the current build
  `1783605626771` in the last 6h. The raw error volume is unchanged in *shape* from run 1: Googlebot
  SEO crawl + a handful of ancient stuck tabs, nothing live-regressing.
- **The stale-tab storm now has THREE named tabs and is still live at 21:02 UTC.** Run 1's
  "Zapotec editor" is resolved to a **single non-admin editor on a 6-day-old (07-03) build**
  (**4,436** `sync_failed`, 217 in the last 3h). **Admin Dr. Greg Anderson** is unchanged (**1,844**
  on the *same* 07-03 build, only `/chat`, 204 in the last 3h). A **third stuck tab appeared** — a
  non-admin on build `1783245164404` (**347** `sync_failed`, 89 in the last 3h). None can receive
  fixes until they hard-reload. This is the day's only real recurring pain.
- **One new error cluster — and it's benign:** `[orama-watcher] delta scan failed` (**17** rows /
  **11** users / 1 row each) is the **same `code:21` SQLITE_MISUSE boot-cascade** as
  `live_query_failed`, just surfacing at the Orama search-index delta scan while OPFS boots on
  `dictionary_opened`. Transient, one-per-session, spread across 11 dictionaries. Fold it into the
  same crawler/boot-cascade demotion already backlogged.
- **One session to keep an eye on:** a single human hit **251 `leader_boot_failed`** (warn-level) in
  ~47 min on the *intermediate* build `1783585533693` — leader election never settled for that tab.
  n=1 user, transient, not on the current build. Watch, don't fix.
- **`logs.db` grew 595 → 706 MB in ~6h** (+111 MB) under the crawl + stuck-tab retries. Retention
  ran fresh at 20:03 UTC; oldest row still inside the 14-day hot window. Host has plenty of headroom.

### Stale-tab storm — the three stuck tabs (still emitting at 21:02 UTC)

| Who | `sync_failed` (24h) | last 3h | Build | Age | Route/cause |
|---|---:|---:|---|---|---|
| Non-admin editor (anonymous — run 1's "Zapotec editor") | **4,436** | 217 | `1783096241136` | 07-03 (~6d) | `client_behind` on Zapotec dicts |
| **Admin — Dr. Greg Anderson** (L2) | **1,844** | 204 | `1783096241136` | 07-03 (~6d) | only `/chat`; `client_behind` |
| Non-admin (anonymous — **NEW third tab**) | **347** | 89 | `1783245164404` | 07-04 (~5d) | stuck tab, null session |

The remaining bulk `sync_failed` on the *current* build is **anonymous crawler traffic** — a UA
check on the last 3h shows **384 Nexus 5X Googlebot** + 114 a Mac Firefox rv:152 scanner + 70 Windows,
essentially zero signed-in humans. Real humans on the current build sit at **near-zero**
`client_behind`.

## Host resources (run 2, past 24h)

**Plenty of headroom — nothing to act on.** 175 host-stats samples (cron now ~19h in).

- CPU avg **4.6%** · hottest 5-min window **27.7%** · `load1` max **1.7** (2-core box)
- RAM avg **29%** · max **48.3%** · swap max **2.6%**
- Disk **15.7%** — but `logs.db` is now **706 MB** (+111 MB since run 1's 595 MB); disk % is fine,
  file growth under the crawl is the thing to keep glancing at.

## Perf (run 2)

`search` avg **~0.97 s** (n=860) — consistent with run 1's ~1.1 s, still slower than the pre-crawl
~319 ms baseline; likely mix/skew from the busy day, not a confirmed regression. `page_load` avg is
bot-deflated to ~253 ms (18.4k rows, crawler loads are fast). Nothing new to chase.

## Action items (run 2 — additive to run 1)

- [ ] **Nudge the three stuck tabs to hard-reload.** Dr. Greg Anderson's chat tab (admin) plus two
      non-admin editor tabs are all on 5–6-day-old builds and account for essentially 100% of the
      human `sync_failed` storm; they cannot self-heal via the service worker. Speakable: "Three
      browser tabs from five and six days ago are stuck retrying sync forever — Greg's chat tab is
      one of them; they need a hard reload." (Reaffirms run 1's carried item — now with a third tab.)
- [ ] **Fold `[orama-watcher] delta scan failed` into the crawler/boot-cascade demotion** already
      backlogged (the `code:21` boot MISUSE family). It is the same transient class as
      `live_query_failed`, just at the Orama delta scan; no separate fix needed. → covered by the
      open "bot/crawler share % on error clusters" item in `.issues/future/dashboard-improvements.md`.
- [ ] **Watch (no fix):** one human's 251-in-47-min `leader_boot_failed` warn burst on the
      intermediate build `1783585533693` — re-check next run; file a focused issue only if
      leader-election failure recurs on the *current* build.
- [ ] **Housekeeping:** `logs.db` +111 MB in 6h. The storage/WAL strip shipped today should make
      this glanceable; consider a VACUUM-after-prune if growth stays this steep once the SEO crawl
      settles. (Reaffirms run 1.)

*Phase B / C / D: no net-new proposals beyond run 1 — the "bot/crawler share % on error clusters"
Phase C item (open in `.issues/future/dashboard-improvements.md`) would have instantly explained
today's `live_query_failed` + the new `orama-watcher` cluster as ~all-crawler, so it remains the
top build. Nothing new to append to the backlog this run.*

---

# Log review — 2026-07-09 (livingdictionaries.app · window: last 24h)

*Read-only investigation of the `living` VPS `logs.db` (raw `client_logs`) + `shared.db` rollups.
No code was changed. Every "already fixed / already shipped" claim below was checked against the
current source and git history.*

## TL;DR

- **Health verdict: HEALTHY for real humans — but the raw numbers look like a fire.** ~**10,400 of
  ~11,500 sessions** today are **Googlebot / GoogleOther** (Nexus 5X UA from South Carolina) crawling
  the new sitemaps shipped in `b7fb42fd` (02:40 UTC). Strip bots and you get **~712 human sessions /
  34 signed-in users** — a normal busy day, not a meltdown.
- **The top "errors" are almost all crawler noise.** `live_query_failed` (1,378) and `initial dict
  sync failed` (931) are **~94–98% Googlebot** hitting `SQLITE_MISUSE` (code 21) while OPFS boots on
  entry deep-links. Among real humans: **34 + 10 + 31** rows across those three messages — low reach,
  and the new `sync_self_healed` path (`seq_cursor_transition`, **970 rows / 17 users**) is doing its
  job after the `71d18e34` server_seq fix.
- **Stale-client storm is unchanged from yesterday.** Admin **Dr. Greg Anderson** (~1,918
  `sync_failed` on a 6-day-old 07-03 tab, only route `/chat`) and one **Zapotec editor** (~4,160
  retries on three Zapotec dicts) still cannot receive fixes. Build-adoption strip shipped today
  (`61465cf8`) would name them.
- **Sugstun FK wedge is ✅ fixed.** Overnight `FOREIGN KEY constraint failed` on `sugtstun` (editors
  + Diego) stopped after `71d18e34` (08:22 UTC). Watch for recurrence on current builds — none seen
  after 08:27 except one Jacob test row.
- **Top opportunity:** put a **bot/crawler share % on each error cluster**. Today's review spent most
  of its time proving the #1/#2 clusters were Googlebot — the dashboard already keeps bot rows in
  clusters "as a real signal" but never shows *how much* of a cluster is bots.

<svg viewBox="0 0 420 140" width="100%" style={{maxWidth:'440px'}} role="img" aria-label="Human vs bot sessions last 24h">
  <text x="0" y="14" fontSize="12" fill="currentColor" fontWeight="600">Sessions · last 24h</text>
  <rect x="0" y="28" width="380" height="28" rx="4" fill="#22c55e" fillOpacity="0.25" />
  <rect x="0" y="28" width="24" height="28" rx="4" fill="#22c55e" />
  <text x="30" y="47" fontSize="11" fill="currentColor">human ~712</text>
  <rect x="0" y="68" width="380" height="28" rx="4" fill="#f59e0b" fillOpacity="0.2" />
  <rect x="0" y="68" width="356" height="28" rx="4" fill="#f59e0b" />
  <text x="8" y="87" fontSize="11" fill="#1c1917">Googlebot/GoogleOther ~10,400</text>
  <text x="0" y="120" fontSize="10" fill="currentColor" fillOpacity="0.65">SEO crawl after sitemaps ship (b7fb42fd) — expected, but blinds raw error triage</text>
</svg>

---

## 1. Errors & crashes

Clustered by message + stack head over the 24h window. **Bot share called out** — without it the
severity is misleading.

| Cluster | n (24h) | Human-ish | Severity | Verdict |
|---|---:|---:|---|---|
| `sync_failed` | ~8,800 (warn+error) | ~2 named stale tabs dominate | 🟠 P2 | **Stale clients** on 07-03 builds — same as 07-08. See §below. |
| `live_query_failed` | 1,378 | **~10** (1 signed-in session) | ⚪ mostly bot | **98.5% Nexus 5X Googlebot**; code 21 / `had_leader:true` on entry pages. Human remainder is transient MISUSE during boot. |
| `initial dict sync failed` | 931 | **~34** (14 signed-in) | 🟡 P3 | **94% Googlebot**; human rows are code 21 `bad parameter or other API misuse` starting ~08:27 (right after `71d18e34` deploy) — pairs with 970 `sync_self_healed` / `seq_cursor_transition`. |
| `Rejected` (`wrsParams.serviceWorkers…`) | ~930 | 0 | ⚪ noise | Headless-scanner SW shim. Filter next run. |
| `leader_boot_failed` | 272 | low | 🟡 P3 | Mostly crawler / OPFS open races; well-instrumented. |
| `sync_self_healed` | 970 | 17 users / 4 sessions | ✅ good | New path from `71d18e34` — **working as designed**. |
| `Failed to read dict bundle from wa-sqlite` | 53 | mixed | 🟡 P3 | Boot-cascade sibling; low reach. |
| `effect_update_depth_exceeded` | 35 | mostly old builds | ✅ fixed + ⚠ 1 current | **0 on mid-day builds**; **1 hit on current `…60626771`** (proto-katwijks editor after Add Entry / IPA keyboard / Save). Watch — may be a residual path. |
| WorldMap `getBoundingClientRect` | 46 | 1 session, stale build `…411458430` | ✅ fixed | Draining tail of `95abffb2`. |
| `Internal Error` / stale-chunk CSS preload | ~50 | anon | ⚪ deploy noise | Expected around the day's many deploys. |
| `{"isTrusted":true}` | 14 | — | ✅ noise-classified | `classify-error.ts` + `remote-log.ts` fix shipped in `61465cf8`. |
| ServiceWorker register failures | ~9 | anon | ⚪ noise | Benign / network. |
| `play() interrupted by pause()` | ~7 | — | ⚪ noise | Benign audio races. |

### The Googlebot × SQLITE_MISUSE cascade (looks like P1, isn't)

Started **08:25–08:30 UTC** — the same minute `71d18e34` (server_seq + self-heal) and build
`1783585533693` went live — and coincides with Google discovering the new sitemaps from `b7fb42fd`
(shipped 02:40 UTC). Pattern on every sample:

- UA: `Nexus 5X … (compatible; Googlebot/2.1)` or `GoogleOther` · geo **US/SC**
- Route: `/{dict}/entry/{id}` deep link
- Context: `code:21` (`SQLITE_MISUSE` / "bad parameter or other API misuse"), `had_leader:true`,
  tables `entries` / `senses` / `entry_relationships` / `relationship_types`
- One session → one `initial dict sync failed` + a burst of `live_query_failed` (repeat_count coalesced)

**Human impact of the same code-21 class:** 14 signed-in users, typically **1 row each** at dict open,
spread across many dictionaries (not one broken dict). Paired with `sync_self_healed` —
recommend treating crawler MISUSE as known-noise *or* demoting bot boot-cascade rows, and keeping a
human-only watch on code 21 for a day.

### Stale-client `sync_failed` storm (carried from 07-08)

| Who | Rows | Build | Age | Cause |
|---|---:|---|---|---|
| Zapotec editor (non-admin) | ~4,160 | `…096241136` | 07-03 (~6d) | `client_behind` on `zapoteco-de-la-sierra-n` + `zapoteco-serrano`; `not_found`/404 on `zapoteco-de-analco` (classifier now names this — shipped `61465cf8`) |
| **Admin — Dr. Greg Anderson** | ~1,918 | `…096241136` | 07-03 (~6d) | `client_behind` on `apatani` + `river`; only navigated `/chat` |
| anonymous stale tabs | ~1,500 | 07-03 builds | — | `client_behind` |
| everyone on current builds | near-zero `client_behind` | `…605626771` | today | — |

Greg was flagged in **both** 07-08 runs and is **still** emitting as of 14:43 UTC. Hard-reload nudge
still outstanding.

### Overnight Sugstun FK wedge — ✅ already fixed

Before `71d18e34` (08:22 UTC), `sugtstun` editors hit `FOREIGN KEY constraint failed` on sync
(56 + 11 rows for two editors; Diego 4; Jacob 1–2). **Zero FK rows on builds after 08:27** except one
Jacob row on an intermediate build. Self-heal + server_seq cursor fix verified live.

### One current-build `effect_update_depth` to watch

At **14:35 UTC** on build `1783605626771` (current), a signed-in editor on
`/proto-katwijks/entry/…` hit `effect_update_depth_exceeded` after: Add Entry → create → open →
Vowels/Diacritics/Suprasegmentals IPA keyboard → Save×3. **n=1** — not a storm — but it is the first
effect-loop hit on a post-`10154e30` current build. Do not panic; do re-check next run. If it
recurs around the IPA / orthography keyboard, file a focused issue.

---

## 2. Usage & engagement

| Window | Raw sessions | Human sessions (est.) | Signed-in users | Events |
|---|---:|---:|---:|---:|
| 24h | **11,585** | **~712** | 38 (34 with human filter) | 89,796 |
| 7d  | 19,026 | ~4,350 | 88 / 73 | 285,423 |
| 30d | 19,131 | ~4,411 | 89 / 74 | 308,034 |

Raw session count is dominated by the SEO crawl. Heartbeat-bearing sessions (real dwell): **353**.
Median session duration (heartbeat span): **~4.0 min**; p90 **~43 min**; max ~20 h (idle tab).

**Top human routes (by sessions):** `/` (132) · `/sugtstun/entries` (72) · `/iipay-aa/entries` (63) ·
`/tutelo-saponi/entries` (34) · `/highlander/entries` (27) · `/boienen-old-buhi-langua/entries` (26) ·
`/chat` (16) · `/create-dictionary` (15).

**Top events (raw — bot-inflated for open/entry):** `dictionary_opened` / `entry_opened` track the
crawl. Human-meaningful: `search_performed` **752** (237 sessions) · `audio_played` **884** (64
sessions) · `entry_created` **88** · `dict_changes_pushed` **684**.

**Top dictionaries (human-ish `dictionary_opened`):** `sugtstun` (93) · `iipay-aa` (72) ·
`tutelo-saponi` (38) · `boienen-old-buhi-langua` (34) · `highlander` (27) · `hazaragi` (23) ·
`kalinago` (20) · `nahuatl` (19).

### Who's doing what

**Admins (named):**

- **Jacob Bowdoin** (L3) — 632 events / 23 sessions, 15:03→14:40 UTC. Heavy `/admin` · `/admin/health`
  · `/admin/sync` · `/admin/dictionaries` · `/admin/messages` · `/chat`, plus `river` / `eyak` /
  `nahuatl` / `bahasa-lani`. 53 error-level rows are mostly his own deploy/test surface around the
  server_seq + health-strip ships. Shipped today's big fixes (`71d18e34`, `61465cf8`, `6a7b3a6b`,
  `dc402625`).
- **Diego Córdova** (L3) — 345 events / 10 sessions, **23:42–00:42 UTC only** (overnight). Hit the
  pre-fix Sugstun FK wedge (4 `sync_failed`), then browsed `/admin/users`, `/admin/messages`,
  `/chat`, `sugtstun`, `batsi-kop-tsotsil-tsot`, `nahuatl` (settings + home). Quiet after midnight.
- **Dr. Greg Anderson** (L2) — **still the poster-child stale tab**: ~1,929 events / 2 sessions over
  the full 24h, **~1,918 `sync_failed`**, build `…096241136` (07-03), only route `/chat`. 👉 hard-reload
  still needed; service worker is not updating him.
- **Anna Luisa Daigneault** (L2) — not active in the 24h window.

**Everyone else (aggregate, unnamed):** ~34 signed-in non-admin editors. Heaviest editing surfaces:
**Chugachmiut / Sugt'stun** (overnight FK pain, then recovered), **Ii'pay Aa**, **Tutelo-Saponi**,
**Boienen / Old Buhi**, hobby/conlang dicts (`intergermanisch-sprak`, `proto-katwijks`, `highlander`).
One Zapotec-dictionaries editor's 6-day-old tab remains the largest single error contributor. No
individual non-admin is named.

---

## 3. Performance & geography

**Client perf (non-Nexus, 24h):** `page_load` avg **~3.0 s** (n=938) · `search` avg **~1.1 s**
(n=750). Search is slower than yesterday's ~319 ms — worth a glance against the
`.issues/search-and-pagination-frozen-2026-07-09.md` fix (`7366e6df`); may be mix/skew, not a
regression.

**Core Web Vitals (raw vitals rows; TTFB heavily bot-skewed toward SC):** human-ish TTFB by country
(avg): US ~877 ms · MX ~886 · IN ~1.3 s · MY ~841 · CA ~249 · NL ~1.5 s · CN ~1.6 s · SG ~2.5 s.
Far-region tax still real (Singapore / China / Netherlands). LCP avg ~2.8 s · INP ~187 ms · CLS
~0.057 — acceptable.

**Capability:** almost everyone who can run a worker is on `opfs-worker` (11,419 raw sessions incl.
bots that report the tier) · `idb-worker` 105. No below-capability Safari < 15.4 signal worth acting on.

**Uptime probes:** 275 / 24h, samples `ok:true` status 200 (vantage `mustang-my`). Healthy. TTFB from
Malaysia vantage ~0.4–1.1 s.

---

## 4. Health & housekeeping

| Metric | Value |
|---|---|
| `logs.db` size | **~595 MB** (+~128 MB vs yesterday's ~467 MB) |
| `logs.db-wal` | ~4.0 MB (ratio ~0.007 — healthy) |
| `shared.db` | ~18.4 MB · WAL ~0.25 MB |
| `logs-archive.db` | essentially empty (16 KB) |
| Oldest raw row | 2026-06-26 (within 14d hot window) |
| Retention last ran | **2026-07-09 14:03 UTC** (fresh) |
| Heartbeat / total events | 3,109 / 89,796 (~3.5%) — low because crawl sessions don't dwell |
| Noise ratio | Dominated by Googlebot opens + stale-tab `sync_failed` |

`logs.db` growth is the crawl + stale-tab storms. Storage/WAL strip shipped today (`61465cf8`) —
confirm it shows the 595 MB figure on `/admin/health`. Archive still empty: retention is pruning
within the hot window, not yet aging into archive (or archive path idle) — fine while oldest ≤14d.

---

## Host resources (past 24h)

**Plenty of headroom.** Host-stats cron shipped today (`6a7b3a6b`, first sample **06:26 UTC**) so
only **~100 samples** (~8h), not a full 288 — expected for day-one, not a telemetry gap.

- CPU avg **6.3%** · hottest 5-min window **27.7%** · `load1` max **1.7** (2-core box — fine)
- RAM avg **30.5%** · max **48.3%** · swap max **2.6%** (~52 MB)
- Disk **15.6%**

No action items from host resources.

---

## 5. Coverage gaps (Phase B)

Prioritized by "would this have answered a question we couldn't answer in A?".

| Priority | Path → event | Level | Fields | Why |
|---|---|---|---|---|
| ★ 1 | Error clusters / analytics — **bot share per cluster** (see §6; mostly a dashboard item, but emission already stamps UA) | — | `bot_pct` or `bot_sessions` on `ErrorCluster` | Today's #1/#2 clusters looked like P1 until a hand UA query. |
| 2 | `remote-log` / boot path — demote or tag **crawler boot-cascade** (`initial dict sync failed` / `live_query_failed` when `is_bot_user_agent`) | `info` or skip | keep `code`, add `audience:'bot'` | Stops Googlebot MISUSE from inflating real-error counts while SEO crawl is hot. |
| 3 | Stale-client recovery — after N persistent `schema_outdated` / `client_behind`, force **hard reload** or SW update prompt | `warn` | `dict_id`, `app_version`, `days_behind` | Greg + Zapotec editor are on day 6; SW is not self-healing. Carried from 07-08. |
| ✅ done | `/og` try/catch + text fallback + rename | — | — | Shipped `61465cf8` (verified). |
| ✅ done | `{"isTrusted":true}` → extract Event target / classify as noise | — | — | Shipped `61465cf8` (verified). |
| ✅ done | `sync_failed` 404 → `not_found` kind + halt | — | — | Shipped `61465cf8` (verified). |
| low | Guard remaining unguarded `vernacularName` call sites if any SSR 500 recurs | — | — | Optional chaining present in home/synopsis; watch server faults panel. |

**Already well-instrumented (no new logging needed):** `live_query_failed` (table/code/had_leader),
`initial dict sync failed` (dict_id/code/error), `sync_self_healed`, `leader_boot_failed`,
`host_stats`, uptime probes.

---

## 6. Dashboard improvements (Phase C + D)

### Phase C — concrete proposal (verified NOT present)

**★ Bot/crawler share on each error cluster** *(novel for LD; tutor filed the same shape 07-08).*

`build_error_clusters` intentionally keeps **all** rows ("a bot hitting a real error is a real
signal") and exposes `count` / `users` / `is_noise` — but **no bot share**. Today that meant:

- `live_query_failed` ranked as the #2 error with 1,378 rows / 294 sessions
- Hand query: **1,358 / 1,378 = 98.5% Nexus 5X Googlebot**

Add `bot_sessions` + `bot_pct` (UA `is_bot_user_agent` ∪ `webdriver` ∪ frequency-bot sessions) to
`ErrorCluster`, and sink or badge clusters with `bot_pct > 90%` as "mostly crawler". Cheap — same
GROUP BY with a conditional `SUM`. Complements (does not replace) the humans/bots audience toggle,
which already filters *usage* but not the diagnostic cluster list.

**Checked and deliberately NOT re-proposed** (already shipped, verified in code / backlog ✅):
build-adoption strip, storage/WAL strip, sync 404 `not_found` class, OG fallbacks, `isTrusted`
noise fold, Sync-Health panel, boot-health, uptime, CWV, bot exclusion on usage/geo.

**Reaffirmed open backlog (still the right next builds):**

1. Per-session error breadth + "⟳ loop" flag on `ErrorCluster`
2. Top-route attribution on `ErrorCluster`
3. Sync-Health: name server cause + RED one-user-dominates *(would have named Greg + Zapotec editor
   today without a hand query)*
4. Stale-bundle error-share % headline

### Phase D — sibling cross-pollination

**Ported INTO LD** (appended to `.issues/future/dashboard-improvements.md`):

- **★ Bot share % on error clusters** *(from tutor 07-08 Phase C — same finding, LD's SEO crawl makes
  it higher urgency here).* Tagged `ported from tutor`.
- **↺ Reaffirm Sync-Health one-user-dominates** *(from house; still live-grounded by Greg + Zapotec
  editor).* No new port needed — already on the backlog.
- **house "dirty-rows-wedged users" aggregate** — skipped for now (LD has Sync-Health stuck-pairs;
  no `dirty_rows_stuck` watchdog equivalent firing today). Revisit if we add a stuck-dirty signal.

**LD wins to flag BACK to siblings:**

- **Build-adoption / stale-client population strip** — shipped today (`61465cf8`); tutor already
  borrowed the idea as OTA-age; house should take the web form.
- **Storage & WAL live bytes strip** — shipped today; house 07-08 noted they still only have prose
  on their storage panel.
- **SEO crawlability just shipped** — siblings reading this: expect a Googlebot session spike and
  make sure error clusters can show bot share before the next sitemap push.

**Skipped as inapplicable:** tutor Mobile-health / memory-OOM; house `/admin/revenue`.

**House flag-back acknowledged:** audit LD junction merges for composite-UNIQUE vs `ON CONFLICT(id)`
(house `77c77f2`). LD's natural-key / server_seq work today (`71d18e34`, `175215a9`) overlaps this
space — treat as a follow-up code audit, not a dashboard item
(`.issues/sync-fk-wedge-server-seq-and-self-heal.md` already tracks the LD side).

---

## Recommendations / action items

- [ ] **Nudge Dr. Greg Anderson to hard-reload** (and the Zapotec editor if reachable) — 6-day-old
      tabs are still ~100% of the human `client_behind` storm. Speakable: "Greg's chat tab is six days
      old and can't get fixes until he reloads."
- [ ] **Backlog (Phase C, prioritize):** add **bot/crawler share %** to each error cluster on
      `/admin/health` so a Googlebot flood can't masquerade as a P1. Grounded in today's 98.5% bot
      `live_query_failed` cluster. → `.issues/future/dashboard-improvements.md`
- [ ] **Watch (do not fix yet):** single current-build `effect_update_depth` on proto-katwijks after
      IPA keyboard + Save — re-check next run; file an issue only if it recurs.
- [ ] **Optional Phase B:** demote or tag crawler boot-cascade rows (`initial dict sync failed` /
      `live_query_failed` when UA is bot) so SEO crawl weeks don't inflate real-error trends.
- [ ] **Carried:** stale-client forced-reload after N `schema_outdated` blocks
      (`.issues/stale-client-sync-storm-2026-07-08.md`).
- [ ] **FYI / already done:** Sugstun FK wedge ✅ `71d18e34`; OG + `isTrusted` + build-adoption +
      storage strip + sync 404 class ✅ `61465cf8`; host-stats ✅ `6a7b3a6b`.
- [ ] **Housekeeping:** `logs.db` at **~595 MB** (+128 MB/day under crawl) — storage strip should
      make this glanceable; consider VACUUM-after-prune if growth stays this steep after the crawl
      settles.

---

## Trend context (7d / 30d)

Daily raw rows · errors · (human-filtered errors where noted):

| Day | Rows | Errors (raw) | Notes |
|---|---:|---:|---|
| 07-09* | 79,329 | 5,688 | SEO crawl + stale tabs; human-ish errors ~1,723; ~521 after stripping stale-build sync |
| 07-08 | 30,142 | 1,867 | Post-fix drain day |
| 07-07 | 64,616 | 12,261 | Entry-page loop storm |
| 07-06 | 33,479 | 1,118 | |
| 07-05 | 19,498 | 212 | |
| 07-04 | 25,514 | 267 | |
| 07-03 | 32,822 | 312 | |
| 07-02 | 12,203 | 107 | |
| 07-01 | 4,979 | 4 | |

\* 07-09 partial through ~14:45 UTC. Raw error spike vs 07-08 is **bots + stale tabs**, not a live
regression on current builds.

<svg viewBox="0 0 420 160" width="100%" style={{maxWidth:'440px'}} role="img" aria-label="Daily raw error counts">
  <text x="0" y="14" fontSize="12" fill="currentColor" fontWeight="600">Raw errors / day (log scale-ish bars)</text>
  {[['02',107],['03',312],['04',267],['05',212],['06',1118],['07',12261],['08',1867],['09',5688]].map(([d,v],i) => {
    const h = Math.min(110, Math.sqrt(v) * 1.1)
    return <g key={d}><rect x={20+i*48} y={130-h} width="36" height={h} rx="3" fill={d==='09'?'#f59e0b':d==='07'?'#ef4444':'#6366f1'} fillOpacity="0.85" /><text x={38+i*48} y="148" textAnchor="middle" fontSize="10" fill="currentColor" fillOpacity="0.65">{d}</text></g>
  })}
  <text x="0" y="160" fontSize="9" fill="currentColor" fillOpacity="0.5">07 = entry-loop P1 · 09 = Googlebot + stale tabs (not a live regression)</text>
</svg>
