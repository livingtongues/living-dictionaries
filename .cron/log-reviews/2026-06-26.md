# Log review — 2026-06-26 (new.livingdictionaries.app · window: last 24h)

> **Second run.** First run (2026-06-25) was the zero-data baseline — pipeline alive but
> **0 rows ever ingested**. Today the pipeline has its **first real end-to-end data: 545 rows**, so
> this is the first run that actually triages traffic. Diff vs baseline noted throughout.

## TL;DR
- **🟢 Pipeline is PROVEN end-to-end.** 545 client+server rows landed today (01:05–05:46Z), up from
  literal zero. Every layer yesterday's report flagged as "defined but never emitted" is now **wired
  and firing**: the 4 core analytics events (`dictionary_opened` 21, `search_performed` 4,
  `entry_opened` 2, `dictionary_created` 10), **server-side `log_server_event`** (`auth_login` 14,
  `dictionary_created` 10, `dict_changes_pushed` 1), **geo** (CF location-headers transform is now
  ON — 520 located rows), and **`db_tier` capability** (`opfs-worker` ×44). Huge progress in one day.
- **⚠️ But ~91% of today's traffic is synthetic.** 40 of 44 sessions are **HeadlessChrome** e2e /
  verification runs (`/e2e-log-*`, `/wfix-*`, `/headful-*` dict slugs) — the ingestion smoke-tests
  from yesterday's action item. Only ~4 sessions are real Chrome. **No genuine end-user traffic yet**
  (all 5 `users` rows still `last_visit_at = NULL`; this is pre-cutover). Read every "usage" number
  below as "the test harness," not real engagement.
- **Top issue (🟠 P2):** 4 `Internal Error` **500 crashes** on `/[dict]/entries` with **empty stacks
  and no paired cause** — because the `+layout.ts` catch's `console.error` ran **server-side during
  SSR, where console.error isn't patched**, so the real error went only to ephemeral `docker logs`
  (already rotated away — unrecoverable). Even though these were synthetic, the *pattern* is a real
  observability hole: a real user hitting a broken dict load would be equally undiagnosable.
- **Top severity-quality issue (🟡):** `+error.svelte` logs **every** error page as `level:'crash'`,
  so an expected **401 "Missing session"** (anon hit `/admin/analytics`) is recorded as a *crash*.
  Status→level mapping needed (5xx=crash, 401/403=warn, 404=info) or our "Sentry" cries wolf.
- **Top dashboard issue (Phase C):** `is_bot_user_agent` (which **does** match `headless`) is applied
  **only to the capability panel** — the headline **usage/engagement/geo** numbers still count bots.
  Today that means the dashboard reports **~44 sessions/day when real humans ≈ 4**. Misleading exactly
  when it matters (pre-cutover, bot-dominated). → Phase C proposal.

## 1. Errors & crashes
11 error/crash rows / 545 total (2.0%), all `source='client'`, all `app_version` old bundles
(redeployed since). Clustered:

| Sev | Cluster | n | sessions | Root-cause hypothesis | Action |
|---|---|---|---|---|---|
| 🟠 **P2** | `Internal Error` (crash, status **500**) on `/e2e-log-mqu8bd7x/entries` | 4 | 4 | `+layout.ts` load threw → its catch's `console.error('dictionary layout load failed')` ran **server-side (SSR)** where console.error is **not** patched → real stack went to `docker logs` only (now rotated). Client just rendered `+error.svelte` → bare crash, empty stack. A missing dict can't be the cause (server layout 301s home), so this was a real load failure (likely OPFS/`open_dict` boot or a transient in the e2e bundle). | **Convert the `+layout.ts` catch to `log_server_event`** so the cause is captured when the load runs on the server. Without it, the next real dict-load failure is just as blind. (Phase B P1.) |
| 🟡 P3 | `[post_request] Network error for /api/log:` (error) | 5 | 1 | One admin tab on `/admin/analytics` left open **~37 min** (session span 2198s); transient network blips on the 5s flush. Self-referential (logging the logger's own POST failure) but caught & non-looping. | ⚪ Treat as noise — consider suppressing `/api/log`-target post_request errors from re-logging (avoid feedback). Low priority. |
| 🟡 | `Missing session` (**crash**, status **401**) on `/admin/analytics` | 1 | 1 | Anonymous visitor hit the admin route → expected 401 auth gate, **mis-recorded as `crash`** by `+error.svelte`'s blanket `level:'crash'`. | **Map error-page status→level** in `+error.svelte` (401/403→warn). Severity-quality fix. |
| 🟡 P3 | `Failed to initialize WebGL` (error, Mapbox GL v3.13) on `/` | 1 | 1 | Anonymous visitor's browser has WebGL disabled/unavailable → Mapbox globe can't init. 3rd-party, single anon session. | Add a graceful no-WebGL fallback for the homepage globe (static image / message) + consider filtering Mapbox-internal WebGL errors as ⚪ noise. |

**No** `unhandled_rejection` rows, no multi-user error clusters, no P1. Given ~91% synthetic traffic,
the real-world error picture is still essentially untested — re-evaluate once humans arrive.

## 2. Usage & engagement
**⚠️ Synthetic-dominated — 40/44 sessions are HeadlessChrome.** Numbers reflect the test harness.

| Metric | 24h | 7d | 30d | Note |
|---|---|---|---|---|
| Sessions (all) | 44 | 44 | 44 | ~4 real Chrome; rest HeadlessChrome e2e |
| Distinct `user_id` | 1 | 1 | 1 | most rows anon (`user_id` NULL) |
| Navigations | 114 | — | — | top dests all `/e2e-log-*/entries` |
| Analytics events | 51 | — | — | see below |

- **Top routes:** `/e2e-log-*/entries` (90+ across slugs), `/` (15), `/wfix-mquib59n/*` (7),
  `/headful-mqugua8o/entries` (2), `/admin/analytics` (1) — i.e. the test slugs + a couple of real
  homepage/admin visits.
- **Analytics events (all wired & firing — vs 0 yesterday):** `dictionary_opened` 21,
  `auth_login` 14, `dictionary_created` 10, `search_performed` 4, `entry_opened` 2. **`audio_played`
  defined but still 0 emitted** (no audio interaction in the synthetic flows — verify it's wired).
- **Session spans:** one 2198s outlier (admin tab idle), the rest 2–18s (headless e2e bursts) —
  median ≈ 10s, meaningless until humans arrive.
- **Platform/UA:** 100% `web`; 40 sessions HeadlessChrome, 4 real Chrome. 6 distinct `app_version`s in
  one day (rapid redeploys during the wiring sprint — expected).

## 3. Performance & geography
- **Perf (44 page-loads):** `page_load` p50 **1222ms** / p90 1768ms / p95 3229ms; `search` p50
  **13ms** / p95 33ms (fast local Orama). **TTFB** p50 **361ms** / p90 1193ms / p95 1276ms.
- **Geo:** **transform is ON now** (yesterday's action item ✅) — 520 located rows, but **all from one
  region: Malaysia (MY, region 14 / Kuala Lumpur)** — the dev/agent running the e2e suite. TTFB ≈
  361ms median from ~15,000km to the Boston origin **is** the distance tax in action, but with a
  single origin region the by-distance RUM split can't differentiate yet. Revisit under multi-region
  human traffic.
- **Capability:** `session_start.db_tier` = **`opfs-worker` ×44** (100% on the modern leader-worker
  OPFS tier); **0 below-capability** (no Safari <15.4) sessions. Clean.
- **Web vitals:** `track_web_vital` (message=`perf`, name=`web_vital`) = **0 rows** despite
  `init_web_vitals()` being called in `+layout.svelte` onMount. Likely HeadlessChrome short sessions
  never trigger LCP/CLS/INP flush before pagehide — **but unconfirmed**. → verify on one real session
  (Phase B).

## 4. Health & housekeeping
- **Volume:** 545 rows, all dated **2026-06-26** (day 1 of real data). Daily breakdown trivially:
  06-26 = 545 (11 errors); 06-25 and earlier = 0.
- **Composition:** info 534 / error 6 / crash 5. client 520 / server 25. Noise/heartbeat ratio
  healthy: heartbeat 75 + visibility 42 + session_start 44 ≈ 30% infra, the rest signal.
- **Retention:** ✅ `db_metadata.log_retention_ran_at = 2026-06-26T21:11:45Z` (ran today). Oldest row
  01:05Z today → ≤14d invariant trivially satisfied. **Archive (`logs-archive.db`) = 0 rows** (nothing
  old enough to cold-tier yet).
- **Containers:** `sveltekit_blue` (primary) + `sveltekit_green` (standby) + `caddy`, blue/green
  layout unchanged. Container DB path `/data/shared.db` confirmed again (prod-db.md still stale — see
  carry-over action item).

## 5. Coverage gaps (Phase B)
Re-prioritized given today's data. The big yesterday gaps (4 core events + server-side logging) are
**closed** ✅. Remaining:

| # | Path | Event / name | Level | Fields | Why |
|---|---|---|---|---|---|
| **P1** | `routes/[dictionaryId]/+layout.ts` catch (line ~182) | `log_server_event` (replace bare `console.error`) | error | `{ dict_id, phase:'dict_layout_load', error, stack }` | **Today's #1 blind spot.** The catch's `console.error` runs server-side during SSR where it's unpatched → cause lands only in ephemeral `docker logs`. The 4 `Internal Error` 500s have **no recoverable cause**. The next *real* dict-load failure would be equally undiagnosable. |
| **P1** | `routes/+error.svelte` (line 11–17) | status→level map | n/a | use `status>=500?'crash':status===404?'info':status>=400?'warn':'error'` | Stop logging expected **401/403 auth gates** (and 404s) as `crash`. Today 1 of 5 "crashes" was an expected 401. Keeps the crash rubric trustworthy. |
| **P2** | audio play button | `audio_played` | info (`track`) | `{ dict_id, entry_id, speaker_id? }` | The one core event **defined but still never emitted** — verify it's wired to the play handler (synthetic flows don't click audio, so absence is ambiguous). |
| **P2** | `init_web_vitals` path | `web_vital` (`perf`) | info | `{ name, value }` | 0 web-vital rows despite init. Confirm the dynamic `web-vitals` import + flush actually fire on a real (non-headless) session; if HeadlessChrome simply never flushes, that's fine — just document it. |
| **P3** | `/[dictionaryId]/entries` for broken/private dict | — | — | — | Confirm the intended UX: server layout 301s home for a *missing* slug, but a load *failure* 500s. Make sure private/permission-denied dicts return a clean 403, not a 500 crash. |
| **P3** | remaining `routes/api/*/+server.ts` failure paths | `log_server_event` | warn/error | `{ route, status, error }` | auth send-code/verify, sync `/changes` errors, uploads, email-inbound. Success paths log now; **failure** paths still mostly silent. |

## 6. Dashboard improvements (Phase C)
Yesterday's 3 proposals **all shipped** (`.issues/logging-buildout.md`): pipeline-health strip,
event-coverage panel, geo-transform hint — plus errors-by-version and a leader-worker health panel.
Strong page. New, concrete proposal from today's data:

1. **(prioritize) Apply bot/headless exclusion consistently across usage + engagement + geo, not just
   the capability panel.** `is_bot_user_agent` already matches `headless`, but the reader only uses it
   for `capability`; the headline insights (`sessions_per_day`, `logs_per_session`, the daily
   sessions/users chart, top events, geo latency) still **count bots**. **Spot-check drift:** today
   the "Sessions/day" insight reflects **44** while real human sessions ≈ **4** — a 10× overcount
   driven entirely by the e2e harness. Either filter bots everywhere by default (with a "include
   bots" toggle) or render a **Human / Bot** split on every usage panel. Most valuable *right now*
   because pre-cutover traffic is bot-dominated.
2. **(secondary) Real session-duration distribution** (median / p90 from heartbeat span) + **events
   per session**, replacing/augmenting the proxy "Logs / session" engagement metric — I had to compute
   the 2198s-vs-~10s spread by hand this run; the page should show it.
3. **(small) Web-vitals health hint** mirroring the geo hint: when sessions exist but `web_vital`
   rows = 0, show "web-vitals not landing — verify init / may be headless-only," so the empty Core
   Web Vitals panel reads as a known state, not a silent gap.

→ appended to `.issues/future/dashboard-improvements.md`.

## Recommendations / action items
- [ ] **(P1, Phase B) Route the `[dictionaryId]/+layout.ts` catch through `log_server_event`** instead
      of bare `console.error`, so SSR dict-load failures carry their real stack into `client_logs`
      (the 4 `Internal Error` 500s today were uncloseable for exactly this reason).
- [ ] **(P1, Phase B) Map error-page status→level in `+error.svelte`** so 401/403/404 stop being
      logged as `crash`. Keeps the crash rubric honest.
- [ ] **(Phase C, prioritize) Exclude bots/headless from usage+engagement+geo dashboard panels**
      (or add a Human/Bot split) — currently only the capability panel excludes them; headline
      sessions/day is ~10× inflated by the e2e harness today.
- [ ] **Verify `audio_played` is wired** and that **web vitals** fire on a real (non-headless) session.
- [ ] **Add a graceful no-WebGL fallback** for the homepage Mapbox globe + filter Mapbox-internal
      WebGL init errors as noise.
- [ ] **Carry-over: fix stale `prod-db.md`** (still says container `sveltekit` + DB
      `/workspace/site/.data/shared.db`; live is `sveltekit_blue`/`_green` + `/data/shared.db`).
- [ ] **Re-run after the production cutover / first genuine human traffic** — that's when these usage,
      perf, geo, and web-vital panels finally measure reality instead of the test harness.

## Trend context (7d / 30d)
First non-zero day. Sparkline data (logs · errors per day):

```
2026-06-25  ▁  0 logs   0 err   (baseline: pipeline alive, no traffic)
2026-06-26  █  545 logs 11 err  (first real data; ~91% synthetic/headless)
```

No multi-day trend exists yet. This run establishes the first real data point; the next run with
human (post-cutover) traffic sets the first meaningful trend — and is when the bot-exclusion Phase C
fix stops being cosmetic and starts being load-bearing.
