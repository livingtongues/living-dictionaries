# Log review — 2026-06-25 (new.livingdictionaries.app · window: last 24h)

> **First run of this command.** No prior reports to diff against. Establishes the baseline.

## TL;DR
- **Pipeline is healthy but has ingested ZERO telemetry — ever.** `client_logs` (in both
  `shared.db` and `logs-archive.db`) and `log_daily_metrics` are all completely empty across all
  time (`MIN(received_at)` is `NULL`). This is **expected**, not a bug: the entire logging+analytics
  beef-up only shipped today (commits `b7cf48d5`→`5197ff9d` at ~07:13, last migration applied 14:02),
  and `new.livingdictionaries.app` has essentially no real traffic pre-cutover — all 4 seed users
  have `last_visit_at = NULL` and the dictionary catalog isn't imported yet (`dictionaries = 0`).
- **Infra is verified alive:** app serves `200` (TTFB ~1.0s), retention cron ran today
  (`db_metadata.log_retention_ran_at = 2026-06-25T20:03:04Z`), today's migrations applied, and all
  **33 logging unit tests pass** (`insert-client-log`, `/api/log`, retention-cron, log-analytics).
  So zero-rows = no traffic, **not** a broken write path.
- **⚠️ But we have NO positive end-to-end proof a single real row has ever landed.** The only
  server-side liveness signal is the retention cron writing a metadata flag. Top action item:
  do one controlled end-to-end ingestion check before we trust this as our "Sentry."
- **Top coverage opportunity (Phase B):** the 4 core analytics events (`search_performed`,
  `dictionary_opened`, `entry_opened`, `audio_played`) are **defined in `log-events.ts` but emitted
  nowhere in the app** — and **0 of 48 `/api/*` routes call `log_server_event`**. Even once traffic
  lands, the dashboard's usage + server-error panels would stay empty by construction.
- **Top dashboard opportunity (Phase C):** add a **pipeline-health / ingestion-liveness strip** so an
  operator can instantly tell "pipeline broken" from "no traffic yet" — exactly the ambiguity that
  cost this review its first 20 minutes.

## 1. Errors & crashes
**None — zero error/warn/info/crash rows exist.** Nothing to cluster or triage.

The triage machinery itself is sound (verified by `log-analytics.test.ts` + `log-retention-cron.test.ts`,
33/33 green), so the moment real errors arrive they'll cluster + surface correctly. Severity rubric
carries forward unchanged for next run.

## 2. Usage & engagement
**No sessions, users, navigations, or analytics events recorded** in any window (24h / 7d / 30d).

| Metric | 24h | 7d | 30d |
|---|---|---|---|
| Sessions | 0 | 0 | 0 |
| Distinct users | 0 | 0 | 0 |
| Navigations | 0 | 0 | 0 |
| Analytics events | 0 | 0 | 0 |

Corroborating signals that this is genuinely "no traffic," not a dropped pipe:
- All 4 `users` rows (`jwrunner7@gmail.com`, `diego@`, `dictionaries@`, `livingtongues@gmail.com`)
  have `last_visit_at = NULL`.
- `dictionaries = 0`, `message_threads = 0` — catalog/data not yet migrated onto the new VPS.

## 3. Performance & geography
- **Perf:** no `perf` rows → no page_load / search percentiles, no web vitals. Nothing to trend.
- **Geo:** no located sessions. **Dependency to confirm before geo is useful:** the dashboard's own
  copy notes that region/city/coordinates (and the distance-to-Boston TTFB split) require the
  Cloudflare **"Add visitor location headers"** managed transform to be enabled in front of
  `new.livingdictionaries.app`. Without it, `latitude/longitude` stay NULL and the RUM
  distance-split panel won't populate even under load. → action item.
- **Capability:** no `session_start` rows → no `db_tier` distribution (opfs-worker / idb-worker /
  idb-main) and no below-capability (Safari < 15.4) count yet.

## 4. Health & housekeeping
- **Volume:** 0 rows. DB main file `shared.db` is one 4 KB page with data in a 708 KB WAL (WAL reads
  verified — migrations + users come back correctly through `{ readonly: true }`).
- **Retention:** ✅ healthy — `log_retention_ran_at = 2026-06-25T20:03:04Z` (today). Oldest row N/A
  (no rows), so the ≤14d invariant is trivially satisfied.
- **Archive:** `/opt/hosting/data/logs-archive.db` exists with a `client_logs` table (also empty) —
  the cold-tier target is provisioned and ready.
- **Noise:** N/A (no rows). The `NOISE_MESSAGE_PATTERNS` filter (`ResizeObserver loop`, `[GSI_LOGGER]`,
  `Sync already in progress`) is in place for when traffic starts.
- **Containers:** blue/green both `Up 7 hours` on `sveltekit-app:current`; `caddy` up 43h.
- **GOTCHA for next runner:** container name is **`sveltekit_blue`** (+ `sveltekit_green`), NOT
  `sveltekit`, and the in-container DB path is **`/data/shared.db`** (matches log-and-fix.md, not the
  `/workspace/site/.data/...` path in prod-db.md — *that doc is stale for this container layout*).

## 5. Coverage gaps (Phase B)
Prioritized by "would this have answered a question we couldn't answer above / will it answer one the
day traffic lands?"

| # | Path | Event / name | Level | Fields | Why |
|---|---|---|---|---|---|
| **P1** | `routes/[dictionaryId]/+page` (entries list load) & globe → dict open | `dictionary_opened` | info (`track`) | `{ dict_id, public }` | Constant is **defined but emitted nowhere**. Core "which dictionaries get used" signal — the whole point of the GA half. |
| **P1** | entry detail view | `entry_opened` | info (`track`) | `{ dict_id, entry_id }` | Defined, never emitted. Primary engagement-depth metric. |
| **P1** | search box submit | `search_performed` | info (`track`) + `track_timing({ name:'search' })` | `{ dict_id, query_len, result_count }` | Defined, never emitted; also feeds the perf `search` panel which is otherwise empty. |
| **P1** | audio play button | `audio_played` | info (`track`) | `{ dict_id, entry_id, speaker_id? }` | Defined, never emitted. Media-engagement signal. |
| **P1** | every `routes/api/*/+server.ts` failure path (48 routes, **0** currently log) | `log_server_event` | warn/error | `{ route, status, error }` | Start with **auth** (`email/verify`, `email/send-code`, `google`), **sync** (`dictionary/[id]/changes`), **uploads** (`/upload`, `gcs_serving_url`), **email-inbound**. Today these failures only hit ephemeral `docker logs` — invisible to this review. |
| **P2** | `dictionaries/create` + `dictionaries/[id]/roles` mutations | `log_server_event` | info/error | `{ dict_id, actor_user_id, action }` | Audit-trail for admin-impacting writes; helps reconstruct "who changed what" without DB forensics. |
| **P2** | R2 snapshot builder cron + sync push/pull failures | `log_server_event` | error | `{ dict_id, phase, error }` | Background jobs are silent today; a stalled snapshot builder would be invisible until an editor complains. |
| **P3** | page-load instrumentation (`track_web_vital` / page_load perf) | `perf` `page_load` | info | `{ ttfb, duration_ms }` | Confirm it actually fires on the real app — needed for the geo TTFB-by-distance RUM panel to ever populate. |

Emission convention reminder for whoever builds these: `track({ event, props })` for analytics,
`log_event({ level, message, context })` for handled client warnings, `track_timing({ name,
duration_ms })` for slow ops, `log_server_event({ level, message, error?, context })` server-side.

## 6. Dashboard improvements (Phase C)
The reader (`log-analytics.ts`, 367 lines) and page (`+page.svelte`, 523 lines) are genuinely strong —
hot+cold per-day merge, capability/bot split, geo TTFB-by-distance, good per-panel empty states
("No … yet"). **It is NOT "fine," though** — with everything at zero it can't tell an operator
*why*. Concrete proposals (logged to the deduped backlog):

1. **(prioritize) Pipeline-health / ingestion-liveness strip** at the top: `last log received_at`
   (absolute + "Xh ago"), `last session_start`, `retention last ran` (from
   `db_metadata.log_retention_ran_at`), archive row count. Turns the ambiguous wall-of-zeros into a
   definitive **"pipeline OK, awaiting traffic"** vs **"NO ingestion in 24h — investigate"** verdict.
2. **Self-instrumentation coverage row:** list the `log-events.ts` vocab and flag each as *seen* vs
   *never emitted*. Would have surfaced today's "4 core events defined, 0 wired up" instantly.
3. **Geo-transform health hint:** when `located_sessions > 0` but all `latitude` are NULL, show an
   explicit "CF visitor-location-headers transform appears OFF" warning instead of the generic
   "No coordinates yet."

→ all appended to `.issues/future/dashboard-improvements.md`.

## Recommendations / action items
- [ ] **Verify end-to-end ingestion once.** Open `new.livingdictionaries.app` in a real browser (or
      send one controlled `POST /api/log` with `build_target:'development'` so retention/filters treat
      it as noise), then confirm exactly one row lands in `shared.db.client_logs`. We currently have
      **no** positive proof the write path has ever succeeded in prod.
- [ ] **Wire the 4 core analytics events** (`dictionary_opened`, `entry_opened`, `search_performed`,
      `audio_played`) into their call sites — see Phase B P1 rows. (Separate `.issues/` file worth
      creating when picked up.)
- [ ] **Add `log_server_event` to API failure paths,** starting with auth + sync + uploads (Phase B).
- [ ] **Confirm the Cloudflare "Add visitor location headers" managed transform is enabled** for
      `new.livingdictionaries.app`, else geo coordinates / TTFB-by-distance never populate.
- [ ] **Fix stale doc:** `prod-db.md` lists the container DB path as `/workspace/site/.data/shared.db`
      and container name `sveltekit`; the live containers are `sveltekit_blue`/`_green` with the DB at
      `/data/shared.db`. Update prod-db.md (medium — flag to Jacob, not auto-fixed: this command is
      read-only).
- [ ] **Re-run after first real traffic** (or post-cutover) — that's when this command earns its keep.

## Trend context (7d / 30d)
Daily counts, all metrics, all sources, last 30 days: **uniformly 0.** No sparkline to draw. This
report is the zero baseline; the next run with non-zero data sets the first real trend.
