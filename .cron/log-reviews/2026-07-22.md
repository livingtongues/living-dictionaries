# Log review — 2026-07-22 (livingdictionaries.app · window: last 24h)

*Read-only production review of `logs.db` + `shared.db`, ending **14:20 UTC**. No application code changed. Current build: **`1784714151639`** (deployed 09:55 UTC). Every recommendation was verified against current source (`classify-error.ts`, `log-analytics.ts`, the upload emitters, `db-client.ts`), recent commits, standing decisions, and the house/tutor July 21 reviews.*

## TL;DR

- 🟢 **Backlog-clearing day, not an incident day.** Commit **`d6871c60`** (06:08 UTC) shipped **three of yesterday's four carried action items in one stroke** — the `real_errors` null-session zombie exclusion, the cross-browser stale-bundle noise strings (`Importing a module script failed.` / `Unable to preload CSS`), and the OPFS dict-boot recovery hardening. Import/media upload-failure enrichment (`import_upload_failed` / `media_upload_failed` with kind/status/stage/bytes/online/mimetype) also landed. All are live in the current build.
- 🟠 **The OPFS recovery mechanism bounds the loop but has not yet healed anyone: 7 `dict_boot_recovery_exhausted` / 0 `dict_boot_recovered` in 24h.** One of the three was a **signed-in iPhone user (`alclaveria`) on the current build** hitting a terminal `sqlite3_open_v2` failure on `boienen` — the reset-from-snapshot correctly refuses (can't prove dirty-state on a file that won't open), so the user stayed locked out of that one dictionary. The other two were anon/webdriver stale-bundle boots where reset legitimately can't help. This is the still-open iOS/Android verification checkbox in `.issues/dict-boot-persistent-opfs-recovery.md`.
- Human activity was healthy beneath the ongoing Googlebot crawl: **759 human sessions / 38 signed-in users**, **135 entries created by 10 people**, 1,870 searches, 664 audio plays. Raw totals (20,018 sessions) are crawler-dominated — never read as audience.
- Genuine (de-noised) errors held at the **~30–50/day baseline** — the only recent excursion was the now-fixed 07-20 structured-clone spike (543). The shipped `real_errors` fix should collapse the inflated headline metric on the next finalized rollup.
- Infrastructure green: CPU **3.8% avg / 48.9% hottest 5-min** (today's 6-build deploy churn), RAM **29.3% / 47.9% max**, swap **77 MB**, disk **18.3%**, 282/288 host samples, retention ran 09:58 UTC.
- **Phase C/D clean no-op** — the two top backlog items just shipped; the last ready item is the LOW `admin_analytics_computed` compute-cost trend. Siblings shipped no fresh portable win. Two LD wins to flag *back* to house/tutor (below).

## 1. Errors & crashes

| Severity | Cluster | Reach (24h) | Verdict |
|---|---|---:|---|
| 🟠 P2/watch | `dict_boot_recovery_exhausted` | 7 rows / 3 sessions / 1 signed-in | New telemetry (shipped today) working: it caps the re-election loop. But **0 recovered** — signed-in iOS `alclaveria` on `boienen` (`sqlite3_open_v2`, current build) stayed locked out; 2 anon/webdriver were stale-bundle boots. Recovery mechanism is not yet healing these modes. |
| ⚪ known noise | `sync_failed` | 1,450 rows / **0 sessions** (all null) / 2 users | The forgotten-laptop zombie storm (Greg ≈1,451). Standing 07-09/07-11/07-14 ruling; no forced reload. **Now folded out of `real_errors`** by today's fix. |
| ⚪ known noise | `leader_boot_failed` | 437 error rows | Deploy-tail + bot boot-cascade. Diego's stale build `1784598014399` tab looped 393× (deploy churn); current-build rows (36 across 29 sessions) are thin anon spread. Standing ruling. |
| ⚪ crawler noise | `Rejected` (serviceWorker.register) | ~200 rows across ~30 dict entry URLs / 0 users | Anonymous bot prerenderers. Standing ruling. |
| ⚪ crawler noise | `Internal Error` | 45 rows / 43 sessions / 0 users | Nexus 5X Googlebot entry-page 500s. Standing crawler ruling. |
| ⚪ stale-bundle noise | `Importing a module script failed.` · `Unable to preload CSS` · `Failed to fetch dynamically imported module` | ~26 rows / anon | Cross-browser deploy-tail from today's 6 builds. **Now in `KNOWN_NOISE_PATTERNS`** (shipped today). |
| ⚪ opaque noise | `Script error.` | 57 rows / 2 users / stale build `1784607195834` | Contentless cross-origin script error (no stack/detail), on a *retired* build, both signed-in browsers. Not actionable — classic opaque third-party/extension artifact. |
| ⚪ transient | Jacob's `/api/admin/analytics` + `/api/translate` network errors | 18 rows / 1 session / **1.4 s window** | All four endpoints failed inside 06:16:49–06:16:50 — one admin's brief connectivity drop, not a server fault. |
| ⚪ media noise | interrupted play / unsuitable media | ~13 rows / anon | Isolated playback promise/resource errors; playback via `<audio>` unaffected. |

### The recovery-efficacy finding (the one real signal)

The OPFS hardening (`d6871c60`) did exactly what was designed for the **loop-bounding** half: `dict_boot_recovery_exhausted` now fires once per exhausted session with `last_stage` / `boot_message` / `attempt` instead of an unbounded re-election log spray. But the **healing** half hasn't demonstrated a single success yet (0 `dict_boot_recovered`):

- `boienen-old-buhi-langua` — signed-in `alclaveria@gmail.com`, iPhone iOS 26, **current build**, `sqlite3_open_v2` at `opfs_open`, 4 repeats then terminal. The file won't open, so dirty-state can't be proven, so reset-from-snapshot refuses (correct per the safety invariant) → user locked out of that dictionary. Note this user *did* create 5 entries in other sessions/dictionaries, so they are an active editor partly blocked.
- `kalinago` (anon, `webdriver:true`) and `ngabere` (anon) — both `Failed to fetch dynamically imported module` / `Importing a module script failed.` at `init`/`opfs_open`. These are stale-bundle worker-chunk fetch failures during the 6-build churn; reset cannot fix a missing code chunk, and correctly it doesn't try.

So the mechanism is behaving correctly at the margins but leaves the genuine iOS-Safari `sqlite3_open_v2` case unrecovered. This is the open verification checkbox, not a new bug.

## 2. Usage & engagement

| Window | Human sessions | Signed-in users | Median span | p90 span |
|---|---:|---:|---:|---:|
| 24 hours | 759 | 38 | 8 s | 34 min |
| 7 days | 4,710 | 87 | — | — |
| 30 days | 41,892 | 128 | — | — |

Editing signals were concrete and healthy: **135 entries created by 10 people**, 1 deletion, no `dirty_rows_stuck` / `write_blocked` / terminal sync-halt for a real editor. **Searches 1,870** (15 signed-in users, p50 12 ms), **audio 664**, **dictionary opens 660**, **entry opens 405**. New entries landed in iipay-aa (68), jejueo (23), appalachian (21), poggeresepl (9), boienen (5), ayook (4), and five more. Top human nav destinations: `/iipay-aa/entries` (128), `/` (86), `/tutelo-saponi/entries` (39), `/dictionaries` (25), `/jejueo/entries` (24), `/translate` (21), `/chat` (15), `/admin` (12).

### Who's doing what

- **Jacob Bowdoin** — a development/deploy day: active 00:21–09:59 UTC across **35 sessions** and shipped **6 builds** (06:20→09:55 UTC), which is the source of today's stale-bundle transient wave. Exercised `/admin`, `/admin/users`, `/admin/messages` (incl. two threads + resolved), `/admin/dictionaries`, `/translate` (21 navs — the day's translation/i18n commits: zh-CN/zh-TW rename, English catalog fixes, progress visibility), and import flows for wenshanhua/iipay-aa/achi/opyt321 plus iipay-aa/settings. His 18 "errors" were a single 1.4-second network blip at 06:16.
- **Diego Córdova** — active 18:17–23:32 UTC (07-21). One stale tab on retired build `1784598014399` fell into a 62-minute worker-chunk boot loop (393 `leader_boot_failed`); deploy-tail noise, not a standing break.
- **Dr. Greg Anderson** — real editing beneath the zombie storm: **2 entries created**, 3 entry opens, 13 navigations across the day. His 1,451 null-session `sync_failed` rows are the ruled-known forgotten-laptop tab and are excluded from his real activity.
- **Cailie Keating** — light: active intermittently 15:05–12:34 UTC, browsing dictionaries and admin surfaces, 1 error. No product fault.
- **Everyone else (anonymous aggregate)** — 34 additional signed-in users and ~700 anonymous human sessions. The standout contributor created **68 entries** in iipay-aa across 10 sessions; a second created 23 in jejueo (Korea); public browsing centered on US-California, England, Kuala Lumpur, and Texas. No non-admin individual is named.

## 3. Performance & geography

| Metric | 24h | Read |
|---|---|---|
| Page load p50 / p90 / p95 | 819 ms / 4.04 s / 6.09 s (n=579) | Tail unchanged from yesterday (722 ms / 6.83 s); device/geo mix + crawler history. |
| Search p50 / p90 / p95 | 12 ms / 153 ms / 247 ms (n=1,869) | Healthy, sub-300 ms — the filter crash is gone and it stayed gone. |
| Dict boot p50 / p90 / p95 | 1.06 s / 4.99 s / 7.03 s (n=460) | Cold-boot median ~1 s; tail rides slow snapshots + deploy-churn stale chunks. |

**Capability:** 772 `opfs-worker` vs **1** `idb-worker` at `session_start` (human) — essentially everyone is on the modern leader-worker tier. **Geo (human sessions):** US-SC 122, US-CA 82, GB 37, MY 35, US-TX 21, US-OR 16, US-AZ 15. *US-SC is residual crawler leakage* — its raw traffic is 18,882 Nexus 5X Googlebot sessions and the ~122 "human" survivors are desktop-Linux/datacenter UAs my rough filter doesn't catch; the production dashboard's `is_bot_ua`+webdriver filter is stricter, so don't read SC as real audience. **TTFB-by-distance** (Boston origin): 500–2,000 km 7 ms, 2,000–6,000 km 292 ms, 6,000 km+ 779 ms — the expected mild distance tax; the dashboard already computes this, no new panel needed.

## 4. Health & housekeeping

- `logs.db`: **2.33 GB**, 1,333,331 hot rows; oldest **2026-07-08 09:58 UTC** (clean 14-day boundary), newest **14:17 UTC**.
- Archive: **356 MB**; `shared.db`: **80.5 MB**.
- Retention last ran **2026-07-22 09:58 UTC**, rollups finalized through July 21. Working as designed.
- 142,765 rows in the rolling 24 h; heartbeats only 4,817 (low ratio — bots don't stick around to beat). Volume remains crawler-driven.
- **The crawl continues** at 120–172k rows/day since July 19 (roughly flat, not growing catastrophically). It is benign Googlebot-mobile (Nexus 5X) SEO indexing of entry pages — CPU cost is negligible (3.8% avg). `logs.db` grows ~230 MB/day at this rate but sits comfortably on an 18% disk. Per the 07-13 baseline this is the *same* re-intensified crawl, not a new one; keep watching `logs.db` size, no action.

## Host resources (past 24h)

Plenty of headroom. **282 samples** (six short of 288). CPU **3.8% average / 48.9% hottest 5-min window**, load1 max **2.69** on a two-core host — the elevated hottest-window CPU, RAM max (47.9%), and brief load spike all coincide with today's six Docker deploy builds, not steady-state load. RAM **29.3% average**, swap peaked 5% and ended at **77 MB**; disk **18.3%**. No resource action warranted.

## 5. Coverage gaps (Phase B)

Yesterday's two Phase-B items **both shipped today**, so there is no new logging gap to open:

1. ✅ **Import/media upload-failure enrichment — SHIPPED.** `upload-media.ts` emits `media_upload_failed` with `{dictionary_id, kind, stage, status, bytes, mimetype, online, error_message}`; `upload-import-file.ts` emits `import_upload_failed` tracking `failure_kind` (`http_status`/`network`/`api_error`) + `status` + `stage`. The next failed upload will be diagnosable.
2. ✅ **Persistent dictionary recovery outcome — SHIPPED (telemetry).** `db-client.ts` now emits `dict_boot_recovery_exhausted` / `dict_boot_recovered` with `{dict_id, last_stage, boot_message, attempt}`. The gap that *remains* is not telemetry — it is **efficacy**: 0 recovered / 7 exhausted shows the healing path isn't yet succeeding on iOS `sqlite3_open_v2`. That belongs in the fix queue (issue open), not a new event.
3. **No broad new event inventory justified.** Entry create/delete, dirty-row watchdogs, write refusal, sync failure kind/status, snapshot/boot stages, import register/update/request, server API failures, retention, snapshots, and host resources all carry structured telemetry (verified).

## 6. Dashboard improvements (Phase C + D)

**Phase C — honest clean no-op.** The two highest-value ready items just shipped in `d6871c60`:
- ✅ **`real_errors` null-session zombie exclusion** — `log-analytics.ts:1218` now carries `AND NOT (session_id IS NULL AND message IN ('sync_failed','leader_boot_failed'))`. The de-noised trend confirms the payoff: genuine errors are ~30–50/day, not the 1,500–3,800/day raw headline.
- ✅ **Cross-browser stale-bundle strings** in `KNOWN_NOISE_PATTERNS` (`classify-error.ts:24-26`).

The current page already renders pipeline liveness, current-vs-stale/deploy-tail error splits, bot-share and loop breadth, host/storage health, sync health, dict-boot cold/warm timings, route performance, Core Web Vitals, and thin-sample guards. No new panel is justified by today's data. The last ready backlog item is the **LOW `admin_analytics_computed`** compute-cost trend (still not in source — verified). Today's 0-recovered/7-exhausted OPFS signal stays an **actionable digest item**, not a panel, per Jacob's 07-14 "no wedged-client dashboard" ruling. Analytics/health load performance shows no regression today (only ephemeral `[profile]` timing exists — the reason `admin_analytics_computed` is worth building when priority allows).

**Phase D — no new inbound port.** House's July 21 top item remains *new-vs-returning readership*; LD already has true monthly-visitor rollups + dictionary viewership primitives, and this was declined 07-21 — no hurried port. Tutor's items are RN-specific (learner cohorts, native memory) or data-gated (`sync_error` panel). Both siblings already share LD's malformed-context guards and scoped loading; neither shipped a fresh portable win.

**LD wins to flag *back* to the siblings** (their reviews read this file):
- **Null-session zombie exclusion from `real_errors`** is now live in LD (`d6871c60`). House/tutor report they don't exhibit a zombie-sync population, so it may not port — but the *pattern* (fold single-tab null-session storms out of the real-error headline) is worth their awareness.
- **Bounded OPFS dict-boot recovery telemetry** (`dict_boot_recovery_exhausted`/`dict_boot_recovered` + migration-resumability hardening). **House shares LD's leader-worker OPFS harness** (per house `$lib/db/worker/PARITY.md`) — this is a genuine, directly portable borrow for house's next review.

## Recommendations / action items

- [ ] 🟠 **Continue the OPFS recovery work toward actual healing** (`.issues/dict-boot-persistent-opfs-recovery.md`, still-open iOS/Android checkbox): the signed-in iOS `sqlite3_open_v2` case (`alclaveria`/`boienen`) exhausted without recovering because reset refuses on a file it can't open to prove clean. Decide a policy for the un-openable-file case (e.g. a clean unopenable file with no prior local writes for that dict is safe to replace from snapshot). 0 recovered / 7 exhausted is the metric to move.
- [ ] **Next run: confirm the `real_errors` headline dropped** now that the null-session exclusion (shipped 06:08 UTC) is applied to finalized rollups — expect ~30–50/day instead of ~1,600–2,000/day.
- [ ] **Build `admin_analytics_computed` next** (LOW, last ready backlog item) — one server event per uncached whole-window compute so the standing dashboard load-perf watch becomes a real trend instead of ephemeral `[profile]` logs.

## Trend context (7d / 30d)

De-noised errors sit at a flat ~30–50/day baseline; the only excursion was the now-fixed 07-20 structured-clone P1 (543). Raw error counts (1,500–3,800/day) are crawler/zombie inflation — exactly what today's `real_errors` fix removes.

<svg viewBox="0 0 500 175" width="100%" style={{maxWidth:'540px'}} role="img" aria-label="Human sessions and de-noised errors, July 15 through July 22">{[['07-15',393,31],['07-16',945,44],['07-17',536,43],['07-18',484,35],['07-19',507,46],['07-20',854,543],['07-21',830,53],['07-22*',362,37]].map(([day,sess,errs],i)=><g key={day}><rect x={26+i*58} y={140-Number(sess)/945*100} width="20" height={Number(sess)/945*100} rx="2" fill="#6366f1"><title>{sess} human sessions</title></rect><rect x={46+i*58} y={140-Number(errs)/543*100} width="20" height={Number(errs)/543*100} rx="2" fill="#f59e0b"><title>{errs} de-noised errors</title></rect><text x={46+i*58} y="154" textAnchor="middle" fontSize="8" fill="currentColor" fillOpacity="0.65">{day}</text></g>)}<line x1="18" y1="140" x2="490" y2="140" stroke="currentColor" strokeOpacity="0.25" /><text x="20" y="13" fontSize="9" fill="#6366f1">■ human sessions</text><text x="150" y="13" fontSize="9" fill="#f59e0b">■ de-noised errors (07-20 = fixed P1)</text></svg>

| Day | Human sess | De-noised errs | Raw errs | Raw rows |
|---|---:|---:|---:|---:|
| Jul 15 | 393 | 31 | 822 | 12,115 |
| Jul 16 | 945 | 44 | 1,491 | 24,484 |
| Jul 17 | 536 | 43 | 1,674 | 30,840 |
| Jul 18 | 484 | 35 | 1,512 | 43,293 |
| Jul 19 | 507 | 46 | 2,690 | 124,564 |
| Jul 20 | 854 | **543** | 3,503 | 172,020 |
| Jul 21 | 830 | 53 | 3,792 | 151,010 |
| Jul 22* | 362 | 37 | 1,671 | 85,720 |

*July 22 is partial (query at 14:20 UTC). Rolling human sessions: **759 / 4,710 / 41,892** for 24h / 7d / 30d.*
