# Log review — 2026-07-13 (livingdictionaries.app · window: last 24h)

*Read-only pass over the `living` VPS `logs.db` + `shared.db` rollups. No code changed. Every
recommendation re-checked against current source. Current build: `1783932811749`.*

## TL;DR

- **Very healthy day. No P1, no P2, zero blocking errors for real users.** The most notable thing is
  a *good* one: **the SEO/bot crawl has settled.** 24h volume collapsed to **33k rows** (from 184k
  yesterday — a −82% drop) and the box went quiet: CPU avg **1.9%**, hottest 5-min **10.7%**, RAM max
  **42.9%**.
- **Real engagement is strong and climbing — 41 signed-in users / 144 sessions in 24h** (vs 17 users
  yesterday), **96 over 7d.** A genuine contributor-driven day: 82 entries created (12 users), 690
  audio plays (1 heavy reviewer), 1,338 searches (15 users).
- **One live contributor (`evelyn…`) is stuck on a stale tab** — 288 `sync_failed`
  (`kind:client_behind` / `schema_outdated` / 409) on dict **solari**, on an **old build**
  (`1783245164404`). This is the known-noise stale-build family (2026-07-09 ruling) — a migration
  shipped and her old tab can't sync until she reloads. **Already visible** to admins on the shipped
  `/admin/health` Sync-health panel; no new action.
- **All error clusters are known-noise / bot families** — `sync_failed` zombie tabs (Greg + anon +
  evelyn), `Rejected` serviceWorker (98, all anon/bot), `initial dict sync failed` (29, bot boot
  cascade), and **12 `Internal Error` 500s that are ALL bots** (Applebot/Googlebot). No server-side
  crash rows at all (`source='server'` level crash/error = **0**).
- **Host + retention green.** Retention ran **15:24 UTC** (fresh); oldest row 2026-06-29 (14d, at the
  hot-window edge). `logs.db` = **1.54 GB** (growth slowed to +66 MB/day as the crawl tapered — was
  +300 MB/day). Disk 16.1%.

---

## 1. Errors & crashes

Clusters over 24h (grouped by message + stack head). Current build `1783932811749`. `sync_failed` is
`warn` — it lands in the by-user tallies (§2), shown here for context.

| Sev | Cluster | n (24h) | users | sess | Verdict |
|---|---|---:|---:|---:|---|
| ⚪ noise | `sync_failed` (stale tabs) | 1,439+ | 3 | — | Greg null-session zombie (2,107) + anon null-null (1,355) + **evelyn live-tab `schema_outdated`** (288). Known-noise stale-build family (07-09). |
| ⚪ noise | `Rejected` (serviceWorker.register) | 98 | 0 | 98 | 1 row/session, all anon/bot prerenderers — 3rd-party. |
| ⚪ noise | `initial dict sync failed` | 29 | 4 | 29 | Boot-cascade / crawler family — known-noise (07-09/10). |
| 🟡 note | `Internal Error` (500, no stack) | 12 | 0 | 11 | **All bots** (Applebot ×2 on `/`, Googlebot on an entry). No matching server crash row → infra/proxy 500 during a blue-green swap, not SSR. Carried coverage note §5. |
| ⚪ noise | `Failed to read dict bundle from wa-sqlite` | 9 | 2 | 9 | `MISUSE` self-heal — known-noise (07-09). |
| ⚪ noise | `Script error.` / SW-register 404 / play()-interrupted | 5 / 7 / 6 | ≤2 | few | Cross-origin + stale-chunk SW fetch + benign audio pause races. |
| ⚪ noise | `r["@context"].toLowerCase` TypeError | 2 | 1 | 2 | Safari, inline `/:3` `global code` — a JSON-LD-reading **browser extension**, not our code. 3rd-party. |
| 🟡 P3 | `[post_request] Network error /pesh/partners` (+ `werikyana/catalog`) | 3 + 1 | 1 | 1 | Real contributor editing Pech partners — transient network on a POST, `repeat_count:3`, single session. Not systemic. |
| ⚪ noise | dynamic-import 404 / `leader_boot_failed` / `orama delta scan` / `importScripts export` | 1 each | ≤1 | ≤1 | Stale-build swap + bot-boot family — all single rows, known-noise. |

**Yesterday's 🟡 P3 search `RangeError`** (`Maximum call stack size exceeded` on `/opata/entries`):
**0 recurrences in 24h.** Stays on watch per the 2026-07-12 decision — no action.

### The one live-user case worth a sentence — `evelyn…` stuck on `solari`

288 `sync_failed` rows, one real session (`49ad2afc`), all identical:
`{kind:"client_behind", error:"schema_outdated", status:409, dict_id:"solari"}` on build
`1783245164404` (current is `1783932811749`). A schema migration shipped since she loaded; her old
tab's client DB is behind, so the server 409s every 30s retry. **This is the known-noise stale-build
family** (2026-07-09: no forced-reload will be built). It resolves the moment she reloads. Notably,
LD **already surfaces exactly this** at the aggregate level — `build_sync_health`'s `stuck[]`
(user,dict) pairs on `/admin/health` — so an admin *can* see she's wedged and nudge her. No code
change.

---

## 2. Usage & engagement

**A strong contributor day — and now with the crawler gone, the raw event counts are mostly human.**

| Window | Signed-in sessions | Signed-in users |
|---|---:|---:|
| 24h | **144** | **41** |
| 7d | — | **96** |

**Analytics events (24h):** `dictionary_opened` 2,197 (31 users) · `entry_opened` 1,628 (16 users) ·
`search_performed` 1,338 (15 users) · `audio_played` 690 (**1 user** — one heavy audio reviewer) ·
`entry_created` 82 (12 users) · `entry_deleted` 9 (2 users). Twelve distinct people created entries —
a broad, healthy editing spread, not one power user.

### Who's doing what

**Admins (named — from the `ADMINS` allow-list):**

- **Jacob Bowdoin** (`jwrunner7`, L3) — 298 events / 10 sessions, 22:04–13:16 UTC. Normal admin/dev
  activity; no blocking errors (3 stray `sync_failed` from a background tab, self-healed).
- **Dr. Greg Anderson** (`livingtongues@gmail.com`, L2) — present mostly as his **null-session zombie
  tab** (2,107 `sync_failed` = the bulk of his 2,324 events), plus a *little* real browsing: 4 real
  `session_start`s, 129 heartbeats, 3 `dictionary_opened`, and a handful of benign `i18n missing key`
  warns (semantic-domain / part-of-speech labels). No editing session, no blocking error. Known-noise.
- **Diego Córdova** (L3) & **Anna Luisa Daigneault** (`dictionaries@livingtongues.org`, L2) — **not
  active** this window. *(Note: `annaluisa@livingtongues.org` — 57 events — is a separate non-admin
  account, not the allow-listed admin address.)*

**Everyone else (aggregate, anonymous — never named):** ~40 signed-in non-admin contributors did
normal work — entry creation across many small dictionaries, one contributor doing heavy audio
review, searches, and a Pech-dictionary editor managing partners/contributors (who hit the one
transient network P3 in §1). One live contributor is wedged on a stale tab (`evelyn…`, §1). No
non-admin hit a blocking error.

---

## 3. Performance & geography

**Perf (24h; page_load/dict_boot still bot/slow-connection-mixed here — the dashboard's
`audience_filter` strips bots):**

- `search` **p50 10 ms / p90 30 ms / p95 41 ms** (n=1,342) — instant local Orama search.
- `page_load` **p50 100 ms / p90 2,599 ms / p95 5,488 ms** (n=2,226). p50 stays snappy; the p90/p95
  tail is **higher than yesterday's 110 ms** — but that's a *composition* shift, not a regression:
  with the crawler's fast SSR-only hits gone, the remaining sample skews toward real humans on slower
  connections. Warm human loads still read fast.
- `dict_boot` **p50 1,000 ms / p90 4,990 ms / p95 7,508 ms** (n=623) — p50 1s is a healthy human warm
  boot; the tail is cold/large-dict boots (dashboard strips bot boots).
- `web_vital` — 4,099 rows (feeds the CWV panel).

**Geo (top areas by sessions, 24h):** US-SC **1,325** (residual crawler, down from 28,298 yesterday) ··
then humans: US-VA 115 · US-WA 109 · SG 105 · CN-HE 52 · US-IA 43 · US-CA 40 · US-PA 32 · US-IL 22 ·
BR-CE 22 · US-TX 21 · MY 16 · US-OR 14. Boston-origin US spread with no far-region TTFB tax evident.

**Capability:** **97.7% `opfs-worker`** (2,289) + **2.3% `idb-worker`** (54), **zero below-capability**
Safari. Every real session ran the leader-worker DB tier.

---

## 4. Health & housekeeping

- **Volume:** 24h = 33,482 rows — **−82% vs yesterday's 184,191 record.** The crawl has clearly
  tapered (07-12 184k → 07-13 33k). Signal rows (create/edit/search/audio/perf/nav) sit healthily
  underneath.
- **`logs.db` = 1.54 GB** (1.47 → 1.54, only **+66 MB** vs the +300 MB/day at crawl peak) — the growth
  curve is flattening exactly as predicted once the crawl settled. `logs-archive.db` = 2 MB. Disk
  16.1%. VACUUM-after-prune already dispatched (07-09). No action.
- **Retention:** `log_retention_ran_at` = **15:24 UTC today** (fresh). Oldest row 2026-06-29 (14d) — at
  the 14-day hot-window edge, as designed. ✅
- **Noise ratio:** dominated by three `sync_failed` stale tabs + anon/bot serviceWorker + boot-cascade
  rows, all classified known-noise.

## Host resources (past 24h)

**Plenty of headroom — the calmest day in a week.** 284 host-stats samples (~288 expected; no gap).

- CPU avg **1.9%** · hottest 5-min **10.7%** (vs 52% yesterday — the crawl load is gone) · `load1` max
  **0.98** (well under the 2-core box).
- RAM avg **29.6%** · max **42.9%** (cooler than yesterday's 59.5%) · swap max **3.4%** (64 MB).
- Disk **16.1%** — flat; `logs.db` at 1.54 GB is the only thing worth a periodic glance, and its growth
  has flattened. Nothing to act on.

---

## 5. Coverage gaps (Phase B)

No new high-value gap this run. Standing observations, both LOW:

1. **`Internal Error` client 500s (12, all bots) have no server-side counterpart.** Verified:
   `handleError` in `hooks.server.ts` already logs every SSR failure via `log_server_event` at `crash`
   level with route/path/status — yet `source='server'` crash/error rows = **0** in 24h. So these bot
   500s on `/` are **not SSR failures**; they're almost certainly transient proxy/infra 500s during a
   blue-green container swap (Caddy served a 5xx while the container cycled). The SSR path is already
   covered. Not worth chasing — all-bot, low-freq, no real-user impact.
2. **The stale-tab `schema_outdated` retry is fully instrumented already** (`kind` + `dict_id` +
   `app_version` in context, surfaced by `build_sync_health`). No gap — the only "fix" is a forced
   reload, which is a declined decision (07-09).

---

## 6. Dashboard improvements (Phase C + D)

### Phase C — top proposal (re-grounded in TODAY's data; verified NOT present)

**★★ Bot/crawler share % on each error cluster — reaffirmed, again grounded by hand-work.** To write
§1–§2 I *again* had to hand-classify UAs to learn US-SC was 1,325 crawler sessions (down from 28k),
that `Rejected`/`initial dict sync failed`/`Internal Error` are 100% bot, and that `sync_failed` is
three stale tabs. Verified in source: `ErrorCluster` (`log-analytics.ts:263`) still carries only
`is_noise` (static pattern) — **no `bot_pct`/`bot_sessions`.** Add `bot_sessions` + `bot_pct` (UA
`is_bot_user_agent` ∪ webdriver ∪ frequency-bot) and badge clusters `>90%` as "mostly crawler." Same
GROUP BY, conditional SUM. Filed (backlog line 125).

**Also still open:** the **`max_per_session` "⟳ loop" marker** on `ErrorCluster` (backlog line 150) —
confirmed unshipped (no `max_per_session` in `build_error_clusters`). Cheap; prioritize alongside the
bot-share item.

### Phase D — cross-pollinate (house + tutor, 07-12 reviews)

- **★ Strong inbound signal — the siblings have SHIPPED LD's two open items.** tutor's 07-12 review
  verifies its `ErrorCluster` now has **breadth / ⟳-loop / 🤖-bot-only** badges (its bingbot cluster is
  badged live), and house's error tooling shows **`sess`/`bot`/`stale`** columns. These are *exactly*
  LD's two open backlog proposals (bot-share % + `max_per_session` loop marker). **Recommend: port
  tutor's `build_error_clusters` bot-only + loop-marker implementation to LD** to finally close both —
  the design is proven in two apps now. Noted on the backlog items.
- **house's proposed "wedged clients" sync-health indicator → LD already has it (LD is ahead).** LD
  shipped `build_sync_health` (`SyncHealth.stuck[]` (user,dict) pairs + current-vs-stale `client_behind`
  split) on `/admin/health` back on 07-06 (`24b080b1`) — today's `evelyn…` stuck tab is already
  visible there. house's variant keys on `dirty_rows_stuck`; LD's (user,dict) key is more granular. No
  new port; flag LD's panel for house/tutor to borrow back.
- **Symmetry — LD wins the siblings are still catching up to:** the **Sync-health stuck-(user,dict)
  panel**, the **thin-data perf guard** (`THIN_SAMPLE_N=15`, tutor shipped its copy `ac0e57c3` this
  week), and the **At-a-Glance strip** (both siblings ported it).
- **Skipped (inapplicable):** tutor's mobile/OTA + memory-OOM panels (LD is web-only) and house's
  `/admin/revenue` funnel (LD has no payments) — per the standing skip list.

---

## Recommendations / action items

- [ ] ★★ **Phase C/D (backlog) — finally ship bot-share % + `max_per_session` "⟳ loop" marker on
      `ErrorCluster`.** Both are now shipped in tutor (and house's tooling); port tutor's
      `build_error_clusters` implementation. Filed in `.issues/future/dashboard-improvements.md`
      (lines 125, 150).
- [ ] 🟡 **Watch — search `RangeError` on `/opata/entries`** (0 recurrences today; stays on the 07-12
      watch list — act only if it reappears on the current build or from a 2nd user).
- [ ] ⚪ **Confirm `logs.db` growth stays flat.** +66 MB today (down from +300 MB/day at crawl peak) —
      crawl-taper looks real. No action; glance next run. Disk fine at 16.1%.
- [x] ✅ **evelyn `schema_outdated` stuck tab** — known-noise stale-build family; already visible on the
      shipped Sync-health panel. No action (per 07-09 no-forced-reload decision).

## Trend context (7d)

Daily `client_logs` volume (all rows, crawler-inclusive) — note the sharp taper on 07-13:

<svg viewBox="0 0 560 140" width="100%" style={{maxWidth:'580px'}} role="img" aria-label="daily log volume 7d">
{[['07-07',64616],['07-08',30142],['07-09',151198],['07-10',166989],['07-11',98158],['07-12',184191],['07-13',33482]].map(([d,n],i)=>{const x=24+i*76;const h=Math.min(n/184191*100,100);const settled=d==='07-13';return <g key={d}><rect x={x} y={115-h} width="50" height={h} rx="2" fill={settled?'#10b981':'#8b5cf6'}/><text x={x+25} y="128" textAnchor="middle" fontSize="9" fill="currentColor">{d}</text><text x={x+25} y={111-h} textAnchor="middle" fontSize="8" fill="currentColor" fillOpacity="0.7">{(n/1000).toFixed(0)}k</text></g>)}
<text x="24" y="10" fontSize="9" fill="currentColor" fillOpacity="0.55">07-13 = crawl settled, −82% vs 07-12</text></svg>

Signed-in users 24h / 7d: **41 / 96** (up from 17 / 91) · signed-in sessions 24h: **144**. Capability
97.7% `opfs-worker` (+2.3% idb fallback, zero below-capability). No P1/P2, zero server crashes; every
error cluster is a known bot/stale-tab family. A calm, strongly contributor-driven day — the crawl is
finally behind us.
