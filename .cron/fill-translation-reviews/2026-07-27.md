# Fill translations receipt — 2026-07-27

Terminal state: **staged-for-human-review**

## Observed gaps (before)

- 1303 active English keys × 19 `TRANSLATABLE_LOCALES` (16 published + `ha`/`am`/`or`).
- **2099 missing rows**: 110 keys missing in all 19 locales, plus 9 keys missing only in
  `zh-TW` (8 `import_page.*` + `gl.seh`). Missing namespaces were the new corpus/media/import
  surfaces: `token.*` (38), `timings.*` (11), `export.*` (14), `import_page.*` (32),
  `grammar.*` (5), `entry.*` (5), `source.*` (2), `entry_field.definition`, `audio.playback_failed`.
- **16 `en_changed` rows**, all one key: `about.export_data`
  (`Export Data (CSV, PDF formats)` → `Export Data (CSV, PDF, SQLite)`).
- Pre-existing human-review debt: **8674 rows** flagged `needs_review='ai'` (whole-locale AI seeds
  in `de` 1193 / `zh-TW` 1184, `as` 472, then 311–423 elsewhere). Reported as evidence only —
  no flags were cleared on that basis.

## Safety

- Today's 20:00 UTC cron backup had not landed (newest was `2026-07-26T20-00-01Z`), so
  `~/code/vps-setup/bin/backup-vps-db living` ran first →
  `r2/backups-rolling/db/living/2026-07-27T22-00-59Z.tar.zst` (237.6 MiB, shared.db + 1406 dict DBs,
  online-backup API, WAL-safe).
- Primary container resolved explicitly: `sveltekit_blue` (`sveltekit_green` carries `IS_STANDBY=true`).
- Writes ran as a scripted `better-sqlite3` transaction inside the container, **dry-run first**
  (identical counts, rolled back), then for real. Scratch scripts removed from host + container after.

## Writes

| action | rows |
| --- | --- |
| inserted (`source='ai'`, `needs_review='ai'`, `updated_by_name='AI (fill-translations)'`) | 2099 |
| insert conflicts skipped (`ON CONFLICT DO NOTHING`) | 0 |
| `en_changed` cleared (value updated, flag cleared) | 16 |
| stale `ai`-flagged value refreshed (flag KEPT `ai`) | 3 |

Per locale: 110 each for `es fr de zh-CN sw ru he ar pt id ms bn as hi vi ha am or`, **119** for `zh-TW`.

### `en_changed` triage (Jacob's policy)

All 16 rows were the same key and the same English delta — the format list gained SQLite. Judged a
**small mechanical update** (SQLite is a proper noun, untranslated in every locale), so each value was
rewritten to list all three formats and the flag cleared. No row was left flagged and none was
cleared without touching the value. Nothing was substantive enough to re-flag as `ai`.

Three locales (`de`, `zh-TW`, `as`) never got an `en_changed` flag for that key because their row was
already `needs_review='ai'` (`sync_en_catalog` only flags rows where `needs_review IS NULL`). Their
values still said "CSV, PDF formats", so the value was corrected **while keeping the `ai` flag** —
these stay in the human queue, guarded by a `WHERE value = <exact old string> AND needs_review='ai'`
predicate so a concurrent human edit could not be clobbered.

## Verification

- Structural validator over all 19 authored files: key-set completeness vs the observed gap,
  exact `{token}` parity with English (`{number} {form} {percent} {count} {sentences} {position}
  {total} {answered}`), no malformed braces, no empty values, no untranslated-English leakage,
  no stray whitespace. 2099/2099 clean.
- Terminology anchored per locale by querying existing `source='human'`/`import` rows first
  (entry / sense / sentence / save / send / download / dictionary), e.g. `zh-TW` 詞條·匯入·檔案·傳送,
  German formal *Sie*, `ru` "Подсказки" for Suggestions to avoid colliding with "Предложения"
  (= sentences), `pt` "Guardar" to match the existing Save button.
- Post-write prod re-query: **0 missing, 0 `en_changed`, 1303/1303 in all 19 locales.**
- `pnpm i18n:refresh` succeeded on the first try this time (no 30 s timeout, unlike 2026-07-20) —
  95 locale files written.
- `pnpm test`: 2183 passed / 3 skipped (302 files). Pre-commit hook additionally ran `pnpm check`
  (3113 files, 0 errors) and `lint:fix`.

## Seed diff sanity

44 files, +2655 / −470. Audited key-by-key rather than by line:

- **Added: 119 per locale** = my 110 + 9 keys that already existed in the DB for the other 18 locales
  but predated the 2026-07-20 bake (8 `import_page.*` + `gl.seh`).
- **Changed: 189** = my 19 `about.export_data` rewrites + 170 pre-existing DB values that had drifted
  ahead of the seed since the last bake (English copy edits like `about.import_data` →
  "Import data from any format", "editors" dropped from the collaboration blurb, citation →
  bibliographic-reference wording).
- **Removed: 152** = 7 keys × 19 locales that are no longer in the English catalog
  (`export.checking_images|no_images|checking_audios|no_audios|csv_data|spreadsheet`,
  `entry_field.definition_english` — all superseded by the new export page + `entry_field.definition`)
  plus the 19 `gl."seh "` rows keyed with a trailing space, now correctly `gl.seh`. Verified each is
  absent from `en.json` and inactive in `i18n_keys`.
- No English seed file touched; all 100 locale JSON files parse.

## Delivery

- Commit `4b1bec38` "i18n: AI translation fill 2026-07-27" (locale files only — unrelated working-tree
  changes in `.cron/`/`.issues/` from another session were left unstaged), pushed to `main` → deploy
  webhook fired.
- Post-deploy check: `https://livingdictionaries.app/` 200; `/api/i18n/export` regenerated
  22:42 UTC with the new values (`es token.confirm`, `zh-TW gl.seh`, `ru about.export_data` spot-checked).

## Human-review queue

- `needs_review='ai'`: **8674 → 10773** (+2099 new, +0 net from triage; the 16 `en_changed` rows left
  the queue entirely, the 3 refreshed rows were already in it).
- Nothing here is accepted. Every AI-written row is waiting on a translator at `/translate`;
  "Notify translators" is safe to press.

## Skips

None — no locale/string was judged unsafe for machine translation this pass.
