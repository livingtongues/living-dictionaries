---
at: 2026-07-17T09:00
runs_on: mustang
provider: claude
notify: poly_pings
---

One-shot follow-up: execute the "Delete after settling" section of
`.issues/post-cutover-teardown.md` in ~/code/living-dictionaries. The Supabase→SQLite cutover
completed 2026-07-03; the agreed ≥2-week settling window has now passed and the Phase-B delta
re-migrate was done 2026-07-03, so these are safe to delete per that issue:

1. `scripts/supabase/cutover/` — the one-shot cutover pipeline (lives on in git history).
2. `scripts/supabase/reset-local-db.ts` — obsolete under the SQLite dev flow.
3. `scripts/supabase/create-entry-caches/` — superseded by R2 snapshots + the OPFS
   leader-worker model. FIRST confirm nothing consumes the old Cloudflare entry caches
   (grep the repo + check `cache.livingdictionaries.app` references; note the entries
   worker used to hit it — verify that path is gone).
4. `scripts/types/supabase/` — legacy generated Supabase TS types. Delete ONLY if nothing
   besides possibly `config-supabase.ts` imports them; if `config-supabase.ts` needs them for
   legacy lookups, KEEP them and note that in the issue.

Do NOT touch `scripts/supabase/config-supabase.ts` or `supabase-creds.private` — Jacob's
standing constraint keeps Supabase read access + GCS creds alive (see the Callout in the issue).
Also do NOT delete `scripts/import/` or `scripts/download-audio.ts` (they're port-to-SQLite
items, not deletions).

Note `scripts/` is NOT a workspace member — if you need to typecheck/run anything there,
`pnpm install --ignore-workspace` inside `scripts/`.

When done: update `.issues/post-cutover-teardown.md` (check off the deletions; the file stays
alive for the remaining port-to-SQLite items), then SELF-DELETE this cron file
(`.cron/teardown-2026-07-17.md`) AND remove its row from `.cron/index.md` in the same commit.
Commit the deletions + bookkeeping (this repo commits to main) but do NOT push if the working
tree contains other agents' uncommitted work you'd sweep up — stage only your own paths
explicitly. Report a short summary in this session.
