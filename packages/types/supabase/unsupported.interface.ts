export interface UnsupportedFields {
  dt?: string // date written as 12/Jan/2020
  hm?: string // 1, 2, 3, 4, 5 (homonym)
  semdom?: string // semdom numbers that weren't represented in our abbreviated semantic domains list
}
