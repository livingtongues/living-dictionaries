{
  "name": "@living-dictionaries/functions",
  "version": "0.0.1",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "deploy:specific": "firebase deploy --only functions:supportEmail -P default",
    "deploy:specific:prod": "firebase deploy --only functions:onNewDictionary -P production",
    "logs": "firebase functions:log",
    "test": "vitest -u"
  },
  "engines": {
    "node": "16"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@aws-sdk/client-ses": "^3.100.0",
    "algoliasearch": "^4.13.0",
    "firebase-admin": "^10.2.0",
    "firebase-functions": "^3.21.0",
    "firebase-tools": "^10.8.0"
  },
  "devDependencies": {
    "@living-dictionaries/types": "^1.0.0",
    "@living-dictionaries/parts": "^0.0.1",
    "@firebase/testing": "^0.20.11",
    "fetch-as": "^1.1.0",
    "firebase-functions-test": "^0.2.0",
    "typescript": "^4.6.4",
    "vitest": "^0.10.0"
  },
  "private": true
}
