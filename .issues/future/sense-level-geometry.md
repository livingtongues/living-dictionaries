# Sense-level geometry (deferred — revisit if demand appears)

`coordinates` land on entries + dialects (v1 + UI both shipped). Sense-level was explicitly
deferred: needs a schema migration + UI + sync work. Salvaged from the retired
`future/openapi-progressive-disclosure.md` when it closed (2026-07-31).
