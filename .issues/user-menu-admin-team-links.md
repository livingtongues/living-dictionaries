# User menu admin and team links

Update the signed-in user menu to match the requested navigation:

- ✅ Keep Admin Panel above the team link.
- ✅ Use the same `mdi/shield-account` icon as the admin-area header.
- ✅ Rename the `/chat` link to “Team (editors & contributors)”.
- ✅ Validate with `svelte-fix`, `pnpm check`, `pnpm lint`, the full Vitest suite, and `git diff --check`.
- ✅ Visually verify the `AdminLadder` svelte-look story in light and dark mode at 320px wide.
