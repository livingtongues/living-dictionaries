# Legal pages — SHIPPED + LIVE; one human confirmation outstanding

The TOS rewrite + new Privacy Policy are live (`$lib/legal/{terms-of-use,privacy-policy}.md` via
`LegalPage.svelte`). `/privacy-policy` is public (linked from the footer + dictionary side menu),
`/terms` links the internal policy, and before/after is reviewable at `/admin/legal-review`. Anna is
off-duty (`admins.ts` `notify: false`; triage `account` routing moved to Jacob). All code done.

## Remaining — human, non-code
- **Age 13+ with guardian consent** (was 18+) is flagged in the root `legal-pages-proposal.md` for
  **Greg to confirm** (was Anna/Greg — Anna offboarded). Hand him the proposal doc as the explainer.
